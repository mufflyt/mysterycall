% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manuscript_direction.R
\name{mysterycall_direction_words}
\alias{mysterycall_direction_words}
\alias{mysterycall_get_direction}
\alias{mysterycall_get_change_verb}
\title{Direction and change words wired to the sign of the data}
\usage{
mysterycall_get_direction(
  x,
  positive = "higher",
  negative = "lower",
  zero = "no different",
  tol = 0
)

mysterycall_get_change_verb(
  x,
  increasing = "increasing",
  decreasing = "decreasing",
  stable = "stable",
  tol = 0
)
}
\arguments{
\item{x}{Numeric vector. The signed quantity (e.g. a difference, slope, or
percent change). Its sign selects the word.}

\item{positive}{Word used when \code{x > tol}. Default \code{"higher"}.}

\item{negative}{Word used when \code{x < -tol}. Default \code{"lower"}.}

\item{zero}{Word used when \code{abs(x) <= tol}. Default \code{"no different"}.}

\item{tol}{Non-negative numeric scalar. Values with \code{abs(x) <= tol} are
treated as no change and get the zero word. Default \code{0} (exact zero only).}

\item{increasing}{Word used when \code{x > tol}. Default \code{"increasing"}.}

\item{decreasing}{Word used when \code{x < -tol}. Default \code{"decreasing"}.}

\item{stable}{Word used when \code{abs(x) <= tol}. Default \code{"stable"}.}
}
\value{
A character vector the same length as \code{x}.
}
\description{
Small helpers for manuscript and abstract prose that pick a direction word
from the \strong{sign of a value}, so the words can never drift out of step with
the numbers they describe. Both functions are vectorized and return
\code{NA_character_} for \code{NA} input.

\code{mysterycall_get_direction()} chooses a comparison word (default
\code{"higher"} / \code{"lower"} / \code{"no different"}); \code{mysterycall_get_change_verb()}
chooses a trend word (default \code{"increasing"} / \code{"decreasing"} / \code{"stable"}).
Every word is an argument, so each subspecialty or table can supply its own
vocabulary while the positive/negative/zero mapping stays fixed to the data.
}
\examples{
mysterycall_get_direction(c(2.4, -1.1, 0, NA))
# "higher" "lower" "no different" NA

mysterycall_get_change_verb(c(3.2, -0.4, 0))
# "increasing" "decreasing" "stable"

# Subspecialty-specific vocabulary, still tied to the data sign:
slope <- -0.8
sprintf("The MFM workforce is \%s (\%.1f per year).",
        mysterycall_get_change_verb(slope), slope)

}
\seealso{
Other reporting:
\code{\link[=mysterycall_abstract_numbers]{mysterycall_abstract_numbers()}},
\code{\link[=mysterycall_exclusion_summary]{mysterycall_exclusion_summary()}},
\code{\link[=mysterycall_irr_to_days]{mysterycall_irr_to_days()}},
\code{\link[=mysterycall_results_paragraph]{mysterycall_results_paragraph()}},
\code{\link[=mysterycall_session_snapshot]{mysterycall_session_snapshot()}},
\code{\link[=mysterycall_supplemental_tables]{mysterycall_supplemental_tables()}},
\code{\link[=mysterycall_write_results_paragraph]{mysterycall_write_results_paragraph()}},
\code{\link[=print.mysterycall_abstract_numbers]{print.mysterycall_abstract_numbers()}},
\code{\link[=print.mysterycall_irr_days]{print.mysterycall_irr_days()}},
\code{\link[=print.mysterycall_snapshot]{print.mysterycall_snapshot()}}
}
\concept{reporting}
