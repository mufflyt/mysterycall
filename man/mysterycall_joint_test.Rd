% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/joint_test.R
\name{mysterycall_joint_test}
\alias{mysterycall_joint_test}
\title{Joint likelihood-ratio test for a multi-level predictor}
\usage{
mysterycall_joint_test(model, predictor)
}
\arguments{
\item{model}{A fitted model (\code{glm}, \code{lme4::glmer}/\code{lmer}, \code{glmmTMB}, ...) or a
mysterycall model wrapper (\code{mysterycall_logistic_model},
\code{mysterycall_nb_model}, \code{mysterycall_lmm}, ...); the underlying fit is
extracted automatically.}

\item{predictor}{Name of the predictor to test jointly. Must appear as a
fixed-effect term in the model.}
}
\value{
An object of class \code{"mysterycall_joint_test"}: a list with
\code{predictor}, \code{chisq}, \code{df}, \code{p_value}, \code{dropped_terms} (the terms removed),
\code{logLik_full}, \code{logLik_reduced}. \code{\link[=as.data.frame]{as.data.frame()}} returns a one-row tibble.
}
\description{
Tests whether a categorical predictor matters \emph{as a whole} -- e.g. "does
subspecialty (7 levels) jointly predict access?" -- by refitting the model
without every term that involves the predictor (its main effect and any
interaction) and comparing the two fits with a likelihood-ratio test.
}
\details{
The test statistic is computed directly from the log-likelihoods
(\code{chi^2 = 2(logLik_full - logLik_reduced)}, \verb{df =} the difference in the
number of estimated parameters). This deliberately avoids reading the degrees
of freedom off an \code{anova()} table, which is a known footgun: \code{lme4::glmer}
labels that column \code{"Df"} (already the difference) while \code{glmmTMB} labels it
\code{"Chi Df"}, and grabbing the wrong one yields the total parameter count
instead of the factor's df and a badly wrong p-value.
}
\examples{
d <- data.frame(
  y = rbinom(200, 1, 0.4),
  grp = factor(sample(letters[1:4], 200, TRUE)),
  x = rnorm(200)
)
fit <- glm(y ~ grp + x, family = binomial, data = d)
mysterycall_joint_test(fit, "grp")
}
