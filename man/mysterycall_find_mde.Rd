% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twopart_power.R
\name{mysterycall_find_mde}
\alias{mysterycall_find_mde}
\title{Minimum detectable effect by binary search over a power function}
\usage{
mysterycall_find_mde(
  power_fn,
  lower,
  upper,
  target_power = 0.9,
  tol = 0.01,
  max_iter = 20
)
}
\arguments{
\item{power_fn}{A function of one numeric argument (the effect size) returning
estimated power in \verb{[0, 1]}. Assumed non-decreasing in the effect over
\verb{[lower, upper]}.}

\item{lower, upper}{Effect-size bracket to search. \code{power_fn(upper)} should
meet or exceed \code{target_power}; if it does not, the search returns \code{NA} with
a warning.}

\item{target_power}{Target power. Default \code{0.90}.}

\item{tol}{Stop when the bracket is narrower than \code{tol}. Default \code{0.01}.}

\item{max_iter}{Maximum bisection steps. Default \code{20}.}
}
\value{
A list with \code{mde} (the smallest effect meeting the target, or \code{NA}),
\code{power_at_mde}, \code{iterations}, and \code{history} (a tibble of evaluated
effect/power pairs).
}
\description{
Finds the smallest effect size at which a (monotone) power function reaches a
target power, by bisection. Generic: \code{power_fn} can wrap any of the package's
simulation power calculators (or your own), so the same solver serves wait
times, offer rates, or rate ratios.
}
\examples{
\donttest{
# smallest wait-time gap (treatment mean) detectable at 90\% power
pf <- function(wait_trt) {
  mysterycall_twopart_power(
    n_total = 400, offer_ref = 0.7, offer_trt = 0.7,
    wait_ref = 14, wait_trt = wait_trt, phi = 1.7,
    n_sim = 40, seed = 1
  )$table$pow_wait
}
mysterycall_find_mde(pf, lower = 14, upper = 28, target_power = 0.90)
}
}
