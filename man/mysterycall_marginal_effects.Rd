% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal_effects.R
\name{mysterycall_marginal_effects}
\alias{mysterycall_marginal_effects}
\title{Compute average marginal effects for a Poisson GLM or GLMER}
\usage{
mysterycall_marginal_effects(
  model,
  term = NULL,
  data = NULL,
  type = c("response", "link"),
  eps = 1e-07,
  ...
)
}
\arguments{
\item{model}{A fitted \code{glm} (with \code{family = poisson}), a \code{glmerMod} object
from lme4, a \code{mysterycall_poisson_model}, or a \code{mysterycall_nb_model}
object. For wrapper objects the \verb{$model} component is extracted
automatically. For \code{mysterycall_nb_model} (glmmTMB), average marginal
effects are computed via \code{\link[marginaleffects:slopes]{marginaleffects::avg_slopes()}} and the result is
coerced into the standard \code{term} / \code{level} / \code{ame} / \code{variable_type}
data frame returned by this function.}

\item{term}{Character vector of predictor names for which to compute
marginal effects. Pass the variable name (e.g. \code{"cyl"}), not the
dummy-encoded column name (e.g. \code{"factor(cyl)6"}). If \code{NULL} (default),
effects are computed for all predictors in the model frame.}

\item{data}{Optional data frame to use instead of the stored model data.
Columns must use the original (un-transformed) variable names. If \code{NULL}
(default), the data used to fit the model is retrieved automatically.}

\item{type}{Character scalar, either \code{"response"} (default) or \code{"link"}.
Passed to \code{stats::predict()}. For lme4 models, population-average
predictions (\code{re.form = NA}) are always used.}

\item{eps}{Positive numeric scalar. Step size for numerical differentiation
of continuous predictors. Default \code{1e-7}.}

\item{...}{Additional arguments forwarded to \code{\link[marginaleffects:slopes]{marginaleffects::avg_slopes()}}
for negative-binomial model wrappers.}
}
\value{
A \code{data.frame} with one row per term-level combination and columns:
\describe{
\item{\code{term}}{Character. The predictor name (as supplied in \code{term} or
derived from the model frame). Continuous predictors yield one row;
categorical predictors yield one row per non-reference level.}
\item{\code{level}}{Character or \code{NA_character_}. \code{NA_character_} for
continuous predictors; the factor level name for categorical predictors.
The reference level is omitted from the output.}
\item{\code{ame}}{Numeric. The average marginal effect. For continuous
predictors, the mean finite-difference derivative across all
observations. For categorical predictors, the mean difference in
predicted values versus the reference level.}
\item{\code{variable_type}}{Character. Either \code{"continuous"} or
\code{"categorical"}.}
}
Row names are not set. No special class attributes are added.
}
\description{
Computes average marginal effects for each predictor in a fitted Poisson
GLM (\code{glm} with \code{family = poisson}) or Poisson GLMER (\code{glmerMod} from
lme4). Also accepts a \code{mysterycall_poisson_model} object, from which
the fitted model is extracted automatically.
}
\details{
For continuous predictors, the AME is estimated by numerical
differentiation: predicted values are computed at the predictor bumped up
and down by \code{eps}, and the average finite-difference derivative is
returned. For factor (categorical) predictors, the AME for each non-reference
level is the average difference in predicted values between that level and
the reference level, holding all other predictors constant.

Standard errors and confidence intervals are NOT computed by this function.
Use \code{\link[=mysterycall_bootstrap_ci]{mysterycall_bootstrap_ci()}} for bootstrap-based confidence intervals.
}
\section{Standard errors}{

This function returns only the AME point estimate. Standard errors and
confidence intervals require a bootstrap procedure; see
\code{mysterycall_bootstrap_ci()}.
}

\section{lme4 models}{

For \code{glmerMod} objects, predictions use \code{re.form = NA} so that the
marginal effect is averaged over the population (not conditional on
estimated random effects). The lme4 package must be installed.
}

\section{Formulas with in-line transformations}{

When a formula contains in-line calls such as \code{factor(cyl)}, pass the
underlying variable name (\code{"cyl"}) to \code{term}, not the transformed name.
The function resolves the original column automatically.
}

\examples{
m <- glm(vs ~ wt + factor(cyl), data = mtcars, family = poisson())
mysterycall_marginal_effects(m)
mysterycall_marginal_effects(m, term = "wt")
mysterycall_marginal_effects(m, term = "cyl")
}
\seealso{
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}, \code{\link[=mysterycall_model_metrics]{mysterycall_model_metrics()}},
\code{\link[marginaleffects:slopes]{marginaleffects::avg_slopes()}} for direct use with glmmTMB NB models.

Other outcomes: 
\code{\link{.as_positive_logical}()},
\code{\link{.disp_rate_ci}()},
\code{\link{.fmt_model_pval}()},
\code{\link{.fmt_pvalue}()},
\code{\link{.me_find_mf_col}()},
\code{\link{.me_mf_to_orig}()},
\code{\link{.wait_stats}()},
\code{\link{.wilson_ci}()},
\code{\link{mysterycall_acceptance_rate}()},
\code{\link{mysterycall_acceptance_rate_calc}()},
\code{\link{mysterycall_auto_model}()},
\code{\link{mysterycall_bootstrap_ci}()},
\code{\link{mysterycall_calendar_sensitivity}()},
\code{\link{mysterycall_caller_drift}()},
\code{\link{mysterycall_check_zero_inflation}()},
\code{\link{mysterycall_forest_plot}()},
\code{\link{mysterycall_icc}()},
\code{\link{mysterycall_icc_report}()},
\code{\link{mysterycall_icc_sentence}()},
\code{\link{mysterycall_impute_calls}()},
\code{\link{mysterycall_insurance_acceptance_rates}()},
\code{\link{mysterycall_interaction_sentences}()},
\code{\link{mysterycall_interaction_table}()},
\code{\link{mysterycall_irr_plot}()},
\code{\link{mysterycall_kaplan_meier}()},
\code{\link{mysterycall_lmm}()},
\code{\link{mysterycall_logistic_model}()},
\code{\link{mysterycall_missing_data_analysis}()},
\code{\link{mysterycall_model_mae_rmse}()},
\code{\link{mysterycall_model_metrics}()},
\code{\link{mysterycall_nb_model}()},
\code{\link{mysterycall_nb_power}()},
\code{\link{mysterycall_overdispersion_test}()},
\code{\link{mysterycall_plot_distribution}()},
\code{\link{mysterycall_plot_effect}()},
\code{\link{mysterycall_plot_emmeans_full}()},
\code{\link{mysterycall_plot_emmeans_interaction}()},
\code{\link{mysterycall_plot_inclexcl}()},
\code{\link{mysterycall_plot_residuals}()},
\code{\link{mysterycall_plot_sjplot_interaction}()},
\code{\link{mysterycall_plot_stacked_bar}()},
\code{\link{mysterycall_poisson_model}()},
\code{\link{mysterycall_power_curve}()},
\code{\link{mysterycall_predicted_means}()},
\code{\link{mysterycall_sample_demographics}()},
\code{\link{mysterycall_screen_interactions}()},
\code{\link{mysterycall_screen_predictors}()},
\code{\link{mysterycall_select_best_model}()},
\code{\link{mysterycall_sensitivity}()},
\code{\link{mysterycall_simple_poisson}()},
\code{\link{mysterycall_wait_time_crossover}()},
\code{\link{mysterycall_wait_time_summary}()},
\code{\link{plot.mysterycall_lmm}()},
\code{\link{print.mysterycall_acceptance_rate_calc}()},
\code{\link{print.mysterycall_auto_model}()},
\code{\link{print.mysterycall_calendar_sensitivity}()},
\code{\link{print.mysterycall_caller_drift}()},
\code{\link{print.mysterycall_icc}()},
\code{\link{print.mysterycall_icc_report}()},
\code{\link{print.mysterycall_impute_calls}()},
\code{\link{print.mysterycall_interaction_table}()},
\code{\link{print.mysterycall_lmm}()},
\code{\link{print.mysterycall_logistic_model}()},
\code{\link{print.mysterycall_missing_data}()},
\code{\link{print.mysterycall_model_mae_rmse}()},
\code{\link{print.mysterycall_nb_model}()},
\code{\link{print.mysterycall_nb_power}()},
\code{\link{print.mysterycall_poisson_model}()},
\code{\link{print.mysterycall_power_curve}()},
\code{\link{print.mysterycall_predicted_means}()},
\code{\link{print.mysterycall_sensitivity}()},
\code{\link{print.mysterycall_wait_time_crossover}()},
\code{\link{tidy.mysterycall_lmm}()},
\code{\link{tidy.mysterycall_logistic_model}()},
\code{\link{tidy.mysterycall_poisson_model}()}
}
\concept{outcomes}
