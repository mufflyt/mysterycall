% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/this_one_works.R
\name{find_max_abog_id}
\alias{find_max_abog_id}
\title{Find the highest valid ABOG diplomate ID}
\usage{
find_max_abog_id(
  seed_id,
  torPort = 9150L,
  max_consecutive_misses = 100L,
  sleep_between = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{seed_id}{Integer. A known valid ID to start scanning from (e.g. the
highest ID in your existing dataset).}

\item{torPort}{Integer. Tor SOCKS5 proxy port (default 9150).}

\item{max_consecutive_misses}{Integer. Stop scanning after this many
back-to-back empty/404 responses (default 100).  Increase if the registry
has long gaps between recent additions.}

\item{sleep_between}{Numeric. Seconds to sleep between probes (default 0.3).}

\item{verbose}{Logical. Print progress (default TRUE).}
}
\value{
The highest integer diplomate ID that returned physician data.
}
\description{
Scans upward from \code{seed_id} through the ABOG API (via Tor), tracking
the highest ID that returns a physician record.  Stops after
\code{max_consecutive_misses} consecutive IDs return nothing, which
reliably signals the end of the registry even when IDs are not perfectly
contiguous.
}
\details{
A binary search is NOT used here because ABOG IDs have gaps -- an empty
response at ID N does not imply N+1 is also empty.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
max_id <- find_max_abog_id(seed_id = 9046331, torPort = 9150)
\dontshow{\}) # examplesIf}
}
\keyword{internal}
