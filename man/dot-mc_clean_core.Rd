% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_data.R
\name{.mc_clean_core}
\alias{.mc_clean_core}
\title{Internal: perform all cleaning steps common to both exported functions}
\usage{
.mc_clean_core(
  data,
  id_col,
  age_col,
  age_breaks,
  age_labels,
  wait_col,
  insurance_col,
  gender_col,
  credential_col,
  academic_col,
  rurality_col,
  central_col,
  transfers_col,
  recode_credential,
  recode_academic,
  recode_rurality,
  recode_central,
  drop_pii,
  fill_cols,
  keep_all
)
}
\arguments{
\item{data}{A data frame of raw mystery-caller records (e.g. a
REDCap export).  Must be a \code{data.frame} or subclass thereof.}

\item{id_col}{Character scalar.  Column used to identify unique
physicians for deduplication (first occurrence kept).  Set to \code{NULL} to
skip deduplication. Default \code{"phone"}.}

\item{age_col}{Character scalar or \code{NULL}.  Name of the numeric age
column. When \code{NULL}, age processing is skipped entirely.  Default
\code{"age"}.}

\item{age_breaks}{Numeric vector of cut points passed to \code{\link[base:cut]{base::cut()}}.
Default \code{c(-Inf, 50, 60, 70, Inf)}.}

\item{age_labels}{Character vector of category labels, length
\code{length(age_breaks) - 1}. Default \code{c("<50 yrs", "50-59 yrs", "60-69 yrs", ">=70 yrs")}.}

\item{wait_col}{Character scalar or \code{NULL}.  Wait-time column (business
days). Renamed to \code{"Business days until appointment"} in output. Default
\code{"business_days_until_appointment"}.}

\item{insurance_col}{Character scalar or \code{NULL}. Default \code{"insurance"}.
Renamed to \code{"Insurance"} in output.}

\item{gender_col}{Character scalar or \code{NULL}. Default \code{"gender"}.
Renamed to \code{"Gender"} in output.}

\item{credential_col}{Character scalar or \code{NULL}. Column holding raw NPPES
credential strings (\code{"MD"}, \code{"DO"}, etc.).  Default
\code{"Provider.Credential.Text"}. Renamed to \code{"Physician credential"} in
output.}

\item{academic_col}{Character scalar or \code{NULL}. Default
\code{"academic_affiliation"}. Renamed to \code{"Academic affiliation"} in output.}

\item{rurality_col}{Character scalar or \code{NULL}. CBSA type column. Default
\code{"cbsatype10"}. Renamed to \code{"Rurality"} in output.}

\item{central_col}{Character scalar or \code{NULL}. Central-scheduling
indicator column. Default \code{"central_number_e_g_appointment_center"}.
Renamed to \code{"Central scheduling"} in output.}

\item{transfers_col}{Character scalar or \code{NULL}. Number-of-transfers
column.  No recode is applied; the column is kept as-is. Default \code{NULL}
(skip).}

\item{recode_credential}{Named character vector mapping raw credential values
to display labels. Default \code{c("MD" = "Allopathic training", "DO" = "Osteopathic training")}.}

\item{recode_academic}{Named character vector mapping raw academic-affiliation
values. Default \code{c("University" = "Academic Practice")}.}

\item{recode_rurality}{Named character vector mapping raw CBSA-type values.
Default \code{c("Metro" = "Metropolitan area", "Micro" = "Rural area")}.}

\item{recode_central}{Named character vector mapping raw central-scheduling
values. Default \code{c("Yes" = "Yes, central scheduling")}.}

\item{drop_pii}{Logical.  When \code{TRUE} (default), columns named
\code{first}, \code{last}, \code{middle}, \code{phone}, \code{address}, \code{notes}, \code{record_id},
\code{npi}, and \code{zip} (case-insensitive) are removed from the output.}

\item{fill_cols}{Character vector of column names to forward-fill
(last-observation-carried-forward) before any recoding or renaming. Uses
\code{tidyr::fill()} when the package is installed, then \code{zoo::na.locf()}, then
a pure-base-R loop. Columns absent from \code{data} are silently ignored.
Default \code{c("academic_affiliation", "cbsatype10")}.}

\item{keep_all}{Logical. When \code{TRUE}, the full column set (minus PII) is
returned.  When \code{FALSE}, only the set of standardized columns defined by
the \verb{*_col} parameters is returned.}
}
\value{
A cleaned data frame.
}
\description{
Called by both \code{mysterycall_clean_data()} and
\code{mysterycall_clean_data_keep_identifiers()}.
}
\keyword{internal}
