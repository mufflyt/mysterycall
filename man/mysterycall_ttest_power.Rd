% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytic_power.R
\name{mysterycall_ttest_power}
\alias{mysterycall_ttest_power}
\title{Analytic power for a two-group continuous outcome under unequal allocation}
\usage{
mysterycall_ttest_power(mde, sd, group_frac = 0.5, alpha = 0.05, power = 0.8)
}
\arguments{
\item{mde}{Minimum detectable effect (difference in group means, same units
as \code{sd}). May be a vector; one result row per value.}

\item{sd}{Common within-group standard deviation.}

\item{group_frac}{Fraction of the sample in the smaller (exposure) group,
in \verb{(0, 1)}. Default \code{0.5} (equal allocation).}

\item{alpha}{Two-sided significance level. Default \code{0.05}.}

\item{power}{Target power. Default \code{0.80}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}}, one row per \code{mde}: \code{mde}, \code{cohens_d},
\code{n_per_group_equal}, \code{equal_total_n}, \code{natural_n_small}, \code{natural_n_large},
\code{natural_total_n}.
}
\description{
The package's analytic power tools are all count/binary; a mystery-caller
study with a continuous outcome (wait days) and a \emph{fixed natural allocation}
-- e.g. only 15\% of practices are rural -- needs a Cohen's-d two-sample
calculation that a naive equal-N formula gets wrong. This reports both the
equal-allocation sample size and the total N required under the study's real
group split, found by a binary search over \code{\link[pwr:pwr.t2n.test]{pwr::pwr.t2n.test()}}.
}
\details{
Requires the \pkg{pwr} package.
}
\examples{
if (requireNamespace("pwr", quietly = TRUE)) {
  mysterycall_ttest_power(mde = c(3, 5, 7), sd = 12, group_frac = 0.15)
}
}
\seealso{
\code{\link[=mysterycall_lm_interaction_power]{mysterycall_lm_interaction_power()}}, \code{\link[=mysterycall_power_curve]{mysterycall_power_curve()}}

Other power: 
\code{\link{mysterycall_adjusted_power}()},
\code{\link{mysterycall_lm_interaction_power}()}
}
\concept{power}
