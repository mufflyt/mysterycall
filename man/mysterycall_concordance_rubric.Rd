% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concordance.R
\name{mysterycall_concordance_rubric}
\alias{mysterycall_concordance_rubric}
\title{Define a Guideline-Concordance Rubric}
\usage{
mysterycall_concordance_rubric(
  item,
  label = item,
  type = "binary",
  correct = TRUE,
  reference_col = NA_character_,
  tier_lookup = NULL,
  weight = 1,
  na_policy = "not_applicable",
  option_sep = ";"
)
}
\arguments{
\item{item}{Character vector. Machine-readable item ids. Each must match a
column in the captured-call data passed to
\code{\link[=mysterycall_score_concordance]{mysterycall_score_concordance()}}. Must be unique.}

\item{label}{Character vector. Manuscript-facing wording per item. Recycled
to \code{length(item)}. Default: \code{item}.}

\item{type}{Character vector. One of \code{"binary"}, \code{"expected_present"},
\code{"reference_match"}, \code{"evidence_tier"}. Recycled. Default \code{"binary"}.
\describe{
\item{\code{binary}}{Concordant when the value equals \code{correct}.}
\item{\code{expected_present}}{Concordant when the cue was elicited at all
(\code{correct} is forced to \code{TRUE}).}
\item{\code{reference_match}}{Concordant when the value equals a per-row
reference column named by \code{reference_col} (gold standard varies by
scenario / state / date).}
\item{\code{evidence_tier}}{The value is a set of named options per call;
concordant when it includes at least one option in the \code{"concordant"}
tier of \code{tier_lookup}.}
}}

\item{correct}{Expected value(s) for \code{binary} items. A scalar (recycled), a
vector of length \code{length(item)}, or a list. Ignored for \code{reference_match}
and \code{evidence_tier}. Default \code{TRUE}.}

\item{reference_col}{Character vector. For \code{reference_match} items, the name
of the per-row gold-standard column. Recycled. Default \code{NA}.}

\item{tier_lookup}{Data frame or \code{NULL}. For \code{evidence_tier} items, a table
with columns \code{option}, \code{tier} (\code{"concordant"} / \code{"nonconcordant"} /
\code{"neutral"}) and optional \code{effective_from} / \code{effective_to} dates for
date-versioned guidelines. Default \code{NULL}.}

\item{weight}{Numeric vector. Composite-score weights (> 0). Recycled.
Default \code{1}.}

\item{na_policy}{Character vector. How to treat uncodable / unreached items:
\code{"not_applicable"} drops the item from that call's denominator (the natural
handling for within-encounter conversation exits); \code{"incorrect"} counts it
as non-concordant against a full denominator. Recycled. Default
\code{"not_applicable"}.}

\item{option_sep}{Character scalar. Delimiter for \code{evidence_tier} option
columns supplied as strings. Default \code{";"}.}
}
\value{
A \code{mysterycall_rubric} object: a list with element \code{items} (a
\code{\link[tibble:tibble]{tibble::tibble()}} of the validated item specifications), \code{tier_lookup},
and \code{option_sep}.
}
\description{
Builds the reference standard that \code{\link[=mysterycall_score_concordance]{mysterycall_score_concordance()}} scores
captured calls against. A rubric is a set of typed items; each study in the
mystery-caller counseling / guideline-adherence literature is expressed as a
rubric assembled from four item types.
}
\examples{
# Lungfiel 2023 - a four-item counseling-quality checklist
mysterycall_concordance_rubric(
  item  = c("advised_window", "take_asap", "redose_after_vomit"),
  label = c("Advised on differing windows of effect",
            "Advised to take as soon as possible",
            "Advised re-dosing after vomiting"),
  type  = "binary", correct = TRUE
)
}
\seealso{
\code{\link[=mysterycall_score_concordance]{mysterycall_score_concordance()}}

Other concordance: 
\code{\link{mysterycall_concordance_kappa}()},
\code{\link{mysterycall_concordance_sentence}()},
\code{\link{mysterycall_score_concordance}()}
}
\concept{concordance}
