% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_coverage.R
\name{mysterycall_scenario_coverage}
\alias{mysterycall_scenario_coverage}
\title{Per-cluster scenario coverage for a matched multi-scenario audit}
\usage{
mysterycall_scenario_coverage(
  data,
  id_col,
  scenario_col,
  scenarios = NULL,
  output_dir = NA,
  filename = NA
)
}
\arguments{
\item{data}{A long data frame: one row per cluster x scenario call.}

\item{id_col}{Cluster / practice identifier column.}

\item{scenario_col}{Scenario column.}

\item{scenarios}{Optional character vector naming the scenario levels the
design intended. When \code{NULL} (default) all non-missing values found in
\code{scenario_col} are used. A "complete" cluster is one observed under every
scenario in this set. Names not present in the data are still reported (as
fully missing), so you can assert the design you \emph{meant} to run.}

\item{output_dir, filename}{Optional CSV export of the per-cluster coverage
table. \code{output_dir = NA} (default) skips writing; \code{NULL} writes to a session
temp directory via \code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}.}
}
\value{
An object of class \code{"mysterycall_scenario_coverage"}: a list with
\describe{
\item{\code{coverage}}{\code{\link[tibble:tibble]{tibble::tibble()}}, one row per cluster: \code{id}, a logical
\verb{has_<scenario>} column per scenario level, and \code{n_scenarios} (how many
of the intended scenarios were observed).}
\item{\code{summary}}{\code{\link[tibble:tibble]{tibble::tibble()}} design totals: \code{n_clusters},
\code{n_complete} (observed under all scenarios), \code{n_partial}, \code{n_singleton},
plus one \verb{n_missing_<scenario>} count per scenario.}
\item{\code{scenarios}}{The scenario levels used.}
}
\code{\link[=as.data.frame]{as.data.frame()}} returns the per-cluster \code{coverage} table.
}
\description{
A matched mystery-caller design calls the \emph{same} practice under two or more
scenarios (insurance types, caller personas), and the paired analyses
(\code{\link[=mysterycall_paired_acceptance_mcnemar]{mysterycall_paired_acceptance_mcnemar()}},
\code{\link[=mysterycall_paired_wait_within_practice]{mysterycall_paired_wait_within_practice()}}) only draw information from
clusters observed under both arms of a contrast. Before running them it is
worth knowing how complete the design actually is: how many practices were
reached under the \emph{full} set of scenarios versus only a partial set, and which
scenario is short. A mistyped or unmatched cluster key silently becomes a
one-scenario singleton and deflates the paired denominator, so this coverage
check is a design-integrity guard as much as a descriptive table. Pair it with
\code{\link[=mysterycall_flag_near_duplicate_keys]{mysterycall_flag_near_duplicate_keys()}} to catch the mistyped keys that cause
spurious singletons.
}
\examples{
d <- data.frame(
  practice = c(1, 1, 1, 2, 2, 3),
  scenario = c("Straight", "Lesbian", "Single mother",
               "Straight", "Lesbian", "Straight")
)
mysterycall_scenario_coverage(d, "practice", "scenario")
}
\seealso{
\code{\link[=mysterycall_paired_acceptance_mcnemar]{mysterycall_paired_acceptance_mcnemar()}},
\code{\link[=mysterycall_flag_near_duplicate_keys]{mysterycall_flag_near_duplicate_keys()}}

Other descriptive helpers: 
\code{\link{mysterycall_demographics_sentence}()},
\code{\link{mysterycall_descriptive_stats}()},
\code{\link{mysterycall_distribution_summary}()},
\code{\link{mysterycall_facet_histogram}()},
\code{\link{mysterycall_log_histogram}()},
\code{\link{mysterycall_physicians_with_detail}()},
\code{\link{mysterycall_scenario_summary}()},
\code{\link{mysterycall_sensitivity_both_insurance}()}
}
\concept{descriptive helpers}
