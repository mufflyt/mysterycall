% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_inclusion.R
\name{mysterycall_exclusion_crosswalk}
\alias{mysterycall_exclusion_crosswalk}
\title{Canonical exclusion-code / label-string crosswalk}
\usage{
mysterycall_exclusion_crosswalk(
  inclusion_value = "Able to contact",
  logistic_include_codes = c(0L, 7L, 9L, 10L)
)
}
\arguments{
\item{inclusion_value}{Character scalar. The \code{reason_for_exclusions} value
the label path treats as included. Default \code{"Able to contact"}.}

\item{logistic_include_codes}{Integer vector. Exclusion codes the integer
path includes in the logistic dataset. Default \code{c(0L, 7L, 9L, 10L)} -
matches \code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}}, one row per known exclusion code, with columns:
\describe{
\item{\code{code}}{Integer exclusion code (as used by
\code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}}).}
\item{\code{code_label}}{Human-readable label for the integer code.}
\item{\code{label_category}}{Semantic key from
\code{\link[=mysterycall_exclusion_summary]{mysterycall_exclusion_summary()}}'s \code{exclusion_categories}, or \code{NA} for
codes with no label-path category (\code{included}, or codes the label path
does not bucket).}
\item{\code{label_string}}{Default \code{reason_for_exclusions} string the label
path expects for this code, or \code{NA} where none exists.}
\item{\code{reached}}{Logical. Was the office reached? (Distinguishes
reached-but-excluded codes from unreachable ones.)}
\item{\code{in_logistic}}{Logical. Integer path includes this code in the
logistic (appointment-offered) dataset.}
\item{\code{in_waittime}}{Logical. Integer path includes this code in the
wait-time dataset (code 0 only).}
\item{\code{label_included}}{Logical. Label path counts this code as included
(\code{"Able to contact"}, i.e. code 0 only).}
}
}
\description{
The package contains two inclusion conventions that evolved independently:
}
\details{
\itemize{
\item the \strong{integer-code path} (\code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}},
\code{\link[=mysterycall_strobe_flow]{mysterycall_strobe_flow()}}) derives samples from an integer
\code{exclusions} column on the \emph{raw} REDCap export; and
\item the \strong{label-string path} (\code{\link[=mysterycall_exclusion_summary]{mysterycall_exclusion_summary()}},
\code{\link[=mysterycall_acceptance_rate_calc]{mysterycall_acceptance_rate_calc()}},
\code{\link[=mysterycall_insurance_acceptance_rates]{mysterycall_insurance_acceptance_rates()}}, and the workflow runners)
derives samples from a \code{reason_for_exclusions} string column on the
\emph{cleaned} export, treating \code{"Able to contact"} as the included set.
}

The two paths do \strong{not} define the same included set. This function returns
the mapping between them as data, so the divergence is inspectable rather
than implicit. It is the single source of truth used by
\code{\link[=mysterycall_reconcile_inclusion]{mysterycall_reconcile_inclusion()}}.
}
\section{The key divergence}{

The integer logistic set (\code{logistic_include_codes}, default
\code{c(0, 7, 9, 10)}) keeps codes 7 (referral required), 9 (not accepting new
patients), and 10 (must see midlevel first) as \emph{included} calls with an
appointment-not-offered outcome. The label path classifies those same calls
as \strong{exclusions} — they are never \code{"Able to contact"}. Rows where
\code{in_logistic} is \code{TRUE} but \code{label_included} is \code{FALSE} are exactly that gap.
}

\examples{
cw <- mysterycall_exclusion_crosswalk()
# The three codes where the two inclusion definitions disagree:
cw[cw$in_logistic & !cw$label_included, c("code", "code_label")]
}
\seealso{
\code{\link[=mysterycall_reconcile_inclusion]{mysterycall_reconcile_inclusion()}},
\code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}}, \code{\link[=mysterycall_exclusion_summary]{mysterycall_exclusion_summary()}}

Other data-preparation:
\code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}},
\code{\link[=mysterycall_reconcile_inclusion]{mysterycall_reconcile_inclusion()}},
\code{\link[=print.mysterycall_prepared]{print.mysterycall_prepared()}},
\code{\link[=print.mysterycall_reconciliation]{print.mysterycall_reconciliation()}}
}
\concept{data-preparation}
