% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_data.R
\name{mysterycall_clean_data}
\alias{mysterycall_clean_data}
\title{Clean raw mystery-caller data for publication-ready analysis}
\usage{
mysterycall_clean_data(
  data,
  id_col = "phone",
  age_col = "age",
  age_breaks = c(-Inf, 50, 60, 70, Inf),
  age_labels = c("<50 yrs", "50-59 yrs", "60-69 yrs", ">=70 yrs"),
  wait_col = "business_days_until_appointment",
  insurance_col = "insurance",
  gender_col = "gender",
  credential_col = "Provider.Credential.Text",
  academic_col = "academic_affiliation",
  rurality_col = "cbsatype10",
  central_col = "central_number_e_g_appointment_center",
  transfers_col = NULL,
  recode_credential = c(MD = "Allopathic training", DO = "Osteopathic training"),
  recode_academic = c(University = "Academic Practice"),
  recode_rurality = c(Metro = "Metropolitan area", Micro = "Rural area"),
  recode_central = c(Yes = "Yes, central scheduling"),
  drop_pii = TRUE,
  fill_cols = c("academic_affiliation", "cbsatype10")
)
}
\arguments{
\item{data}{A data frame of raw mystery-caller records (e.g. a
REDCap export).  Must be a \code{data.frame} or subclass thereof.}

\item{id_col}{Character scalar.  Column used to identify unique
physicians for deduplication (first occurrence kept).  Set to \code{NULL} to
skip deduplication. Default \code{"phone"}.}

\item{age_col}{Character scalar or \code{NULL}.  Name of the numeric age
column. When \code{NULL}, age processing is skipped entirely.  Default
\code{"age"}.}

\item{age_breaks}{Numeric vector of cut points passed to \code{\link[base:cut]{base::cut()}}.
Default \code{c(-Inf, 50, 60, 70, Inf)}.}

\item{age_labels}{Character vector of category labels, length
\code{length(age_breaks) - 1}. Default \code{c("<50 yrs", "50-59 yrs", "60-69 yrs", ">=70 yrs")}.}

\item{wait_col}{Character scalar or \code{NULL}.  Wait-time column (business
days). Renamed to \code{"Business days until appointment"} in output. Default
\code{"business_days_until_appointment"}.}

\item{insurance_col}{Character scalar or \code{NULL}. Default \code{"insurance"}.
Renamed to \code{"Insurance"} in output.}

\item{gender_col}{Character scalar or \code{NULL}. Default \code{"gender"}.
Renamed to \code{"Gender"} in output.}

\item{credential_col}{Character scalar or \code{NULL}. Column holding raw NPPES
credential strings (\code{"MD"}, \code{"DO"}, etc.).  Default
\code{"Provider.Credential.Text"}. Renamed to \code{"Physician credential"} in
output.}

\item{academic_col}{Character scalar or \code{NULL}. Default
\code{"academic_affiliation"}. Renamed to \code{"Academic affiliation"} in output.}

\item{rurality_col}{Character scalar or \code{NULL}. CBSA type column. Default
\code{"cbsatype10"}. Renamed to \code{"Rurality"} in output.}

\item{central_col}{Character scalar or \code{NULL}. Central-scheduling
indicator column. Default \code{"central_number_e_g_appointment_center"}.
Renamed to \code{"Central scheduling"} in output.}

\item{transfers_col}{Character scalar or \code{NULL}. Number-of-transfers
column.  No recode is applied; the column is kept as-is. Default \code{NULL}
(skip).}

\item{recode_credential}{Named character vector mapping raw credential values
to display labels. Default \code{c("MD" = "Allopathic training", "DO" = "Osteopathic training")}.}

\item{recode_academic}{Named character vector mapping raw academic-affiliation
values. Default \code{c("University" = "Academic Practice")}.}

\item{recode_rurality}{Named character vector mapping raw CBSA-type values.
Default \code{c("Metro" = "Metropolitan area", "Micro" = "Rural area")}.}

\item{recode_central}{Named character vector mapping raw central-scheduling
values. Default \code{c("Yes" = "Yes, central scheduling")}.}

\item{drop_pii}{Logical.  When \code{TRUE} (default), columns named
\code{first}, \code{last}, \code{middle}, \code{phone}, \code{address}, \code{notes}, \code{record_id},
\code{npi}, and \code{zip} (case-insensitive) are removed from the output.}

\item{fill_cols}{Character vector of column names to forward-fill
(last-observation-carried-forward) before any recoding or renaming. Uses
\code{tidyr::fill()} when the package is installed, then \code{zoo::na.locf()}, then
a pure-base-R loop. Columns absent from \code{data} are silently ignored.
Default \code{c("academic_affiliation", "cbsatype10")}.}
}
\value{
A cleaned \code{data.frame} with:
\itemize{
\item Deduplicated rows (one row per physician).
\item An \code{"Age category"} column (ordered factor) when \code{age_col} is
present.
\item Recoded and frequency-ordered factor columns.
\item Human-readable, title-case column names for all recognised fields.
\item PII columns removed when \code{drop_pii = TRUE}.
}
}
\description{
Transforms a raw REDCap / Phase-2 export into a tidy, human-readable data
frame suitable for \code{gtsummary::tbl_summary()}, regression models, and
manuscript tables.  Steps include deduplication, age categorisation, value
recoding, factor-level ordering by frequency, column renaming to title-case
labels, and optional PII removal.
}
\section{Recode convention}{

Each \verb{recode_*} argument is a named character vector where \strong{names} are
the raw (input) values and \strong{values} are the desired display labels.
Non-matching values are left unchanged.  Pass an empty vector (\code{character(0)})
to suppress recoding for a particular column.
}

\section{Optional dependencies}{

\code{tidyr} (in \code{Suggests}) is used for forward-filling; \code{zoo} and \code{forcats}
are used opportunistically when installed.  All three are optional: the
function degrades gracefully to built-in fallbacks when they are absent.
}

\examples{
# Minimal example with base R only ------------------------------------------
set.seed(42)
raw <- data.frame(
  phone         = c("303-111-2222", "303-111-2222", "720-555-9999"),
  age           = c(55, 55, 72),
  insurance     = c("Medicaid", "Medicaid", "Medicare"),
  gender        = c("Female", "Female", "Male"),
  Provider.Credential.Text = c("MD", "MD", "DO"),
  academic_affiliation     = c("University", NA, "Community"),
  cbsatype10               = c("Metro", "Metro", "Micro"),
  business_days_until_appointment = c(7L, 7L, 14L),
  central_number_e_g_appointment_center = c("Yes", "Yes", "No"),
  first  = c("Jane", "Jane", "Bob"),
  last   = c("Doe",  "Doe",  "Smith"),
  record_id = 1:3,
  stringsAsFactors = FALSE
)

cleaned <- mysterycall_clean_data(raw)
print(names(cleaned))
print(cleaned[["Age category"]])

# Custom age breaks ---------------------------------------------------------
cleaned2 <- mysterycall_clean_data(
  raw,
  age_breaks = c(-Inf, 45, 65, Inf),
  age_labels = c("<45 yrs", "45-64 yrs", ">=65 yrs")
)
print(cleaned2[["Age category"]])

# Skip age processing entirely ----------------------------------------------
cleaned3 <- mysterycall_clean_data(raw, age_col = NULL)
stopifnot(!"Age category" \%in\% names(cleaned3))
}
\seealso{
\code{\link[=mysterycall_clean_data_keep_identifiers]{mysterycall_clean_data_keep_identifiers()}} for a variant that
retains PII columns; \code{\link[=mysterycall_reorder_by_freq]{mysterycall_reorder_by_freq()}} for the base-R
frequency-ordering used as a \code{forcats} fallback.

Other data utilities:
\code{\link[=mysterycall_clean_data_keep_identifiers]{mysterycall_clean_data_keep_identifiers()}}
}
\concept{data utilities}
