% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical.R
\name{mysterycall_test_categorical}
\alias{mysterycall_test_categorical}
\title{Association Test for a Contingency Table (auto chi-squared / Fisher)}
\usage{
mysterycall_test_categorical(
  data,
  row_var,
  col_var,
  method = c("auto", "chisq", "fisher"),
  min_expected = 5,
  correct = TRUE,
  posthoc = FALSE,
  p_adjust = "BH",
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame.}

\item{row_var, col_var}{Character scalars. Names of the two categorical
columns to cross-tabulate.}

\item{method}{Character. \code{"auto"} (default) uses an exact test when any
expected count is \verb{< min_expected}, otherwise chi-squared; \code{"chisq"} and
\code{"fisher"} force the choice. For tables larger than 2x2 the exact test is
the Fisher-Freeman-Halton test (simulated if the exact computation is
infeasible).}

\item{min_expected}{Numeric. Expected-count threshold that triggers the exact
test under \code{"auto"}. Default \code{5}.}

\item{correct}{Logical. Apply Yates' continuity correction for 2x2
chi-squared tests. Default \code{TRUE}.}

\item{posthoc}{Logical. If \code{TRUE} and the outcome is binary and the overall
test is significant at \code{1 - conf_level}, compute pairwise proportion
comparisons across the other variable's levels. Default \code{FALSE}.}

\item{p_adjust}{Character. Multiple-comparison method for post-hoc p-values,
passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}. Default \code{"BH"}.}

\item{conf_level}{Numeric. Confidence level (drives the post-hoc alpha).
Default \code{0.95}.}
}
\value{
A \code{mysterycall_categorical_test} object: a list with \code{method},
\code{statistic}, \code{df}, \code{p_value}, \code{cramers_v}, \code{effect_size} (label),
\code{n}, \code{min_expected}, \code{table}, and \code{posthoc} (a tibble or \code{NULL}).
}
\description{
Cross-tabulates two categorical variables and tests their association,
automatically falling back from Pearson's chi-squared to an exact test when
expected cell counts are small -- the standard pattern in mystery-caller
audits (Lungfiel 2023, Sharma 2025, Hodson 2025). Reports Cramer's V as an
effect size and, optionally, Benjamini-Hochberg-adjusted post-hoc pairwise
proportion comparisons when the outcome is binary.
}
\examples{
df <- data.frame(
  offered = c(rep("yes", 30), rep("no", 10), rep("yes", 18), rep("no", 22)),
  payer   = c(rep("commercial", 40), rep("medicaid", 40))
)
res <- mysterycall_test_categorical(df, "offered", "payer", posthoc = TRUE)
res
}
\seealso{
Other categorical: 
\code{\link{mysterycall_cmh_test}()},
\code{\link{mysterycall_compare_ranks}()},
\code{\link{mysterycall_prevalence_ci}()}
}
\concept{categorical}
