% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode_city_state.R
\name{mysterycall_geocode_city_state}
\alias{mysterycall_geocode_city_state}
\title{Look up latitude/longitude for city + state}
\usage{
mysterycall_geocode_city_state(city, state, overrides = NULL)
}
\arguments{
\item{city}{Character vector of city names (case/space-insensitive).}

\item{state}{Character vector of two-letter state abbreviations, the same
length as \code{city}.}

\item{overrides}{Optional data frame of manual coordinates for places the
bundled table misses (townships, renamed municipalities). Must have columns
\code{city}, \code{state}, \code{lat}, \code{lon}; matched case-insensitively and applied on top
of the table lookup.}
}
\value{
A tibble with columns \code{city}, \code{state} (both upper-cased/trimmed),
\code{lat}, \code{lon}. Unmatched rows get \code{NA} coordinates.
}
\description{
A lightweight geocoder for the common case where a provider roster carries a
city and a two-letter state but no coordinates. It joins against the package's
bundled \code{city_state_to_lat_long} table (roughly 32,000 US places), so it needs
no network and no API key -- enough to seed a distance caliper for matched
sampling or to QC how far apart matched practices sit. For rooftop-accurate
geocoding of full street addresses, use a dedicated geocoding service instead.
}
\examples{
mysterycall_geocode_city_state(c("Denver", "Boston"), c("CO", "MA"))
}
