% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_both_insurance.R
\name{mysterycall_sensitivity_both_insurance}
\alias{mysterycall_sensitivity_both_insurance}
\title{Sensitivity analysis: physicians called under both insurance types}
\usage{
mysterycall_sensitivity_both_insurance(
  data,
  phone_col = "phone",
  insurance_col = "insurance",
  outcome_col = "business_days_until_appointment",
  medicaid_label = "medicaid",
  bcbs_label = "blue cross/blue shield",
  output_dir = NULL,
  filename = "sensitivity_both_insurance.csv"
)
}
\arguments{
\item{data}{A data frame of mystery-caller records. Must contain \code{phone_col},
\code{insurance_col}, and \code{outcome_col}.}

\item{phone_col}{Character scalar. Column that uniquely identifies each
physician (e.g. phone number or NPI). Default \code{"phone"}.}

\item{insurance_col}{Character scalar. Name of the column recording insurance
type. Default \code{"insurance"}.}

\item{outcome_col}{Character scalar. Name of the numeric column holding
wait-time days. Default \code{"business_days_until_appointment"}.}

\item{medicaid_label}{Character scalar. Lowercase comparison value for
Medicaid after \code{tolower(trimws(...))}. Default \code{"medicaid"}.}

\item{bcbs_label}{Character scalar. Lowercase comparison value for Blue Cross
after normalization. Default \code{"blue cross/blue shield"}.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for CSV output.
\code{NULL} (default) writes to a session temp directory via
\code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing.}

\item{filename}{Character scalar. CSV file name.
Default \code{"sensitivity_both_insurance.csv"}.}
}
\value{
A named list with seven elements:
\describe{
\item{\code{n_both}}{Integer. Number of physicians with records under both
Medicaid and BCBS.}
\item{\code{n_total}}{Integer. Total unique physicians (by \code{phone_col}).}
\item{\code{both_data}}{\code{\link[tibble:tibble]{tibble::tibble()}} of rows for physicians with both
insurance types (insurance column is normalized to lowercase).}
\item{\code{wait_comparison}}{\code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{insurance},
\code{mean_wait}, \code{sd_wait}, \code{median_wait}, \code{iqr_wait} for each insurance
type. Empty tibble when \code{n_both == 0}.}
\item{\code{t_test}}{Object returned by a \strong{paired} \code{\link[stats:t.test]{stats::t.test()}} on one
observation per physician (their mean wait in each arm), reflecting the
paired design, or \code{NULL} when there are insufficient data.}
\item{\code{sentence}}{Character scalar. Manuscript-ready descriptive sentence.}
\item{\code{phone_ids}}{Character vector of phone numbers (or IDs) for
physicians called under both insurance types.}
}
}
\description{
Identifies physicians (by \code{phone_col}) who were called under both Medicaid
and Blue Cross/Blue Shield, then compares their wait times between insurance
types with summary statistics and a t-test. Mirrors the sensitivity analysis
in the mystery-caller study Rmd (lines 1469-1505).
}
\examples{
set.seed(42)
df <- data.frame(
  phone      = rep(paste0("P", 1:10), each = 2L),
  insurance  = rep(c("Medicaid", "Blue Cross/Blue Shield"), 10L),
  business_days_until_appointment = rpois(20L, 14),
  stringsAsFactors = FALSE
)
res <- mysterycall_sensitivity_both_insurance(df, output_dir = NA)
cat(res$sentence)
}
\seealso{
\code{\link[=mysterycall_scenario_summary]{mysterycall_scenario_summary()}}

Other descriptive helpers: 
\code{\link{mysterycall_demographics_sentence}()},
\code{\link{mysterycall_descriptive_stats}()},
\code{\link{mysterycall_distribution_summary}()},
\code{\link{mysterycall_facet_histogram}()},
\code{\link{mysterycall_log_histogram}()},
\code{\link{mysterycall_physicians_with_detail}()},
\code{\link{mysterycall_scenario_coverage}()},
\code{\link{mysterycall_scenario_summary}()}
}
\concept{descriptive helpers}
