% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/county_covariates.R
\name{mysterycall_get_county_provider_counts}
\alias{mysterycall_get_county_provider_counts}
\title{Count distinct providers per county}
\usage{
mysterycall_get_county_provider_counts(
  providers,
  npi_col = "npi",
  county_col = "fips_county",
  taxonomy_col = NULL,
  population = NULL,
  pop_county_col = "fips_county",
  pop_col = "population",
  per = 1e+05,
  denominator_label = NULL
)
}
\arguments{
\item{providers}{A data frame with one row per provider (or per
provider-location); duplicate NPIs within a county are counted once.}

\item{npi_col}{Character. Column holding the NPI (or any provider id used for
the distinct count). Default \code{"npi"}.}

\item{county_col}{Character. Column holding the county FIPS code (values are
zero-padded to 5 characters). Default \code{"fips_county"}.}

\item{taxonomy_col}{Character or \code{NULL}. Optional taxonomy/specialty column;
when supplied, an additional per-county-per-specialty count is returned in
\code{by_specialty}. Default \code{NULL}.}

\item{population}{A data frame of county populations for density, or \code{NULL}
to skip density. Default \code{NULL}.}

\item{pop_county_col, pop_col}{Character. Columns in \code{population} holding the
county FIPS and the population count. Defaults \code{"fips_county"} / \code{"population"}.}

\item{per}{Numeric. Density scaling (providers per \code{per} residents).
Default \code{100000}.}

\item{denominator_label}{Character or \code{NULL}. When supplied (e.g. \code{"women"}),
it is appended to the density column name so it self-documents the
denominator you passed via \code{pop_col} — for example
\code{providers_per_100k_women} when \code{population} holds a female count. \code{NULL}
(default) keeps the generic \code{providers_per_100k}. This changes only the
column name, not the arithmetic.}
}
\value{
A list of class \code{mysterycall_provider_counts} with elements:
\describe{
\item{\code{by_county}}{Tibble: \code{fips_county}, \code{n_providers}, and (when
\code{population} supplied) \code{population} and the density column
(\code{providers_per_100k}, or \verb{providers_per_100k_<denominator_label>} when
\code{denominator_label} is set), scaled by \code{per}.}
\item{\code{by_specialty}}{Tibble of \code{fips_county}, taxonomy, \code{n_providers},
or \code{NULL} when \code{taxonomy_col} is \code{NULL}.}
\item{\code{n_counties}}{Integer count of counties represented.}
}
Rows with a missing county FIPS emit a warning and are dropped.
}
\description{
Aggregates a provider roster to the number of distinct providers (NPIs) in
each county, with an optional per-capita density. County-level physician
supply is a standard confounder for insurance-based access disparities: a
Medicaid patient's odds of getting an appointment depend partly on how many
clinicians practise locally.
}
\examples{
roster <- data.frame(
  npi         = sprintf("1\%09d", 1:6),
  fips_county = c("08031", "08031", "8031", "48201", "48201", NA),
  taxonomy    = c("OBGYN", "OBGYN", "FM", "OBGYN", "OBGYN", "FM"),
  stringsAsFactors = FALSE
)
pop <- data.frame(fips_county = c("08031", "48201"),
                  population   = c(715522, 4731145))
mysterycall_get_county_provider_counts(roster, population = pop)

# Female denominator -> self-documenting column `providers_per_100k_women`
women <- data.frame(fips_county = c("08031", "48201"),
                    population   = c(361000, 2350000))
mysterycall_get_county_provider_counts(roster, population = women,
                                       denominator_label = "women")
}
\seealso{
Other census:
\code{\link[=mysterycall_add_hhi]{mysterycall_add_hhi()}},
\code{\link[=mysterycall_add_medicaid_expansion]{mysterycall_add_medicaid_expansion()}},
\code{\link[=mysterycall_census_female_population]{mysterycall_census_female_population()}},
\code{\link[=mysterycall_get_acs_adults_18_90]{mysterycall_get_acs_adults_18_90()}},
\code{\link[=mysterycall_get_acs_female_insurance]{mysterycall_get_acs_female_insurance()}},
\code{\link[=mysterycall_get_acs_women_18_90]{mysterycall_get_acs_women_18_90()}},
\code{\link[=mysterycall_get_census_data]{mysterycall_get_census_data()}},
\code{\link[=mysterycall_get_payer_mix]{mysterycall_get_payer_mix()}},
\code{\link[=mysterycall_plot_census_age]{mysterycall_plot_census_age()}},
\code{\link[=mysterycall_read_kff_hhi]{mysterycall_read_kff_hhi()}},
\code{\link[=mysterycall_summarize_census]{mysterycall_summarize_census()}},
\code{\link[=mysterycall_summarize_county_enrollment]{mysterycall_summarize_county_enrollment()}},
\code{\link[=print.mysterycall_provider_counts]{print.mysterycall_provider_counts()}}
}
\concept{census}
