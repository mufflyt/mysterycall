% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geocode_address.R
\name{mysterycall_geocode_address}
\alias{mysterycall_geocode_address}
\title{Geocode full street addresses (US Census batch, with fallbacks)}
\usage{
mysterycall_geocode_address(
  data,
  street_col = "street",
  city_col = "city",
  state_col = "state",
  zip_col = "zip",
  batch_size = 1000L,
  fallback = TRUE,
  benchmark = "Public_AR_Current",
  vintage = "Current_Current",
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of addresses.}

\item{street_col, city_col, state_col, zip_col}{Column names holding the street
line, city, state, and ZIP. Defaults \code{"street"}, \code{"city"}, \code{"state"},
\code{"zip"}.}

\item{batch_size}{Addresses per Census request (1-10000). Default 1000.}

\item{fallback}{Logical. When \code{TRUE} (default) unmatched addresses are retried
one at a time via Nominatim then ArcGIS. Nominatim asks callers to stay
under ~1 request/second, so this is slow for large unmatched sets.}

\item{benchmark, vintage}{Census geocoder benchmark and vintage.}

\item{verbose}{Logical; print per-batch progress. Default \code{TRUE}.}
}
\value{
\code{data} with six columns appended: \code{geo_lat}, \code{geo_long},
\code{geo_match} (\code{"Match"}/\code{"No_Match"}/\code{"Tie"}), \code{geo_matched_address},
\code{geo_tract} (11-digit 2020 GEOID, Census matches only), and \code{geo_source}
(\code{"census"}, \code{"nominatim"}, \code{"arcgis"}, or \code{NA}). Feed \code{geo_lat}/\code{geo_long}
to \code{\link[=mysterycall_assign_area_covariates]{mysterycall_assign_area_covariates()}} for ADI/SVI/HHI.
}
\description{
Geocodes complete practice addresses (street, city, state, ZIP) to
coordinates and 2020 census geography using the US Census Bureau \strong{batch}
geocoder (\url{https://geocoding.geo.census.gov}; free, no key). Addresses the
Census cannot match optionally fall back to Nominatim (OpenStreetMap) and then
ArcGIS, one address at a time.
}
\details{
This is more precise than \code{\link[=mysterycall_geocode_city_state]{mysterycall_geocode_city_state()}}, which resolves
only to a city centroid: a street match places the physician in the correct
census tract and ZCTA, which matters for area-level covariates
(\code{\link[=mysterycall_assign_area_covariates]{mysterycall_assign_area_covariates()}}). The HTTP request/response machinery
is ported from the \code{isochrones} project's geocoding seams so it is unit
testable without a network (see the internal \verb{.mc_build_*} / \verb{.mc_parse_*}
seams).
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
addr <- data.frame(street = "12631 E 17th Ave", city = "Aurora",
                   state = "CO", zip = "80045")
mysterycall_geocode_address(addr)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_geocode_city_state]{mysterycall_geocode_city_state()}} for the coarser city-centroid
geocoder; \code{\link[=mysterycall_assign_area_covariates]{mysterycall_assign_area_covariates()}} to attach area covariates.

Other npi: 
\code{\link{.mc_build_census_batch_request}()},
\code{\link{.mc_census_batch}()},
\code{\link{.mc_geocode_http_get}()},
\code{\link{.mc_geocode_http_post}()},
\code{\link{.mc_geocode_http_with_retry}()},
\code{\link{.mc_geocode_one_fallback}()},
\code{\link{.mc_normalize_sex}()},
\code{\link{.mc_nppes_sex_one}()},
\code{\link{.mc_parse_census_batch_response}()},
\code{\link{mysterycall_enrich_npi}()},
\code{\link{mysterycall_get_clinician_data}()},
\code{\link{mysterycall_nppes_gender}()},
\code{\link{mysterycall_search_and_process_npi}()},
\code{\link{mysterycall_search_taxonomy}()}
}
\concept{npi}
