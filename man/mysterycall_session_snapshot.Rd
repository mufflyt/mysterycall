% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/session_snapshot.R
\name{mysterycall_session_snapshot}
\alias{mysterycall_session_snapshot}
\title{Write a reproducibility snapshot at analysis end}
\usage{
mysterycall_session_snapshot(
  file = "session_snapshot.txt",
  seeds = NULL,
  notes = NULL,
  append = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{file}{Character scalar.  Output file path.
Default \code{"session_snapshot.txt"}.}

\item{seeds}{Named integer vector or \code{NULL}.  Any seeds used during the
analysis, e.g. \code{c(bootstrap = 42L, imputation = 123L)}.  Unnamed elements
are labelled \code{seed_1}, \code{seed_2}, etc.  Default \code{NULL}.}

\item{notes}{Character vector or \code{NULL}.  Free-text notes appended verbatim
under the \verb{=== NOTES ===} section.  Each element is written on its own
line.  Default \code{NULL}.}

\item{append}{Logical scalar.  If \code{TRUE} \emph{and} \code{file} already exists,
content is appended rather than overwriting.  Default \code{FALSE}.}

\item{quiet}{Logical scalar.  If \code{FALSE} (default), emits a
\code{\link[base:message]{base::message()}} reporting the output path on success.}
}
\value{
\code{invisible(file)} — the output path — with S3 class
\code{"mysterycall_snapshot"}.  When the return value is printed explicitly,
the file path and first 20 lines of the snapshot are displayed.
}
\description{
Records the R session state — timestamp, R version, loaded package versions,
any named seeds used during the analysis, and optional free-text notes — to a
structured plain-text file.  Call this once at the very end of a script to
capture everything needed to reproduce the run.
}
\details{
Seeds are often buried inside individual functions; this function provides a
single top-level call that collects them all in one place.
}
\section{Output file structure}{


\if{html}{\out{<div class="sourceCode">}}\preformatted{=== REPRODUCIBILITY SNAPSHOT ===
Date/Time: <timestamp>
R Version: <version>
Platform:  <platform>

=== SEEDS ===
<name>: <value>

=== LOADED PACKAGES ===
<pkg>  <version>

=== FULL SESSION INFO ===
<capture.output(sessionInfo())>

=== NOTES ===
<notes>
}\if{html}{\out{</div>}}
}

\examples{
snap <- tempfile(fileext = ".txt")

# Basic usage — seeds and notes
mysterycall_session_snapshot(
  file  = snap,
  seeds = c(bootstrap = 42L, imputation = 123L),
  notes = "Primary sensitivity analysis.",
  quiet = TRUE
)

# Inspect what was written
out <- mysterycall_session_snapshot(file = snap, append = TRUE, quiet = TRUE)
print(out)
}
\seealso{
Other reporting: 
\code{\link{mysterycall_abstract_numbers}()},
\code{\link{mysterycall_exclusion_summary}()},
\code{\link{mysterycall_geographic_map}()},
\code{\link{mysterycall_irr_to_days}()},
\code{\link{mysterycall_results_paragraph}()},
\code{\link{mysterycall_supplemental_tables}()},
\code{\link{mysterycall_write_results_paragraph}()},
\code{\link{print.mysterycall_abstract_numbers}()},
\code{\link{print.mysterycall_irr_days}()},
\code{\link{print.mysterycall_snapshot}()}
}
\concept{reporting}
