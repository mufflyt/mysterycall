% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exclusion_summary.R
\name{mysterycall_exclusion_summary}
\alias{mysterycall_exclusion_summary}
\title{Summarise call-level exclusions into a manuscript-ready paragraph}
\usage{
mysterycall_exclusion_summary(
  data,
  exclusion_col = "reason_for_exclusions",
  inclusion_value = "Able to contact",
  exclusion_categories = c(phone_not_answered =
    "Phone not answered or busy signal on repeat calls", voicemail = "Went to voicemail",
    wrong_number = "Number contacted did not correspond to expected office/specialty",
    referral_required = "Physician referral required before scheduling appointment",
    not_accepting = "Not accepting new patients", on_hold =
    "Greater than 5 minutes on hold"),
  id_col = NULL
)
}
\arguments{
\item{data}{data.frame. The \strong{full} dataset including all excluded rows.
Must have at least one column whose name matches \code{exclusion_col}.}

\item{exclusion_col}{Character scalar. Name of the column that holds the
outcome or reason-for-exclusion value for each call.
Default \code{"reason_for_exclusions"}.}

\item{inclusion_value}{Character scalar. The value in \code{exclusion_col} that
identifies a call that was successfully completed and is included in the
analysis. Default \code{"Able to contact"}.}

\item{exclusion_categories}{Named character vector mapping semantic group
names to the exact string values found in \code{exclusion_col}. The vector
must contain \strong{all six} of the following keys (values may be overridden):
\describe{
\item{\code{phone_not_answered}}{Unsuccessful: phone not answered / busy signal.}
\item{\code{voicemail}}{Unsuccessful: call went to voicemail.}
\item{\code{wrong_number}}{Unsuccessful: number did not reach the expected office.}
\item{\code{referral_required}}{Reached but excluded: physician referral required.}
\item{\code{not_accepting}}{Reached but excluded: office not accepting new patients.}
\item{\code{on_hold}}{Reached but excluded: caller placed on hold > 5 minutes.}
}}

\item{id_col}{Character scalar or \code{NULL}. When non-\code{NULL}, \code{data} is
deduplicated by this column before all counting so that repeated call
attempts to the same provider collapse to one record.
Default \code{NULL} (no deduplication).}
}
\value{
A list of class \code{mysterycall_exclusion_summary} containing:
\describe{
\item{\code{total}}{Integer. Total calls (or unique IDs when \code{id_col} is
non-\code{NULL}).}
\item{\code{n_reached}}{Integer. Calls successfully connected (not in the
unreachable group).}
\item{\code{n_unreachable}}{Integer. Calls in the unreachable group
(phone not answered + voicemail + wrong number).}
\item{\code{pct_reached}}{Numeric. \code{n_reached / total * 100}, or \code{NA} when
\code{total} is zero.}
\item{\code{n_included}}{Integer. Calls included in the final analysis
(\code{n_reached} minus reached-but-excluded calls).}
\item{\code{counts}}{Named integer vector. Count for each of the six
canonical exclusion-category keys.}
\item{\code{percentages}}{Named numeric vector. \code{counts / total * 100}, or
\code{NA} for each element when \code{total} is zero.}
\item{\code{paragraph}}{Character scalar. Full exclusion paragraph ready to
paste into a manuscript Methods or Results section.}
\item{\code{table}}{data.frame. Tabyl-style summary with columns
\code{category} (key name), \code{label} (human-readable value), \code{n} (integer
count), and \code{pct} (numeric percentage of total).}
}
}
\description{
Automates the computation and prose formatting of an exclusion paragraph
from a mystery-caller dataset that contains a column recording the outcome
(included vs. a reason for exclusion) for each call attempt. The function
mirrors the manual paragraph construction used in ortho sports-medicine
mystery-caller Rmd reports (lines 1033-1081) and generalises it for any
study design.
}
\details{
\strong{Semantic grouping of the six built-in exclusion keys:}
\itemize{
\item \emph{Unsuccessful connections} (could not reach anyone):
\code{phone_not_answered}, \code{voicemail}, \code{wrong_number}.
\item \emph{Reached but excluded}:
\code{referral_required}, \code{not_accepting}, \code{on_hold}.
}
Calls that match \code{inclusion_value} are counted as \emph{included}. Any row
that does not match \code{inclusion_value} or any value in
\code{exclusion_categories} is silently classified as unrecognised and
contributes to the denominator but not to any named exclusion bucket.
}
\examples{
## --- minimal runnable example (base R only) -------------------------------
reasons <- c(
  "Able to contact",
  "Phone not answered or busy signal on repeat calls",
  "Went to voicemail",
  "Number contacted did not correspond to expected office/specialty",
  "Physician referral required before scheduling appointment",
  "Not accepting new patients",
  "Greater than 5 minutes on hold"
)
n_each <- c(120L, 40L, 30L, 10L, 15L, 20L, 5L)

df <- data.frame(
  call_id               = seq_len(sum(n_each)),
  reason_for_exclusions = rep(reasons, n_each),
  stringsAsFactors      = FALSE
)

result <- mysterycall_exclusion_summary(df)
print(result)       # prints the prose paragraph
result$table        # tabyl-style counts

## --- custom exclusion values (different study coding scheme) --------------
df2 <- df
names(df2)[2] <- "call_outcome"
result2 <- mysterycall_exclusion_summary(
  data          = df2,
  exclusion_col = "call_outcome",
  inclusion_value = "Able to contact",
  exclusion_categories = c(
    phone_not_answered = "Phone not answered or busy signal on repeat calls",
    voicemail          = "Went to voicemail",
    wrong_number       = "Number contacted did not correspond to expected office/specialty",
    referral_required  = "Physician referral required before scheduling appointment",
    not_accepting      = "Not accepting new patients",
    on_hold            = "Greater than 5 minutes on hold"
  )
)
cat(result2$paragraph)

## --- deduplicate by provider before counting ------------------------------
df3 <- df
df3$npi <- c(
  seq_len(120L),                  # included (unique)
  rep(121L, 40L),                 # one provider, 40 no-answer attempts
  seq_len(sum(n_each[-c(1L, 2L)])) + 121L  # remaining unique providers
)
# id_col collapses repeated dials to the same NPI
result3 <- mysterycall_exclusion_summary(df3, id_col = "npi")
result3$total   # unique providers, not raw call rows
}
\seealso{
\code{\link[=mysterycall_strobe_flow]{mysterycall_strobe_flow()}}, \code{\link[=mysterycall_flag_date_outliers]{mysterycall_flag_date_outliers()}}

Other reporting: 
\code{\link{mysterycall_abstract_numbers}()},
\code{\link{mysterycall_irr_to_days}()},
\code{\link{mysterycall_results_paragraph}()},
\code{\link{mysterycall_session_snapshot}()},
\code{\link{mysterycall_supplemental_tables}()},
\code{\link{mysterycall_write_results_paragraph}()},
\code{\link{print.mysterycall_abstract_numbers}()},
\code{\link{print.mysterycall_irr_days}()},
\code{\link{print.mysterycall_snapshot}()}
}
\concept{reporting}
