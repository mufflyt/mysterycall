% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_outcomes.R
\name{mysterycall_outcome_gradient}
\alias{mysterycall_outcome_gradient}
\title{Ordered Multi-Category Outcome Summary (Access Gradient)}
\usage{
mysterycall_outcome_gradient(
  data,
  var,
  levels = NULL,
  group_var = NULL,
  conf_level = 0.95,
  cumulative = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{var}{Character scalar. The categorical outcome column.}

\item{levels}{Character vector or \code{NULL}. The level ordering. \code{NULL} uses the
factor levels of \code{var}, or the sorted unique values.}

\item{group_var}{Character scalar or \code{NULL}. Optional grouping column.}

\item{conf_level}{Numeric. Wilson interval confidence level. Default \code{0.95}.}

\item{cumulative}{Logical. Add a \code{cumulative_prop} column following \code{levels}
order (meaningful for an ordinal outcome). Default \code{TRUE}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns (optional \code{group}), \code{level}, \code{n},
\code{total}, \code{proportion}, \code{ci_lower}, \code{ci_upper}, and optionally
\code{cumulative_prop}.
}
\description{
Summarises a categorical outcome across its levels with per-level counts,
proportions, Wilson intervals, and -- for an ordered (graded) outcome such as
an access tier -- cumulative proportions. Fits the graded access ladders in
the literature (Sharma's "information by phone / in-person only / must
establish care"; a triage disposition; a staff-role breakdown).
}
\examples{
df <- data.frame(
  access = c("phone", "phone", "in_person", "must_establish", "phone",
             "in_person", "must_establish", "must_establish")
)
mysterycall_outcome_gradient(
  df, "access", levels = c("phone", "in_person", "must_establish")
)
}
\seealso{
Other call-outcomes: 
\code{\link{mysterycall_classify_call_outcome}()},
\code{\link{mysterycall_multiresponse_tabulate}()},
\code{\link{mysterycall_ordinal_model}()}
}
\concept{call-outcomes}
