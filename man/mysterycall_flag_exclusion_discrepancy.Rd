% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_exclusion_discrepancy.R
\name{mysterycall_flag_exclusion_discrepancy}
\alias{mysterycall_flag_exclusion_discrepancy}
\title{Flag Records with Exclusions That Also Have a Wait Time}
\usage{
mysterycall_flag_exclusion_discrepancy(
  data,
  days_col = "business_days_until_appointment",
  exclusion_col = "reason_for_exclusions",
  contact_value = "Able to contact",
  select_cols = c("physician_information", "id_number", "notes", "reason_for_exclusions",
    "business_days_until_appointment"),
  min_days = 0,
  output_dir = NULL,
  filename = "discrepancy_rows.csv"
)
}
\arguments{
\item{data}{A data frame of mystery-caller records.}

\item{days_col}{Character scalar. Name of the wait-time column.
Default \code{"business_days_until_appointment"}.}

\item{exclusion_col}{Character scalar. Name of the exclusion-reason column.
Default \code{"reason_for_exclusions"}.}

\item{contact_value}{Character scalar. The value in \code{exclusion_col} that
means the call succeeded (no exclusion). Default \code{"Able to contact"}.}

\item{select_cols}{Character vector of extra columns to include in the
returned table. Default returns a standard audit set.}

\item{min_days}{Numeric scalar. Lower bound for the wait-time comparison
(inclusive). Default \code{0} (any non-negative value is flagged).}

\item{output_dir}{Character scalar or \code{NULL}. Directory for the CSV output.
\code{NULL} (default) writes to a session temp directory via
\code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing entirely.}

\item{filename}{Character scalar. Output CSV file name.
Default \code{"discrepancy_rows.csv"}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of discrepant rows, sorted descending by the
id column (if present). Returns a zero-row tibble (invisibly) when no
discrepancies are found.
}
\description{
A quality-control check that finds records where \code{reason_for_exclusions}
indicates the call was NOT successfully completed (i.e. not
\code{"Able to contact"}) but \code{business_days_until_appointment} is nonetheless
non-negative — a logical discrepancy suggesting a data-entry error.
}
\section{What this detects}{

If a call is logged as excluded (e.g. \code{reason_for_exclusions == "Physician not available"}) but also has `business_days_until_appointment= 0`, the record is contradictory — an excluded call should not have avalid appointment wait time. These rows must be resolved before analysis.
}

\examples{
df <- data.frame(
  physician_information          = c("Dr A", "Dr B", "Dr C"),
  id_number                      = c("001", "002", "003"),
  notes                          = c("Called twice", NA, "Left VM"),
  reason_for_exclusions          = c("Physician not available",
                                     "Able to contact",
                                     "Number disconnected"),
  business_days_until_appointment = c(5, 3, 0),
  stringsAsFactors = FALSE
)
# Dr A and Dr C are flagged: excluded but have a wait time >= 0
result <- mysterycall_flag_exclusion_discrepancy(df, output_dir = NA)
result
}
\seealso{
\code{\link[=mysterycall_flag_repeat_physicians]{mysterycall_flag_repeat_physicians()}} for duplicate-entry checks;
\code{mysterycall_sanity_checks()} for broader pre-analysis validation.

Other quality control: 
\code{\link{mysterycall_clean_medicaid_col}()},
\code{\link{mysterycall_dedup_by_insurance}()},
\code{\link{mysterycall_flag_excluded_with_appointments}()},
\code{\link{mysterycall_flag_included_na_appointments}()},
\code{\link{mysterycall_flag_repeat_physicians}()}
}
\concept{quality control}
