% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_outcomes.R
\name{mysterycall_ordinal_model}
\alias{mysterycall_ordinal_model}
\title{Proportional-Odds Model for a Graded Ordinal Outcome}
\usage{
mysterycall_ordinal_model(
  data,
  outcome_var,
  predictors,
  levels = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome_var}{Character scalar. The ordinal outcome column.}

\item{predictors}{Character vector. Predictor column names.}

\item{levels}{Character vector or \code{NULL}. The outcome's increasing level
order. \code{NULL} uses the existing factor ordering or sorted unique values.}

\item{conf_level}{Numeric. Confidence level for the Wald intervals. Default
\code{0.95}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{term}, \code{or}, \code{ci_lower},
\code{ci_upper}, \code{p_value} (one row per predictor coefficient).
}
\description{
Fits an ordinal (cumulative-link) model to a graded outcome such as an access
tier, returning proportional-odds odds ratios with Wald confidence intervals
and p-values. Thin wrapper around \code{\link[MASS:polr]{MASS::polr()}}.
}
\examples{
if (requireNamespace("MASS", quietly = TRUE)) {
  set.seed(1)
  df <- data.frame(
    access = factor(sample(c("phone", "in_person", "must_establish"), 90, TRUE),
                    levels = c("phone", "in_person", "must_establish"),
                    ordered = TRUE),
    medicaid = sample(c(0, 1), 90, TRUE)
  )
  mysterycall_ordinal_model(df, "access", "medicaid")
}
}
\seealso{
Other call-outcomes:
\code{\link[=mysterycall_classify_call_outcome]{mysterycall_classify_call_outcome()}},
\code{\link[=mysterycall_multiresponse_tabulate]{mysterycall_multiresponse_tabulate()}},
\code{\link[=mysterycall_outcome_gradient]{mysterycall_outcome_gradient()}}
}
\concept{call-outcomes}
