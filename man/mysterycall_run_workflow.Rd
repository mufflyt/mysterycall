% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_mystery_caller_workflow.R
\name{mysterycall_run_workflow}
\alias{mysterycall_run_workflow}
\title{Run the end-to-end mystery caller workflow}
\usage{
mysterycall_run_workflow(
  taxonomy_terms = NULL,
  name_data = NULL,
  phase1_data,
  lab_assistant_names,
  output_directory,
  phase2_data,
  phase2_output_directory = output_directory,
  quality_check_path,
  phase1_output_directory = output_directory,
  split_insurance_order = c("Medicaid", "Blue Cross/Blue Shield"),
  phase2_required_strings = c("physician_information", "able_to_contact_office",
    "are_we_including", "reason_for_exclusions", "appointment_date",
    "number_of_transfers", "call_time", "hold_time", "notes", "person_completing",
    "state", "npi", "name"),
  phase2_standard_names = c("physician_info", "contact_office", "included_in_study",
    "exclusion_reasons", "appt_date", "transfer_count", "call_duration", "hold_duration",
    "notes", "completed_by", "state", "npi", "name"),
  npi_search_args = list(),
  all_states = NULL,
  taxonomy_states = NULL,
  verbose = interactive(),
  npi_progress_observer = NULL
)
}
\arguments{
\item{taxonomy_terms}{Character vector of taxonomy descriptions to pass to
\code{\link[=mysterycall_search_taxonomy]{mysterycall_search_taxonomy()}}. Set to \code{NULL} to skip taxonomy-based searches.}

\item{name_data}{Optional data frame containing \code{first} and \code{last} columns to
use with \code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}}. Provide \code{NULL} to skip name-based
searches.}

\item{phase1_data}{Data frame holding Phase 1 calling roster information to
pass to \code{\link[=mysterycall_clean_phase1]{mysterycall_clean_phase1()}}.}

\item{lab_assistant_names}{Character vector of caller names used when splitting
the cleaned roster via \code{\link[=mysterycall_split_and_save]{mysterycall_split_and_save()}}. Must contain at least two
entries.}

\item{output_directory}{Directory where \code{\link[=mysterycall_split_and_save]{mysterycall_split_and_save()}} should write the
complete and per-caller workbooks.}

\item{phase2_data}{Data frame or file path consumed by
\code{\link[=mysterycall_clean_phase2]{mysterycall_clean_phase2()}}.}

\item{phase2_output_directory}{Directory where Phase 2 exports should be
written. Defaults to \code{output_directory}.}

\item{quality_check_path}{File path where \code{\link[=mysterycall_save_quality_table]{mysterycall_save_quality_table()}} should
write the quality check CSV.}

\item{phase1_output_directory}{Directory where \code{\link[=mysterycall_clean_phase1]{mysterycall_clean_phase1()}}
should write the cleaned Phase 1 CSV. Defaults to \code{output_directory}.}

\item{split_insurance_order}{Ordering passed to \code{\link[=mysterycall_split_and_save]{mysterycall_split_and_save()}}'s
\code{insurance_order} argument. Defaults to \code{c("Medicaid", "Blue Cross/Blue Shield")}.}

\item{phase2_required_strings}{Character vector of substrings used when
standardizing Phase 2 column names via \code{\link[=mysterycall_clean_phase2]{mysterycall_clean_phase2()}}.}

\item{phase2_standard_names}{Replacement names corresponding to
\code{phase2_required_strings}.}

\item{npi_search_args}{Named list of additional arguments forwarded to
\code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}}.}

\item{all_states}{Optional character vector of all states to supply to
\code{\link[=mysterycall_not_contacted_states]{mysterycall_not_contacted_states()}}.}

\item{taxonomy_states}{Optional character vector of two-letter state
abbreviations forwarded to \code{\link[=mysterycall_search_taxonomy]{mysterycall_search_taxonomy()}} as its \code{states}
argument. When \code{NULL} (default) the search is national and capped at 1,200
records per taxonomy term. Pass all 50 abbreviations to bypass the cap for
large specialties.}

\item{verbose}{Logical. When \code{TRUE}, print stage updates to the console while
running the workflow. Defaults to \code{interactive()}.}

\item{npi_progress_observer}{Optional callback that receives progress updates
from \code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}}. It is invoked with the same payload as the
\code{progress_callback} argument for that function.}
}
\value{
A list containing intermediate artifacts from each workflow stage:
\code{roster}, \code{validated_roster}, \code{cleaned_phase1}, \code{cleaned_phase2},
\code{coverage_summary}, \code{quality_check_table}, and a \code{workflow_summary} data
frame documenting row counts, retention rates, and output paths for audit
transparency.
}
\description{
This helper orchestrates the core steps required to prepare and execute a
mystery caller campaign. It stitches together roster creation, NPI
validation, call sheet preparation, workload splitting, and Phase 2 data
hygiene checks so teams can focus on execution instead of plumbing.
}
\section{Contract}{

\strong{Inputs:}
\itemize{
\item \code{phase1_data} must contain columns: \code{names}, \code{practice_name}, \code{phone_number},
\code{state_name}, \code{npi}, \code{for_redcap}.
\item \code{lab_assistant_names} must have at least two entries for workload splitting.
\item \code{output_directory} must be writable; created automatically if absent.
}

\strong{Guarantees:}
\itemize{
\item Output list always contains \code{workflow_summary} even when upstream steps fail.
\item Row counts at each stage are recorded in \code{workflow_summary} for audit
transparency.
\item Files written to \code{output_directory} are overwritten deterministically on
re-run with the same inputs.
}

\strong{Fails if:}
\itemize{
\item \code{phase1_data} is missing required columns (error from \code{mysterycall_clean_phase1()}).
\item \code{lab_assistant_names} has fewer than two entries.
\item \code{output_directory} is not writable and cannot be created.
}
}

\section{Performance}{

Complexity is approximately O(n) in number of Phase 1 rows for local steps.
NPI registry calls via \code{mysterycall_search_taxonomy()} add O(t * p) where
\code{t} = number of taxonomy terms and \code{p} = max records per term (default 1,200).
Budget ~5-30 s per taxonomy term depending on network latency.
}

\section{Calls}{

\itemize{
\item \code{\link[=mysterycall_search_taxonomy]{mysterycall_search_taxonomy()}} (when \code{taxonomy_terms} is non-NULL)
\item \code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}} (when \code{name_data} is non-NULL)
\item \code{\link[=mysterycall_clean_phase1]{mysterycall_clean_phase1()}}
\item \code{\link[=mysterycall_validate_npi]{mysterycall_validate_npi()}}
\item \code{\link[=mysterycall_split_and_save]{mysterycall_split_and_save()}}
\item \code{\link[=mysterycall_clean_phase2]{mysterycall_clean_phase2()}}
\item \code{\link[=mysterycall_not_contacted_states]{mysterycall_not_contacted_states()}}
\item \code{\link[=mysterycall_save_quality_table]{mysterycall_save_quality_table()}}
}
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
results <- mysterycall_run_workflow(
  phase1_data = data.frame(
    names = "Jane Doe", practice_name = "Clinic A",
    phone_number = "555-0100", state_name = "Colorado",
    npi = "1234567890", for_redcap = "Yes"
  ),
  lab_assistant_names = c("Alice", "Bob"),
  output_directory = tempdir(),
  phase2_data = data.frame(),
  quality_check_path = file.path(tempdir(), "qc.csv")
)
\dontshow{\}) # examplesIf}
}
\seealso{
Stage functions called by this workflow:
\code{\link[=mysterycall_search_taxonomy]{mysterycall_search_taxonomy()}}, \code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}},
\code{\link[=mysterycall_validate_npi]{mysterycall_validate_npi()}}, \code{\link[=mysterycall_clean_phase1]{mysterycall_clean_phase1()}},
\code{\link[=mysterycall_clean_phase2]{mysterycall_clean_phase2()}}.
Run \code{\link[=mysterycall_preflight_check]{mysterycall_preflight_check()}} before starting to catch missing
API keys and malformed inputs early.

Other workflow: 
\code{\link{mysterycall_call_productivity}()},
\code{\link{mysterycall_clean_phase1}()},
\code{\link{mysterycall_clean_phase2}()},
\code{\link{mysterycall_print_dashboard}()},
\code{\link{mysterycall_run_workflow_logged}()},
\code{\link{mysterycall_split_and_save}()},
\code{\link{mysterycall_verify_artifact}()}
}
\concept{workflow}
