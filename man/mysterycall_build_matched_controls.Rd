% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matched_controls.R
\name{mysterycall_build_matched_controls}
\alias{mysterycall_build_matched_controls}
\title{Build a propensity-score-matched control cohort}
\usage{
mysterycall_build_matched_controls(
  treated,
  candidates,
  ps_formula,
  id_col,
  exact = NULL,
  coords = NULL,
  caliper_miles = 10,
  min_in_caliper = 1L,
  cluster_col = NULL,
  seed = 42
)
}
\arguments{
\item{treated}{Data frame of treated units (one row per unit, unless
\code{cluster_col} is set).}

\item{candidates}{Data frame of candidate controls. Must contain the
propensity covariates, \code{id_col}, any \code{exact} columns, and (if used) the
\code{coords} columns.}

\item{ps_formula}{A two-sided formula \code{treatment ~ x1 + x2 + ...}. The
left-hand side names the treatment indicator this function creates
internally (1 for \code{treated}, 0 for \code{candidates}); the right-hand side are
the matching covariates, which must exist in both frames.}

\item{id_col}{Name of the unique identifier column (present in both frames).}

\item{exact}{Optional character vector of columns that a control must match
the treated unit on exactly (e.g. \code{"state"}). Default \code{NULL}.}

\item{coords}{Optional length-2 character vector naming latitude and longitude
columns (in both frames), used for the distance caliper. \code{NULL} disables the
caliper. See \code{\link[=mysterycall_geocode_city_state]{mysterycall_geocode_city_state()}} to populate them.}

\item{caliper_miles}{Maximum control distance in miles (haversine). Ignored
when \code{coords} is \code{NULL}. Default \code{10}.}

\item{min_in_caliper}{Require at least this many candidates within the caliper
before a treated unit is matched (guards against matching to an isolated
control). Default \code{1}.}

\item{cluster_col}{Optional column identifying clusters (e.g. an office/site
shared by several treated providers). When set, at most one treated unit per
cluster is matched; units within a cluster are tried in random order until
one matches.}

\item{seed}{Integer seed for the within-cluster shuffle (reproducibility).
Default \code{42}.}
}
\value{
An object of class \code{"mysterycall_matched_controls"}: a list with
\code{pairs} (a tibble: \code{pair_id}, \code{treated_id}, \code{control_id}, \code{ps_treated},
\code{ps_control}, \code{ps_diff}, \code{distance_miles}), \code{matched_treated},
\code{matched_controls} (the matched rows from each input), \code{ps_model} (the fitted
\code{glm}), \code{balance} (standardized mean differences for numeric covariates,
treated vs matched controls), and counts \code{n_treated}, \code{n_matched},
\code{n_unmatched}. \code{\link[=as.data.frame]{as.data.frame()}} returns \code{pairs}.
}
\description{
Many mystery-caller audits are case-control by design: a treated group
(private-equity-owned practices, hospital-acquired practices, a specific
chain) is compared against otherwise-similar controls. To make the comparison
fair -- and to keep the call burden bounded -- you match each treated practice
to a nearby, comparable control. This builds that matched cohort: it fits a
propensity model on the treated units versus a candidate pool, then greedily
matches each treated unit 1:1 to its nearest control on the propensity score,
within an exact stratum (e.g. same state) and an optional geographic caliper,
without replacement.
}
\details{
The greedy nearest-neighbour scheme is the one audit teams actually use when a
control must be \emph{callable} (same state, a short drive away), which off-the-
shelf matchers do not express directly. For unconstrained statistical matching
consider the \pkg{MatchIt} package instead.
}
\examples{
set.seed(1)
treated <- data.frame(
  npi = 1:20, state = sample(c("CO", "TX"), 20, TRUE),
  lat = runif(20, 39, 40), lon = runif(20, -105, -104),
  years = rpois(20, 12), female = rbinom(20, 1, 0.5))
candidates <- data.frame(
  npi = 101:400, state = sample(c("CO", "TX"), 300, TRUE),
  lat = runif(300, 39, 40), lon = runif(300, -105, -104),
  years = rpois(300, 10), female = rbinom(300, 1, 0.5))
m <- mysterycall_build_matched_controls(
  treated, candidates, treatment ~ years + female,
  id_col = "npi", exact = "state", coords = c("lat", "lon"),
  caliper_miles = 40)
m
}
