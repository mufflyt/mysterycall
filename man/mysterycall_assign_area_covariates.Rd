% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assign_area_covariates.R
\name{mysterycall_assign_area_covariates}
\alias{mysterycall_assign_area_covariates}
\title{Assign area-level covariates (ADI, SVI, HHI) from physician coordinates}
\usage{
mysterycall_assign_area_covariates(
  data,
  lat_col = "lat",
  long_col = "long",
  which = c("adi", "svi", "hhi"),
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame of physicians with latitude and longitude columns.}

\item{lat_col, long_col}{Column names holding latitude and longitude in
decimal degrees (WGS84). Defaults \code{"lat"} / \code{"long"}.}

\item{which}{Character vector naming which covariates to attach; any of
\code{"adi"}, \code{"svi"}, \code{"hhi"}. Default all three.}

\item{verbose}{Logical; print a per-covariate coverage summary. Default
\code{TRUE}.}
}
\value{
\code{data} with geocoded keys (\code{zcta}, \code{tract},
\code{msa}) and the requested covariate columns appended: \code{adi},
\code{svi}, \code{hhi} (and \code{hhi_cat}). A \code{"coverage"} attribute
holds the non-missing count for each covariate.
}
\description{
Attaches neighbourhood and market context to physicians from their practice
latitude/longitude, maximising coverage of the three area-level covariates
used as sensitivity adjustments in mystery-caller access studies:
\itemize{
\item \strong{ADI} -- Area Deprivation Index (\code{\link{adi_zcta}}),
\item \strong{SVI} -- Social Vulnerability Index (\code{\link{svi_zcta}}),
\item \strong{HHI} -- hospital-market Herfindahl-Hirschman Index
(\code{\link{kff_hhi}}).
}
}
\details{
Each coordinate is resolved once through the US Census Bureau geocoder
(\url{https://geocoding.geo.census.gov}; no key required) to its 2020 ZIP Code
Tabulation Area, census tract, and Metropolitan Statistical Area. ADI and SVI
then join on ZCTA -- which covers essentially every populated location, so
their coverage is limited only by whether a coordinate is present and falls in
a ZCTA. HHI joins on MSA and therefore remains \strong{structurally metro-only}:
KFF publishes HHI for 387 metropolitan markets, so physicians outside any MSA
(rural/micropolitan) get \code{NA} HHI by construction, not by a matching
failure.

Geocoding is one network call per unique coordinate (duplicates are resolved
once and reused). Failed or out-of-US coordinates yield \code{NA} keys and
therefore \code{NA} covariates.
}
\examples{
\donttest{
df <- data.frame(lat = 39.7392, long = -104.9903)  # Denver
mysterycall_assign_area_covariates(df)
}
}
\seealso{
\code{\link{adi_zcta}}, \code{\link{svi_zcta}}, \code{\link{kff_hhi}}.

Other data integrity:
\code{\link[=.mc_age_impute]{.mc_age_impute()}},
\code{\link[=.mc_age_key]{.mc_age_key()}},
\code{\link[=.mc_check_acs_vintage]{.mc_check_acs_vintage()}},
\code{\link[=.mc_data]{.mc_data()}},
\code{\link[=.mc_geo_layer]{.mc_geo_layer()}},
\code{\link[=.mc_geocode_point]{.mc_geocode_point()}},
\code{\link[=.mc_healthgrades_ages]{.mc_healthgrades_ages()}},
\code{\link[=.mc_state_to_abbr]{.mc_state_to_abbr()}},
\code{\link{mc-census-vintage}},
\code{\link[=mysterycall_categorize_wait]{mysterycall_categorize_wait()}},
\code{\link[=mysterycall_clean_zip]{mysterycall_clean_zip()}},
\code{\link[=mysterycall_flag_near_duplicate_keys]{mysterycall_flag_near_duplicate_keys()}},
\code{\link[=mysterycall_link_physicians]{mysterycall_link_physicians()}},
\code{\link[=mysterycall_lookup_age]{mysterycall_lookup_age()}},
\code{\link[=mysterycall_parse_duration]{mysterycall_parse_duration()}}
}
\concept{data integrity}
