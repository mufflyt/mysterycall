% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concordance.R
\name{mysterycall_score_concordance}
\alias{mysterycall_score_concordance}
\title{Score Captured Calls Against a Concordance Rubric}
\usage{
mysterycall_score_concordance(
  data,
  rubric,
  call_id_col = NULL,
  group_col = NULL,
  date_col = NULL,
  conf_level = 0.95,
  output_dir = NULL,
  filename = "concordance_item_rates.csv"
)
}
\arguments{
\item{data}{A data frame with one row per call. Rubric item ids are columns.
\code{evidence_tier} items hold either a list-column of option vectors or a
delimited string (see \code{option_sep} on the rubric).}

\item{rubric}{A \code{mysterycall_rubric} from \code{\link[=mysterycall_concordance_rubric]{mysterycall_concordance_rubric()}}.}

\item{call_id_col}{Character scalar or \code{NULL}. Column uniquely identifying a
call. \code{NULL} uses the row index.}

\item{group_col}{Character scalar or \code{NULL}. Optional grouping column; when
supplied, the result carries a per-group composite summary suitable for a
disparities table.}

\item{date_col}{Character scalar or \code{NULL}. Optional call-date column used to
select the \code{evidence_tier} rows in force at call time (date-versioned
guidelines).}

\item{conf_level}{Numeric. Confidence level for item-rate Wilson intervals.
Default \code{0.95}.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for the item-rate
CSV. \code{NULL} writes to a session temp directory via \code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}.
Pass \code{NA} to skip writing.}

\item{filename}{Character scalar. Output CSV file name. Default
\code{"concordance_item_rates.csv"}.}
}
\value{
A \code{mysterycall_concordance} object: a list with
\describe{
\item{\code{call_scores}}{Tibble, one row per call: \code{call_id}, \code{n_applicable},
\code{n_concordant}, \code{score_pct}, \code{weighted_pct}.}
\item{\code{item_rates}}{Tibble, one row per item: \code{item}, \code{label}, \code{type},
\code{n_applicable}, \code{n_concordant}, \code{rate}, \code{ci_lower}, \code{ci_upper}.}
\item{\code{overall}}{Named list: \code{mean_score_pct}, \code{sd_score_pct},
\code{median_score_pct}, \code{n_calls}, \code{mean_items_applicable}.}
\item{\code{by_group}}{Tibble or \code{NULL}: per-group \code{n_calls}, \code{mean_score_pct},
\code{sd_score_pct}.}
\item{\code{rubric}, \code{settings}}{The rubric and call settings.}
}
}
\description{
Compares each captured call to the reference standard defined by a
\code{\link[=mysterycall_concordance_rubric]{mysterycall_concordance_rubric()}} and returns call-level composite scores,
item-level concordance rates (with Wilson confidence intervals), and an
overall summary. Item denominators track \emph{applicability} separately from the
number of calls, so within-encounter conversation exits (which shrink an
item's denominator) are handled natively rather than being mistaken for a
zero.
}
\examples{
calls <- data.frame(
  advised_window     = c(TRUE, TRUE, FALSE, NA,   TRUE),
  take_asap          = c(TRUE, FALSE, TRUE, TRUE, TRUE),
  redose_after_vomit = c(FALSE, TRUE, NA,   TRUE, TRUE),
  district           = c("A", "A", "B", "B", "B")
)
rub <- mysterycall_concordance_rubric(
  item = c("advised_window", "take_asap", "redose_after_vomit"),
  type = "binary", correct = TRUE
)
res <- mysterycall_score_concordance(calls, rub, group_col = "district",
                                     output_dir = NA)
res$item_rates
}
\seealso{
Other concordance:
\code{\link[=mysterycall_concordance_kappa]{mysterycall_concordance_kappa()}},
\code{\link[=mysterycall_concordance_rubric]{mysterycall_concordance_rubric()}},
\code{\link[=mysterycall_concordance_sentence]{mysterycall_concordance_sentence()}}
}
\concept{concordance}
