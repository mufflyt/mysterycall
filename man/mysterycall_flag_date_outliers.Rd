% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_date_outliers.R
\name{mysterycall_flag_date_outliers}
\alias{mysterycall_flag_date_outliers}
\title{Flag call dates that look like fat-finger entry errors}
\usage{
mysterycall_flag_date_outliers(
  data,
  date_col,
  group_col = NULL,
  tolerance_days = 7L,
  min_group_size = 3L,
  flag_col = "date_flag",
  expected_col = "expected_date"
)
}
\arguments{
\item{data}{A data frame.}

\item{date_col}{Character scalar. Name of the Date (or coercible-to-Date)
column to check.}

\item{group_col}{Character scalar or \code{NULL}. Optional column that identifies
the call wave or batch. When supplied, outlier detection is performed
independently within each group. Default \code{NULL} (whole data set as one
group).}

\item{tolerance_days}{Non-negative integer. Maximum absolute deviation (in
calendar days) from the group modal date that is still considered
acceptable. Default \code{7} (one week). Set to \code{0} to flag any date that
differs from the exact mode.}

\item{min_group_size}{Positive integer. Groups with fewer rows than this are
skipped (all flagged as \code{NA}). Useful when a wave genuinely has only 1-2
calls. Default \code{3}.}

\item{flag_col}{Character scalar. Name of the logical flag column to add.
Default \code{"date_flag"}.}

\item{expected_col}{Character scalar. Name of the column that records the
group modal date. Default \code{"expected_date"}.}
}
\value{
\code{data} with two new columns appended:
\describe{
\item{\verb{<flag_col>}}{Logical. \code{TRUE} if the row's date is more than
\code{tolerance_days} away from the group modal date; \code{FALSE} otherwise;
\code{NA} if the group was too small to evaluate.}
\item{\verb{<expected_col>}}{Date. The modal date for the row's group (or
\code{NA} for small groups).}
}
}
\description{
In mystery-caller studies all calls in a wave are typically placed on the
same day (or within a narrow window). A single row with a date 30 days
outside the consensus is almost always a data-entry error - e.g., "June 17"
mis-keyed as "July 17". This function identifies those outliers.
}
\details{
For each group (defined by \code{group_col}, or the whole data set if
\code{group_col = NULL}), the \emph{modal date} (most-frequent date) is used as the
expected anchor. Any row whose date differs from that anchor by more than
\code{tolerance_days} is flagged.
}
\section{Interpreting output}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{out <- mysterycall_flag_date_outliers(df, "call_date", group_col = "wave")
out[out$date_flag \%in\% TRUE, c("wave", "call_date", "expected_date")]
}\if{html}{\out{</div>}}

Rows where \code{date_flag == TRUE} should be reviewed against the original
call logs. Common causes: month transposition (06 vs 07), year rollover
(Dec vs Jan), or a genuine make-up call placed weeks later.
}

\examples{
df <- data.frame(
  wave      = c(rep("A", 10), rep("B", 10)),
  call_date = as.Date(c(
    rep("2025-06-17", 9), "2025-07-17",   # wave A: 9 correct, 1 fat-finger
    rep("2025-09-03", 10)                 # wave B: all correct
  )),
  stringsAsFactors = FALSE
)
out <- mysterycall_flag_date_outliers(df, "call_date", group_col = "wave")
out[, c("wave", "call_date", "expected_date", "date_flag")]
}
\seealso{
\code{\link[=mysterycall_check_normality]{mysterycall_check_normality()}}, \code{\link[=mysterycall_sanity_checks]{mysterycall_sanity_checks()}}

Other data-quality:
\code{\link[=mysterycall_validate_npi]{mysterycall_validate_npi()}}
}
\concept{data-quality}
