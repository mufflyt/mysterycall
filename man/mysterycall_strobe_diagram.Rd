% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/strobe_diagram.R
\name{mysterycall_strobe_diagram}
\alias{mysterycall_strobe_diagram}
\title{Draw a validated STROBE participant-flow diagram}
\usage{
mysterycall_strobe_diagram(
  spec,
  title = NULL,
  output_path = NULL,
  width = 9,
  height = 7,
  dpi = 300,
  base_size = 10,
  background = "white"
)
}
\arguments{
\item{spec}{A \code{mysterycall_flow_spec} from \code{\link[=mysterycall_flow_spec]{mysterycall_flow_spec()}}.}

\item{title}{Character or \code{NULL}. Plot title.}

\item{output_path}{Character or \code{NULL}. Where to save. Extension picks the
device (\code{.png}, \code{.pdf}, \code{.svg}, ...). \code{NULL} returns the plot undrawn.}

\item{width, height}{Numeric. Inches. Defaults 9 and 7.}

\item{dpi}{Integer. Raster resolution. Default 300.}

\item{base_size}{Numeric. Base text size in points. Default 10.}

\item{background}{Colour for the plot background. Default \code{"white"}.
\code{\link[ggplot2:theme_void]{ggplot2::theme_void()}} leaves the background blank, which writes a
TRANSPARENT PNG -- fine on a white web page, unpredictable once the figure
is placed in a Word manuscript or on a coloured slide, where whatever is
behind it shows through the boxes. Pass \code{NA} for a transparent background
deliberately.}
}
\value{
Invisibly, the \code{ggplot} object.
}
\description{
Renders a \code{\link[=mysterycall_flow_spec]{mysterycall_flow_spec()}} as a STROBE/CONSORT participant-flow
diagram: a main column of steps, exclusion boxes teeing off to the right,
and any splits drawn beneath their parent.
}
\details{
Uses ggplot2 only. There is no Graphviz or htmlwidget path, and so no
headless browser is needed to write a PNG -- the diagram renders in a plain
CI container, which is where a check of its numbers is worth having.
}
\examples{
spec <- mysterycall_flow_spec(
  spine = c("Screened" = 100, "Enrolled" = 80),
  exclusions = list("Screened" = c("Ineligible" = 15, "Declined" = 5)),
  splits = list("Enrolled" = c("Completed" = 70, "Withdrew" = 10))
)
p <- mysterycall_strobe_diagram(spec, title = "Participant flow")

}
\seealso{
\code{\link[=mysterycall_flow_spec]{mysterycall_flow_spec()}}
}
