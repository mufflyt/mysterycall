% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/acceptance_waffle.R
\name{mysterycall_acceptance_waffle}
\alias{mysterycall_acceptance_waffle}
\title{Waffle chart of insurance acceptance rates}
\usage{
mysterycall_acceptance_waffle(
  acceptance_result,
  medicaid_label = "Medicaid",
  bcbs_label = "Blue Cross / Blue Shield",
  colors = c(Accepted = "grey20", Declined = "grey85"),
  rows = 10L,
  title = NULL,
  flip = FALSE,
  monochrome = TRUE,
  dpi = 150L,
  output_dir = NULL,
  filename = "acceptance_waffle.png"
)
}
\arguments{
\item{acceptance_result}{A data frame with at least columns
\code{insurance_type}, \code{outcome} (values \code{"Accepted"} / \code{"Declined"}), and
\code{n}.  Typically the output of \code{table(df$insurance_type, df$outcome)} or a
\code{dplyr::count()} summary.}

\item{medicaid_label}{Character scalar.  Label for the Medicaid panel.
Default \code{"Medicaid"}.}

\item{bcbs_label}{Character scalar.  Label for the BCBS panel.
Default \code{"Blue Cross / Blue Shield"}.}

\item{colors}{Named character vector.  Colours for each outcome level.
Default \code{c(Accepted = "grey20", Declined = "grey85")}.}

\item{rows}{Integer.  Number of rows in each waffle grid.  Default \code{10L}.}

\item{title}{Character or \code{NULL}.  Main chart title added via
\pkg{patchwork}.  \code{NULL} produces no title.}

\item{flip}{Logical.  If \code{TRUE}, flip the waffle orientation (columns
become rows).  Default \code{FALSE}.}

\item{monochrome}{Logical.  If \code{TRUE} (default), apply
\code{\link[=mysterycall_bw_theme]{mysterycall_bw_theme()}} to each panel for Greene-journal compatibility.}

\item{dpi}{Integer.  Resolution for the saved PNG.  Default \code{150L}.}

\item{output_dir}{Character scalar or \code{NA}.  Directory for PNG output.
\code{NA} skips writing.  Defaults to a session temp directory.}

\item{filename}{Character scalar.  Output file name.
Default \code{"acceptance_waffle.png"}.}
}
\value{
A \code{patchwork} / \code{ggplot} object, returned invisibly.
}
\description{
Produces a side-by-side waffle chart comparing appointment acceptance rates
for two insurance groups (e.g. Medicaid vs. Blue Cross Blue Shield).
Each square represents one call; shading encodes Accepted vs. Declined.
}
\details{
Inspired by Nicola Rennie (2024), "Five Charts You Can Make with the
waffle Package", \url{https://nrennie.rbind.io/}.
}
\note{
Requires the \pkg{waffle} package (GitHub-only -- not on CRAN) and
\pkg{patchwork}.  Install with:
\code{remotes::install_github("hrbrmstr/waffle")} and
\code{install.packages("patchwork")}.
}
\examples{
\dontrun{
acc <- data.frame(
  insurance_type = c("Medicaid", "Medicaid", "BCBS", "BCBS"),
  outcome        = c("Accepted", "Declined", "Accepted", "Declined"),
  n              = c(47L, 53L, 72L, 28L),
  stringsAsFactors = FALSE
)
mysterycall_acceptance_waffle(acc, output_dir = NA)
}
}
\seealso{
Other visualization:
\code{\link[=mysterycall_bw_theme]{mysterycall_bw_theme()}},
\code{\link[=mysterycall_flowchart]{mysterycall_flowchart()}},
\code{\link[=mysterycall_plot_density]{mysterycall_plot_density()}},
\code{\link[=mysterycall_plot_line]{mysterycall_plot_line()}},
\code{\link[=mysterycall_plot_scatter]{mysterycall_plot_scatter()}}
}
\concept{visualization}
