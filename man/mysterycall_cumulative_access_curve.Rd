% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cumulative_access_curve.R
\name{mysterycall_cumulative_access_curve}
\alias{mysterycall_cumulative_access_curve}
\title{Cumulative appointment-acquisition curve for single-contact designs}
\usage{
mysterycall_cumulative_access_curve(
  data,
  time_col,
  offered_col,
  group_col = NULL,
  horizon = 90,
  plot = FALSE,
  output_dir = NA,
  filename = NA
)
}
\arguments{
\item{data}{A data frame, one row per call.}

\item{time_col}{Name of the wait-time column (business days to the appointment
among offered calls; ignored/\code{NA} for non-offered calls).}

\item{offered_col}{Name of the binary offered indicator. Values are coerced
with \code{\link[=as.logical]{as.logical()}} after mapping \code{1}/\code{0} and \code{"TRUE"}/\code{"FALSE"}; anything
that is not truthy counts as not offered.}

\item{group_col}{Optional grouping column; one curve per group. If \code{NULL}, a
single overall curve is produced.}

\item{horizon}{Maximum business day to evaluate (waits are capped here).
Default \code{90}.}

\item{plot}{Logical; if \code{TRUE} (requires \pkg{ggplot2}) attach a step-curve
figure to \verb{$plot}.}

\item{output_dir, filename}{Optional. If both are non-\code{NA}, the tidy curve
table is written to \code{file.path(output_dir, filename)} as CSV.}
}
\value{
An object of class \code{"mysterycall_cumulative_access_curve"}: a list
with \code{table} (tidy \code{group}, \code{day}, \code{p}, \code{n}), \code{plateau} (each group's final
cumulative proportion = its offer rate), \code{horizon}, and \code{plot}.
\code{\link[=as.data.frame]{as.data.frame()}} returns the tidy table.
}
\description{
Plots (and tabulates) the empirical cumulative proportion of \emph{all} calls that
had secured an appointment by business-day \code{t}. This is deliberately \strong{not} a
Kaplan-Meier / time-to-event analysis: a mystery-caller study is a single
simulated contact, not longitudinal follow-up, so there is no censoring and no
at-risk set. Calls that never received an offer are retained in the
denominator as never obtaining an appointment, which is why each curve
plateaus at its group's offer rate rather than approaching 1.
}
\details{
Prefer this over \code{\link[=mysterycall_kaplan_meier]{mysterycall_kaplan_meier()}} when "wait" is the delay to a
one-shot offer: KM right-censors the non-offered calls at the horizon, which
implies continued follow-up the design does not have and inflates the implied
cumulative probability.
}
\examples{
set.seed(1)
d <- data.frame(
  wait_days = c(5, 12, NA, 30, 8, NA, 45, 20),
  offered   = c(1, 1, 0, 1, 1, 0, 1, 1),
  group     = rep(c("A", "B"), each = 4)
)
res <- mysterycall_cumulative_access_curve(
  d, "wait_days", "offered", "group", horizon = 60
)
res$plateau
}
