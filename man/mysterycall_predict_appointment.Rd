% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_appointment.R
\name{mysterycall_predict_appointment}
\alias{mysterycall_predict_appointment}
\title{Predict appointment probability for new patient profiles}
\usage{
mysterycall_predict_appointment(
  fit,
  newdata,
  re.form = NA,
  conf_level = 0.95,
  allow_new_levels = TRUE,
  ci = TRUE
)
}
\arguments{
\item{fit}{A \code{mysterycall_logistic_model} object returned by
\code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}}.}

\item{newdata}{A data frame of new patient/practice profiles to predict.
Must contain all fixed-effect predictors used when fitting \code{fit}.}

\item{re.form}{Formula or \code{NA}. Passed to \code{\link[lme4:predict.merMod]{lme4::predict.merMod()}}.
\code{NA} (default) gives \strong{population-level} predictions (ignores random
effects -- appropriate for new practices). \code{NULL} includes the estimated
random effects for practices already in the training data.}

\item{conf_level}{Numeric in (0, 1). Confidence level for the intervals.
Default \code{0.95}.}

\item{allow_new_levels}{Logical. Whether to allow new random-effect levels
(new practice names) in \code{newdata}. Default \code{TRUE}.}

\item{ci}{Logical. Whether to compute confidence intervals via the delta
method on the logit scale. Default \code{TRUE}. Set to \code{FALSE} for faster
predictions when CIs are not needed.}
}
\value{
The input \code{newdata} data frame with three columns appended:
\describe{
\item{\code{prob}}{Predicted probability of appointment acceptance (0-1).}
\item{\code{ci_lower}}{Lower confidence bound on the probability scale.
\code{NA} when \code{ci = FALSE}.}
\item{\code{ci_upper}}{Upper confidence bound on the probability scale.
\code{NA} when \code{ci = FALSE}.}
}
}
\description{
Generates predicted probabilities of appointment acceptance (and optional
confidence intervals) from a fitted \code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}} object.
Predictions are \strong{population-level} by default (\code{re.form = NA}), which is
appropriate for new practices not seen during model training.
}
\details{
\strong{Delta method CIs:} Standard errors are computed by propagating the
fixed-effects variance-covariance matrix through the model matrix for
\code{newdata}, then transforming the Wald interval from the logit scale back
to the probability scale via \code{plogis()}. This approach ignores random-effect
uncertainty and is therefore anti-conservative when the number of clusters
is small.

\strong{Population-level vs. cluster-level:} With \code{re.form = NA} (default),
predictions represent the average practice. With \code{re.form = NULL}, the
function uses estimated random intercepts for practices seen during
training; new practice names require \code{allow_new_levels = TRUE} (which
sets the random intercept to zero, equivalent to an average practice).
}
\examples{
\donttest{
set.seed(1)
df <- data.frame(
  practice  = rep(paste0("P", 1:10), each = 6),
  insurance = rep(c("Medicaid", "BCBS"), 30),
  scenario  = rep(c("A", "B", "C"), 20),
  accepted  = rbinom(60, 1, prob = ifelse(
    rep(c("Medicaid", "BCBS"), 30) == "Medicaid", 0.35, 0.65
  ))
)
fit <- mysterycall_logistic_model(df, "accepted",
                                  c("insurance", "scenario"),
                                  "practice")
new_profiles <- data.frame(
  insurance = c("Medicaid", "BCBS", "Medicaid"),
  scenario  = c("A",        "A",    "B"),
  practice  = c("new1",     "new1", "new2")
)
mysterycall_predict_appointment(fit, new_profiles)
}

}
\seealso{
\code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}}, \code{\link[=mysterycall_forest_plot]{mysterycall_forest_plot()}}
}
\concept{logistic model helpers}
