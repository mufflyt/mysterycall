% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_near_duplicate_keys.R
\name{mysterycall_flag_near_duplicate_keys}
\alias{mysterycall_flag_near_duplicate_keys}
\title{Flag near-duplicate cluster keys (likely mistyped grouping values)}
\usage{
mysterycall_flag_near_duplicate_keys(
  keys,
  max_edit = 4L,
  max_norm = 0.15,
  ignore_case = TRUE,
  output_dir = NA,
  filename = NA
)
}
\arguments{
\item{keys}{Character vector of cluster keys (or a data-frame column). Missing
and empty entries are dropped before comparison.}

\item{max_edit}{Integer. Flag a pair when its raw edit distance is at most
this. Default \code{4L}.}

\item{max_norm}{Numeric in \verb{[0, 1]}. Flag a pair when its normalized edit
distance is at most this. Default \code{0.15}.}

\item{ignore_case}{Logical; compare case-insensitively. Default \code{TRUE}.}

\item{output_dir, filename}{Optional CSV export of the flagged pairs.
\code{output_dir = NA} (default) skips writing; \code{NULL} writes to a session temp
directory via \code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per flagged pair -- columns \code{key_a},
\code{key_b}, \code{edit_distance} (integer), \code{norm_distance} (rounded to 3) -- sorted
by \code{edit_distance} then \code{norm_distance}. Zero rows (with the same columns)
when nothing is flagged.
}
\description{
A random-intercept mystery-caller analysis clusters calls by a practice /
cluster key. When that key is entered by hand across data-collection waves, a
single mistyped character (\code{"Aurora Womens Health"} vs.
\code{"Aurora Women's Health"}) splits one practice into two, silently corrupting
the estimated random-effect variance and deflating every paired denominator
(see \code{\link[=mysterycall_scenario_coverage]{mysterycall_scenario_coverage()}}). Exact-duplicate checks
(\code{\link[=mysterycall_check_duplicates]{mysterycall_check_duplicates()}}) cannot catch this because the strings
differ. This scans all unique keys with an edit-distance
(\code{\link[utils:adist]{utils::adist()}}) and returns the pairs that are close enough to be worth a
human's eyes -- a loud safety net rather than a silent singleton.
}
\details{
A pair is flagged when its raw edit distance is at most \code{max_edit} \strong{or} its
length-normalized distance (edit distance / longer key length) is at most
\code{max_norm}. The \code{OR} catches both short keys a few characters apart and long
keys differing by a small fraction. Comparison is case- and
whitespace-insensitive by default; feed keys already run through
\code{\link[=mysterycall_normalize_org_name]{mysterycall_normalize_org_name()}} to cut false positives.
}
\examples{
keys <- c("Aurora Womens Health", "Aurora Women's Health",
          "Denver Fertility", "Boulder OBGYN")
mysterycall_flag_near_duplicate_keys(keys)
}
\seealso{
\code{\link[=mysterycall_check_duplicates]{mysterycall_check_duplicates()}}, \code{\link[=mysterycall_normalize_org_name]{mysterycall_normalize_org_name()}},
\code{\link[=mysterycall_scenario_coverage]{mysterycall_scenario_coverage()}}

Other data integrity: 
\code{\link{.mc_age_impute}()},
\code{\link{.mc_age_key}()},
\code{\link{.mc_check_acs_vintage}()},
\code{\link{.mc_data}()},
\code{\link{.mc_geo_layer}()},
\code{\link{.mc_geocode_point}()},
\code{\link{.mc_healthgrades_ages}()},
\code{\link{.mc_state_to_abbr}()},
\code{\link{mc-census-vintage}},
\code{\link{mysterycall_assign_area_covariates}()},
\code{\link{mysterycall_categorize_wait}()},
\code{\link{mysterycall_clean_zip}()},
\code{\link{mysterycall_link_physicians}()},
\code{\link{mysterycall_lookup_age}()},
\code{\link{mysterycall_parse_duration}()}
}
\concept{data integrity}
