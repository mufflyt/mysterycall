% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paired_analyses.R
\name{mysterycall_paired_acceptance_mcnemar}
\alias{mysterycall_paired_acceptance_mcnemar}
\title{Within-practice paired McNemar test for a binary acceptance outcome}
\usage{
mysterycall_paired_acceptance_mcnemar(
  data,
  id_col,
  scenario_col,
  outcome_col,
  positive_values = TRUE,
  contrasts = NULL,
  power = 0.8,
  alpha = 0.05,
  output_dir = NA,
  filename = NA
)
}
\arguments{
\item{data}{A long data frame: one row per practice x scenario.}

\item{id_col}{Practice / cluster identifier column.}

\item{scenario_col}{Scenario column (2+ levels).}

\item{outcome_col}{Binary acceptance column.}

\item{positive_values}{Value(s) of \code{outcome_col} counted as acceptance.
Default \code{TRUE} (also matches \code{"TRUE"}/\code{1}/\code{"Yes"} via \code{\%in\%}).}

\item{contrasts}{Optional list of length-2 character vectors naming the
scenario pairs to test. Default \code{NULL} tests all pairwise combinations.}

\item{power, alpha}{Power and two-sided level for the minimum-detectable odds
ratio. Defaults \code{0.80} and \code{0.05}.}

\item{output_dir, filename}{Optional CSV export of the result table.}
}
\value{
An object of class \code{"mysterycall_paired_mcnemar"}: a list with \code{table}
(one row per contrast: \code{contrast}, \code{n_paired}, \code{concordant}, \code{discordant},
\code{disc_favor_a}, \code{disc_favor_b}, \code{odds_ratio}, \code{mcnemar_p},
\code{mde_or_power}). \code{\link[=as.data.frame]{as.data.frame()}} returns the table.
}
\description{
The unmatched comparison of acceptance across scenarios (insurance types,
caller personas) is confounded whenever the scenarios were not called at the
same set of practices -- one scenario's calls can land disproportionately at
generous practices. The matched comparison removes each practice's baseline
generosity: only practices called under \emph{both} scenarios contribute, and for a
binary outcome only the \strong{discordant} practices (a different answer to the
two callers) carry information, so the effective sample size is the discordant
count, not the paired count. This runs the exact McNemar test on each pairwise
scenario contrast and reports the discordant split, odds ratio, exact p-value,
and the minimum detectable odds ratio at the requested power.
}
\examples{
set.seed(1)
d <- data.frame(
  practice = rep(1:40, each = 2),
  scenario = rep(c("Commercial", "Medicaid"), 40),
  accepted = rbinom(80, 1, rep(c(0.8, 0.5), 40))
)
mysterycall_paired_acceptance_mcnemar(d, "practice", "scenario", "accepted")
}
