% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/physicians_with_detail.R
\name{mysterycall_physicians_with_detail}
\alias{mysterycall_physicians_with_detail}
\title{Retrieve detailed records for a set of flagged physician IDs}
\usage{
mysterycall_physicians_with_detail(
  data,
  flagged_ids,
  id_col = "id_number",
  select_cols = c("id_number", "physician_information", "reason_for_exclusions",
    "insurance", "business_days_until_appointment"),
  output_dir = NULL,
  filename = "physicians_with_detail.csv"
)
}
\arguments{
\item{data}{A data frame of mystery-caller records.}

\item{flagged_ids}{A character or numeric vector of IDs to look up, \strong{or} a
data frame that contains a column named \code{id_col} (the unique values of that
column are used as the lookup set).}

\item{id_col}{Character scalar. Column to match on in both \code{data} and
(when \code{flagged_ids} is a data frame) \code{flagged_ids}. Default \code{"id_number"}.}

\item{select_cols}{Character vector. Columns to return (only those that
actually exist in \code{data} are kept). Default:
\code{c("id_number", "physician_information", "reason_for_exclusions", "insurance", "business_days_until_appointment")}.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for the CSV output.
\code{NULL} (default) writes to a session temp directory via
\code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing entirely.}

\item{filename}{Character scalar. Name of the output CSV file.
Default \code{"physicians_with_detail.csv"}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of matching rows arranged by \code{id_col}. Returns
a zero-row tibble (with the requested columns) when no IDs match.
}
\description{
Filters the full mystery-caller data frame to the rows whose \code{id_col} value
appears in \code{flagged_ids}, then returns the requested \code{select_cols} arranged
by \code{id_col}. Mirrors the source-Rmd pattern:
\preformatted{
df \%>\%
  filter(id_number \%in\% unique(temp$id_number)) \%>\%
  dplyr::select(id_number, physician_information,
                reason_for_exclusions, insurance,
                business_days_until_appointment) \%>\%
  arrange(id_number)
}
}
\examples{
df <- data.frame(
  id_number                      = c("001", "002", "003", "004"),
  physician_information          = c("Dr A", "Dr B", "Dr C", "Dr D"),
  reason_for_exclusions          = c("Able to contact", NA,
                                     "No appointment", "Able to contact"),
  insurance                      = c("Medicaid", "BCBS", "Medicaid", "BCBS"),
  business_days_until_appointment = c(5, NA, NA, 10),
  stringsAsFactors               = FALSE
)
mysterycall_physicians_with_detail(df, flagged_ids = c("001", "003"),
                                   output_dir = NA)
}
\seealso{
\code{\link[=mysterycall_flag_exclusion_discrepancy]{mysterycall_flag_exclusion_discrepancy()}},
\code{\link[=mysterycall_flag_repeat_physicians]{mysterycall_flag_repeat_physicians()}}

Other descriptive helpers:
\code{\link[=mysterycall_demographics_sentence]{mysterycall_demographics_sentence()}},
\code{\link[=mysterycall_descriptive_stats]{mysterycall_descriptive_stats()}},
\code{\link[=mysterycall_distribution_summary]{mysterycall_distribution_summary()}},
\code{\link[=mysterycall_facet_histogram]{mysterycall_facet_histogram()}},
\code{\link[=mysterycall_log_histogram]{mysterycall_log_histogram()}},
\code{\link[=mysterycall_scenario_summary]{mysterycall_scenario_summary()}},
\code{\link[=mysterycall_sensitivity_both_insurance]{mysterycall_sensitivity_both_insurance()}}
}
\concept{descriptive helpers}
