% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjusted_power.R
\name{mysterycall_adjusted_power}
\alias{mysterycall_adjusted_power}
\title{Simulation power for a covariate-adjusted NB GLMM with a cluster ICC}
\usage{
mysterycall_adjusted_power(
  n_total,
  rural_frac,
  wait_urban,
  wait_rural,
  phi,
  state_icc = 0.05,
  n_states = 51,
  n_subspecs = 7,
  n_extra_covars = 6,
  alpha = 0.05,
  n_sim = 100,
  seed = NULL
)
}
\arguments{
\item{n_total}{Integer total sample size (calls).}

\item{rural_frac}{Fraction of calls in the rural (exposure) group, in
\verb{(0, 1)}.}

\item{wait_urban, wait_rural}{Mean wait (days) in the urban and rural groups;
their ratio is the true effect on the log scale.}

\item{phi}{Negative-binomial dispersion (\code{size}); smaller is more
overdispersed.}

\item{state_icc}{Target intraclass correlation for the cluster random
intercept, in \verb{[0, 0.9)}. Default \code{0.05}.}

\item{n_states}{Number of clusters (random-intercept levels). Default \code{51}.}

\item{n_subspecs}{Number of subspecialty fixed-effect levels (\code{1} drops the
term). Default \code{7}.}

\item{n_extra_covars}{Number of nuisance adjustment covariates. Default \code{6}.}

\item{alpha}{Two-sided significance level. Default \code{0.05}.}

\item{n_sim}{Number of Monte Carlo replicates. Default \code{100}.}

\item{seed}{Optional integer seed for reproducibility. Default \code{NULL}.}
}
\value{
A one-row \code{\link[tibble:tibble]{tibble::tibble()}}: \code{n_total}, \code{n_rural}, \code{n_urban},
\code{power} (rural-effect rejection rate), \code{mean_log_effect}, \code{mean_se},
\code{median_se}, \code{convergence_rate}, \code{state_icc}, \code{sigma_state}, \code{n_states},
\code{n_subspecs}, \code{n_extra_covars}, \code{n_sim}. Pair with \code{\link[=mysterycall_find_mde]{mysterycall_find_mde()}}
to solve for a detectable effect, or call across an \code{n_total} grid for a
power curve.
}
\description{
The existing power tools cluster on the \emph{physician} (paired calls). A
geographic access study instead adjusts for confounders and clusters callers
within a higher-level unit -- states, markets -- whose correlation is
expressed as an intraclass correlation coefficient (ICC), not a random-slope
design. Analysts usually paper over this with a "+20\% for clustering" rule of
thumb. This instead simulates power for the \emph{actual adjusted analysis}: a
negative-binomial GLMM for the wait outcome with a rural (exposure) fixed
effect, a subspecialty fixed effect, a configurable number of nuisance
adjustment covariates, and a \strong{state-level random intercept whose SD is
derived from a target ICC} via
\code{sigma = sqrt(ICC/(1 - ICC) * (trigamma(1/phi) + pi^2/3))} -- the same
latent-scale decomposition \code{\link[=mysterycall_icc]{mysterycall_icc()}} uses. It
reports the power to detect the rural effect along with the mean estimated
effect, its standard error, and the model convergence rate.
}
\details{
Requires the \pkg{glmmTMB} package.
}
\examples{
\donttest{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
  mysterycall_adjusted_power(
    n_total = 400, rural_frac = 0.15,
    wait_urban = 20, wait_rural = 28, phi = 1.7,
    state_icc = 0.05, n_sim = 20, seed = 1
  )
}
}
}
\seealso{
\code{\link[=mysterycall_marginal_power]{mysterycall_marginal_power()}}, \code{\link[=mysterycall_twopart_power]{mysterycall_twopart_power()}},
\code{\link[=mysterycall_find_mde]{mysterycall_find_mde()}}

Other power:
\code{\link[=mysterycall_lm_interaction_power]{mysterycall_lm_interaction_power()}},
\code{\link[=mysterycall_ttest_power]{mysterycall_ttest_power()}}
}
\concept{power}
