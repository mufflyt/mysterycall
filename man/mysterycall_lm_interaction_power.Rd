% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analytic_power.R
\name{mysterycall_lm_interaction_power}
\alias{mysterycall_lm_interaction_power}
\title{Analytic power for factorial linear-model terms via Cohen's f-squared}
\usage{
mysterycall_lm_interaction_power(
  terms,
  n_params,
  f2 = c(small = 0.02, medium = 0.15),
  alpha = 0.05,
  power = 0.9
)
}
\arguments{
\item{terms}{Either a named integer vector mapping term label to numerator df
(e.g. \code{c("rural" = 1, "rural x subspec" = 6)}), or a data frame with
columns \code{term} and \code{df_num}.}

\item{n_params}{Number of parameters (cell means) in the full saturated
model, e.g. \code{2 * n_subspec * n_insurance}. Added, with an intercept, to the
solved residual df to give a total N.}

\item{f2}{Named numeric vector of Cohen's f-squared effect sizes to report.
Default \code{c(small = 0.02, medium = 0.15)}.}

\item{alpha}{Significance level. Default \code{0.05}.}

\item{power}{Target power. Default \code{0.90}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}}, one row per term: \code{term}, \code{df_num}, and one
\verb{n_<name>} column per \code{f2} entry giving the required total N.
}
\description{
A saturated factorial audit model (e.g. \code{rural * subspecialty * insurance})
has a main effect and interaction for every term, each an F-test with its own
numerator degrees of freedom. This reports the total sample size each term
needs at a target power, using \code{\link[pwr:pwr.f2.test]{pwr::pwr.f2.test()}} with the correct
numerator df and adding back the full model's parameter count so the returned
figure is a usable N rather than a residual df.
}
\details{
Requires the \pkg{pwr} package.
}
\examples{
if (requireNamespace("pwr", quietly = TRUE)) {
  mysterycall_lm_interaction_power(
    terms = c("rural" = 1, "subspecialty" = 6, "rural x insurance" = 1),
    n_params = 2 * 7 * 2
  )
}
}
\seealso{
\code{\link[=mysterycall_ttest_power]{mysterycall_ttest_power()}}, \code{\link[=mysterycall_joint_test]{mysterycall_joint_test()}}

Other power:
\code{\link[=mysterycall_adjusted_power]{mysterycall_adjusted_power()}},
\code{\link[=mysterycall_ttest_power]{mysterycall_ttest_power()}}
}
\concept{power}
