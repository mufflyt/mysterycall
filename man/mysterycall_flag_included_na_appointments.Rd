% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_included_na_appointments.R
\name{mysterycall_flag_included_na_appointments}
\alias{mysterycall_flag_included_na_appointments}
\title{Flag "Included" Records with a Missing Wait Time}
\usage{
mysterycall_flag_included_na_appointments(
  data,
  days_col = "business_days_until_appointment",
  exclusion_col = "reason_for_exclusions",
  contact_value = "Able to contact",
  select_cols = c("physician_information", "id_number", "notes", "reason_for_exclusions",
    "business_days_until_appointment"),
  output_dir = NULL,
  filename = "included_with_na_appointments.csv"
)
}
\arguments{
\item{data}{A data frame of mystery-caller records.}

\item{days_col}{Character scalar. Name of the wait-time column.
Default \code{"business_days_until_appointment"}.}

\item{exclusion_col}{Character scalar. Name of the exclusion-reason column.
Default \code{"reason_for_exclusions"}.}

\item{contact_value}{Character scalar. The value in \code{exclusion_col} that
means the call succeeded. Default \code{"Able to contact"}.}

\item{select_cols}{Character vector of columns to return. Columns not
present in \code{data} are silently dropped.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for CSV output.
\code{NULL} writes to a session temp directory via \code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}.
Pass \code{NA} to skip writing.}

\item{filename}{Character scalar. Output CSV file name.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of flagged rows, sorted descending by
\code{id_number} (when present). Returns a zero-row tibble (invisibly) when
no records are flagged.
}
\description{
Finds records where \code{reason_for_exclusions == contact_value} (the call was
successfully completed) but \code{business_days_until_appointment} is \code{NA}.
These are data-entry gaps: a successfully-contacted physician should have a
recorded wait time.
}
\examples{
df <- data.frame(
  physician_information           = c("Dr A", "Dr B", "Dr C"),
  id_number                       = c("001", "002", "003"),
  notes                           = c(NA, NA, NA),
  reason_for_exclusions           = c("Able to contact", "Able to contact", "Not available"),
  business_days_until_appointment = c(NA, 5L, NA),
  stringsAsFactors = FALSE
)
# Only Dr A is flagged (included but NA wait time)
mysterycall_flag_included_na_appointments(df, output_dir = NA)
}
\seealso{
\code{\link[=mysterycall_flag_excluded_with_appointments]{mysterycall_flag_excluded_with_appointments()}} for the
complementary check; \code{\link[=mysterycall_flag_exclusion_discrepancy]{mysterycall_flag_exclusion_discrepancy()}} for a
generalised discrepancy detector.

Other quality control: 
\code{\link{mysterycall_flag_excluded_with_appointments}()},
\code{\link{mysterycall_flag_exclusion_discrepancy}()},
\code{\link{mysterycall_flag_repeat_physicians}()}
}
\concept{quality control}
