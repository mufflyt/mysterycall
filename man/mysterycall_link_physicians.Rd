% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/link_physicians.R
\name{mysterycall_link_physicians}
\alias{mysterycall_link_physicians}
\title{Probabilistic record linkage of two physician lists without a shared key}
\usage{
mysterycall_link_physicians(
  df_a,
  df_b,
  name_cols,
  block_var = NULL,
  threshold = 0.85,
  jw_weight = 0.25,
  partial_match = TRUE
)
}
\arguments{
\item{df_a, df_b}{The two data frames to link.}

\item{name_cols}{Character vector of the shared name columns to match on
(must exist in both frames), e.g. \code{c("first_name", "last_name")}.}

\item{block_var}{Optional character scalar naming a column present in both
frames to block on (e.g. \code{"state"}): only records sharing a block value are
compared, which makes large linkages feasible. Default \code{NULL} (no blocking).}

\item{threshold}{Posterior-probability cutoff for calling a pair a match.
Default \code{0.85}.}

\item{jw_weight}{Jaro-Winkler prefix weight passed to
\code{fastLink::fastLink()}. Default \code{0.25}.}

\item{partial_match}{Logical; when \code{TRUE} (default), allow partial string
agreement on the \code{name_cols} (passed to \code{fastLink}'s \code{partial.match}).}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of matched pairs -- the \code{name_cols} from each
frame (suffixed \verb{_a} / \verb{_b}), the source row indices \code{index_a} / \code{index_b},
and \code{posterior} (the match probability) -- sorted descending by
\code{posterior}. Zero rows when nothing clears \code{threshold}.
}
\description{
Audit sampling frames often must be reconciled against an external roster --
a called cohort against a Medicaid-acceptance list, applicants against a
resident roster -- when neither carries an NPI or any other unique key, only
names. Exact-name joins miss \code{"Katherine"} vs \code{"Kathryn"} and every
transposition; a deterministic join over-links common surnames. This wraps
\pkg{fastLink}'s Fellegi-Sunter linkage with Jaro-Winkler string comparison
on the name fields, optional blocking to keep the comparison space tractable,
and returns the matched pairs with their posterior match probability so a
human can review the borderline ones. Use it when you have names but no NPI;
when you have (or can look up) an NPI, prefer
\code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}} / \code{\link[=mysterycall_enrich_npi]{mysterycall_enrich_npi()}}.
}
\details{
Requires the \pkg{fastLink} package.
}
\examples{
\donttest{
if (requireNamespace("fastLink", quietly = TRUE)) {
  a <- data.frame(first_name = c("Katherine", "Robert"),
                  last_name  = c("Smith", "Jones"))
  b <- data.frame(first_name = c("Kathryn", "Bob"),
                  last_name  = c("Smith", "Jones"))
  mysterycall_link_physicians(a, b, c("first_name", "last_name"))
}
}
}
\seealso{
\code{\link[=mysterycall_normalize_org_name]{mysterycall_normalize_org_name()}},
\code{\link[=mysterycall_search_and_process_npi]{mysterycall_search_and_process_npi()}}

Other data integrity: 
\code{\link{.mc_age_impute}()},
\code{\link{.mc_age_key}()},
\code{\link{.mc_check_acs_vintage}()},
\code{\link{.mc_data}()},
\code{\link{.mc_geo_layer}()},
\code{\link{.mc_geocode_point}()},
\code{\link{.mc_healthgrades_ages}()},
\code{\link{.mc_state_to_abbr}()},
\code{\link{mc-census-vintage}},
\code{\link{mysterycall_assign_area_covariates}()},
\code{\link{mysterycall_categorize_wait}()},
\code{\link{mysterycall_clean_zip}()},
\code{\link{mysterycall_flag_near_duplicate_keys}()},
\code{\link{mysterycall_lookup_age}()},
\code{\link{mysterycall_parse_duration}()}
}
\concept{data integrity}
