% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overdispersion_threshold.R
\name{mysterycall_overdispersion_threshold}
\alias{mysterycall_overdispersion_threshold}
\title{Canonical overdispersion threshold for Poisson-vs-negative-binomial choice}
\usage{
mysterycall_overdispersion_threshold()
}
\value{
A single positive number: the current overdispersion threshold.
}
\description{
Every function in the package that reduces a Pearson dispersion statistic
(phi = sum(pearson_resid^2) / resid_df) to a "keep Poisson vs switch to
negative binomial" decision routes through this single value, so the model
choice can never again diverge between the automated selector and the
standalone diagnostics. When phi exceeds the threshold, a negative-binomial
(or otherwise overdispersion-robust) model is recommended.
}
\details{
The default is \code{1.5}, a common overdispersion rule of thumb. Override it
globally with \verb{options(mysterycall.overdispersion_threshold = <value>)};
every consumer (\code{mysterycall_auto_model()}, \code{mysterycall_poisson_model()},
\code{mysterycall_simple_poisson()}, \code{mysterycall_overdispersion_test()},
\code{mysterycall_overdispersion_sentence()}, \code{mysterycall_marginal_power()})
picks up the new value through its default argument.
}
\examples{
mysterycall_overdispersion_threshold()
withr::with_options(
  list(mysterycall.overdispersion_threshold = 2.0),
  mysterycall_overdispersion_threshold()
)
}
\seealso{
\code{\link[=mysterycall_auto_model]{mysterycall_auto_model()}}, \code{\link[=mysterycall_overdispersion_test]{mysterycall_overdispersion_test()}}
}
\concept{model selection}
