% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_consistency.R
\name{mysterycall_check_consistency}
\alias{mysterycall_check_consistency}
\title{Apply a battery of consistency rules to a call log}
\usage{
mysterycall_check_consistency(
  data,
  rules = mysterycall_default_consistency_rules(),
  id_col = NULL,
  keep_cols = NULL,
  output_dir = NA,
  filename = NA
)
}
\arguments{
\item{data}{A data frame (one row per call).}

\item{rules}{A list of \code{\link[=mysterycall_consistency_rule]{mysterycall_consistency_rule()}} objects. Defaults to
\code{\link[=mysterycall_default_consistency_rules]{mysterycall_default_consistency_rules()}}.}

\item{id_col}{Optional identifier column carried into the report so flagged
rows can be traced back to the source record.}

\item{keep_cols}{Optional character vector of extra columns to include in the
report for context.}

\item{output_dir, filename}{Optional. If both are non-\code{NA}, the worklist is
written to \code{file.path(output_dir, filename)} as CSV.}
}
\value{
An object of class \code{"mysterycall_consistency_report"}: a list with
\code{report} (a tibble of \code{row}, optional id/keep columns, \code{flag}, \code{priority},
\code{description}, \code{action}, sorted by priority then row), \code{summary} (per-flag
counts), and \code{n_rules}. \code{\link[=as.data.frame]{as.data.frame()}} returns the report.
}
\description{
Runs a list of cross-field rules over a call log and returns a single,
priority-sorted worklist of every row that violates a rule -- the general
form of the one-off \verb{mysterycall_flag_*} checks. Each flagged row appears
once per rule it trips, tagged with the rule's flag, priority, description,
and recommended action, so the output can drive a data-cleaning pass or a
correction report.
}
\examples{
d <- data.frame(
  record_id           = 1:3,
  office_answered     = c(FALSE, TRUE, TRUE),
  appointment_date    = c("2026-02-01", NA, "2026-03-01"),
  taking_new_patients = c("Yes", "Yes", "Yes"),
  complete            = c("Complete", "Complete", "Complete"),
  call_date           = c("2026-01-01", "2026-01-02", NA),
  caller              = c("AB", NA, "CD")
)
mysterycall_check_consistency(d, id_col = "record_id")
}
