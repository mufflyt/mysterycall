% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/univariate_poisson_screen.R
\name{mysterycall_univariate_poisson_screen}
\alias{mysterycall_univariate_poisson_screen}
\title{Screen predictors one-at-a-time using univariate Poisson GLMs}
\usage{
mysterycall_univariate_poisson_screen(
  data,
  outcome_col = "business_days_until_appointment",
  exclude_cols = outcome_col,
  alpha = 0.2,
  output_dir = NULL,
  filename = "univariate_poisson_screen.csv"
)
}
\arguments{
\item{data}{A data frame.}

\item{outcome_col}{Character scalar. The count or binary outcome column.}

\item{exclude_cols}{Character vector. Columns to exclude from the predictor
loop. Default is \code{outcome_col} alone; supply additional columns as needed.}

\item{alpha}{Numeric. Significance threshold for the \verb{$significant} table.
Default \code{0.2}.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for CSV output.
\code{NULL} uses \code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing.}

\item{filename}{Character scalar. CSV file name.
Default \code{"univariate_poisson_screen.csv"}.}
}
\value{
A named list with four elements:
\describe{
\item{\code{results}}{\code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{Variable}, \code{P_Value},
\code{Formatted_P_Value}, \code{Direction} for every predictor attempted.}
\item{\code{significant}}{Subset of \code{results} where \code{P_Value < alpha}, sorted
ascending by \code{P_Value}.}
\item{\code{sentence}}{Character scalar summarising significant predictors.}
\item{\code{alpha}}{The threshold used.}
}
}
\description{
For each predictor (excluding \code{outcome_col} and any \code{exclude_cols}), fits a
simple Poisson GLM (no random effects) using \code{\link[stats:glm]{stats::glm()}} with
\code{family = poisson(link = "log")}. Predictors with only one unique value are
skipped with a message.
}
\details{
Direction of association is determined from the sign of the predictor
coefficient: positive estimates are labelled \code{"Higher"} and negative
estimates \code{"Lower"}.
}
\examples{
set.seed(1)
df <- data.frame(
  wait_days = rpois(60, 10),
  insurance = rep(c("BCBS", "Medicaid"), 30),
  gender    = rep(c("M", "F"), 30),
  stringsAsFactors = FALSE
)
res <- mysterycall_univariate_poisson_screen(
  df, outcome_col = "wait_days", output_dir = NA
)
}
\seealso{
\code{\link[=mysterycall_univariate_lmm_screen]{mysterycall_univariate_lmm_screen()}},
\code{\link[=mysterycall_interaction_screen]{mysterycall_interaction_screen()}}

Other modeling helpers: 
\code{\link{mysterycall_check_normality}()},
\code{\link{mysterycall_create_formula}()},
\code{\link{mysterycall_interaction_screen}()},
\code{\link{mysterycall_overdispersion_sentence}()},
\code{\link{mysterycall_plot_interaction}()},
\code{\link{mysterycall_r2_sentence}()},
\code{\link{mysterycall_random_effect_variance}()},
\code{\link{mysterycall_univariate_lmm_screen}()}
}
\concept{modeling helpers}
