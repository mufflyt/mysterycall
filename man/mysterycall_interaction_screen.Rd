% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interaction_screen.R
\name{mysterycall_interaction_screen}
\alias{mysterycall_interaction_screen}
\title{Screen all pairwise factor interactions using linear mixed models}
\usage{
mysterycall_interaction_screen(
  data,
  outcome_col = "business_days_until_appointment",
  random_effect = "last",
  alpha = 0.05,
  p_adjust_method = "none",
  max_pairs = 50L,
  output_dir = NULL,
  filename = "interaction_screen.csv"
)
}
\arguments{
\item{data}{A data frame. Rows where \code{outcome_col} is \code{NA} are dropped.}

\item{outcome_col}{Character scalar. Numeric outcome column.
Default \code{"business_days_until_appointment"}.}

\item{random_effect}{Character scalar. Column for the random intercept.
Default \code{"last"}.}

\item{alpha}{Numeric. P-value threshold for a pair to be considered
significant. Default \code{0.05}.}

\item{p_adjust_method}{Character scalar passed to \code{\link[stats:p.adjust]{stats::p.adjust()}} after
all raw per-pair p-values are collected. \code{"none"} (default) skips
adjustment and preserves existing behaviour. Common choices: \code{"BH"},
\code{"bonferroni"}, \code{"holm"}. When not \code{"none"}, a \code{P_Value_Adjusted} column
is added to \verb{$interaction_results} and significance is evaluated against
the adjusted values.}

\item{max_pairs}{Integer. Maximum pairs to test. When the pool exceeds this,
\code{max_pairs} pairs are sampled randomly with \code{set.seed(42)}.
Default \code{50L}.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for CSV output.
\code{NULL} uses \code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing.}

\item{filename}{Character scalar. CSV file name.
Default \code{"interaction_screen.csv"}.}
}
\value{
A named list:
\describe{
\item{\code{interaction_results}}{\code{\link[tibble:tibble]{tibble::tibble()}} with columns
\code{Interaction} and \code{P_Value} for significant interactions.}
\item{\code{aic_results}}{\code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{Interaction} and
\code{AIC} for every converged model.}
\item{\code{best_interaction}}{Single-row tibble (lowest AIC), or \code{NULL} if no
models converged.}
\item{\code{n_pairs_tested}}{Integer. Number of pairs actually tested.}
\item{\code{sentence}}{Character scalar summarising the result.}
}
}
\description{
For every pair of factor columns in \code{data} (excluding \code{outcome_col} and
\code{random_effect}), fits a linear mixed model with an interaction term using
\code{\link[lmerTest:lmer]{lmerTest::lmer()}}. Pairs that yield at least one significant non-intercept
coefficient (p <= \code{alpha}) are collected. AIC is computed for every model
that converges, allowing the best-fitting interaction to be identified.
}
\details{
When the number of possible pairs exceeds \code{max_pairs}, a random sample of
\code{max_pairs} pairs is drawn with \code{set.seed(42)}.
}
\examples{
\dontshow{if (requireNamespace("lmerTest", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
df <- data.frame(
  business_days_until_appointment = rpois(60, 10),
  insurance = factor(rep(c("BCBS", "Medicaid"), 30)),
  gender    = factor(rep(c("M", "F"), 30)),
  last      = rep(paste0("Dr", 1:10), each = 6),
  stringsAsFactors = FALSE
)
res <- mysterycall_interaction_screen(df, output_dir = NA)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_univariate_lmm_screen]{mysterycall_univariate_lmm_screen()}},
\code{\link[=mysterycall_univariate_poisson_screen]{mysterycall_univariate_poisson_screen()}}

Other modeling helpers: 
\code{\link{mysterycall_check_normality}()},
\code{\link{mysterycall_create_formula}()},
\code{\link{mysterycall_overdispersion_sentence}()},
\code{\link{mysterycall_plot_interaction}()},
\code{\link{mysterycall_r2_sentence}()},
\code{\link{mysterycall_random_effect_variance}()},
\code{\link{mysterycall_univariate_lmm_screen}()},
\code{\link{mysterycall_univariate_poisson_screen}()}
}
\concept{modeling helpers}
