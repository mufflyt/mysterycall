% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/appointment_obtained.R
\name{mysterycall_appointment_obtained}
\alias{mysterycall_appointment_obtained}
\title{Derive the appointment-obtained indicator and wait, with same-day = 0}
\usage{
mysterycall_appointment_obtained(
  data,
  call_col = "call_date",
  appt_col = "appointment_date",
  obtained_col = "appt_obtained",
  wait_col = "business_days_until_appointment",
  add_wait = TRUE,
  calendar = NULL
)
}
\arguments{
\item{data}{A data frame, one row per call.}

\item{call_col}{Character scalar. Column holding the call date. Accepts
\code{Date}, \code{POSIXct}, or \code{"YYYY-MM-DD"} strings. Default \code{"call_date"}.}

\item{appt_col}{Character scalar. Column holding the offered appointment
date. An absent value (\code{NA} or empty string) means no appointment was
offered. Default \code{"appointment_date"}.}

\item{obtained_col}{Character scalar. Name of the binary indicator column to
add (\code{1} = appointment obtained, \code{0} = not obtained, \code{NA} = undetermined).
Default \code{"appt_obtained"}.}

\item{wait_col}{Character scalar. Name of the business-day wait column to add
when \code{add_wait = TRUE}. Default \code{"business_days_until_appointment"} -
matches the outcome name used across the package.}

\item{add_wait}{Logical. When \code{TRUE} (default) also attach the business-day
wait column. When \code{FALSE} only the obtained indicator is added.}

\item{calendar}{A \code{bizdays} calendar from
\code{\link[=mysterycall_us_federal_calendar]{mysterycall_us_federal_calendar()}}. Built automatically when \code{NULL}
(default). Pass a pre-built calendar when calling this many times.}
}
\value{
\code{data} with \code{obtained_col} (integer 0/1/NA) appended, and - when
\code{add_wait = TRUE} - \code{wait_col} (integer business days, \code{0} for same-day,
\code{NA} where no appointment was obtained or dates are invalid). The wait is
\code{NA} for every not-obtained row, matching the \code{wait | obtained} structure
\code{\link[=mysterycall_hurdle_wait]{mysterycall_hurdle_wait()}} expects.
}
\description{
The two-part / hurdle / Heckman models
(\code{\link[=mysterycall_hurdle_wait]{mysterycall_hurdle_wait()}}, selection models) need two derived columns from
the raw call and appointment dates: a binary \strong{appointment-obtained}
indicator (the selection / hurdle outcome) and, among obtained appointments,
the \strong{business-day wait}. Deriving these by hand is where a subtle but
consequential bug creeps in: computing the wait as \code{appt_date <= call_date}
-> \code{NA} sends \emph{same-day} appointments to \code{NA}, which then reads as "no
appointment obtained" in the selection stage. That contradicts the study
convention that a same-day appointment is a 0-day wait, and drops the
strongest-access cases from the model.
}
\details{
This function is the correct, reusable derivation. An appointment is
\strong{obtained} whenever a valid appointment date is present and is not before
the call date - \strong{same day counts as obtained, with a wait of 0}. The wait
is computed with \code{\link[=mysterycall_count_business_days]{mysterycall_count_business_days()}}, so it inherits the one
canonical same-day = 0 / appointment-before-call = \code{NA} policy instead of a
hand-rolled counter.
}
\section{Obtained / not-obtained / undetermined}{

\describe{
\item{obtained (\code{1})}{Appointment date present and on/after the call date
(includes same-day, wait \code{0}).}
\item{not obtained (\code{0})}{Call date present, appointment date absent.}
\item{undetermined (\code{NA})}{Appointment date present but \emph{before} the call
date (a data-entry error - also warned by
\code{\link[=mysterycall_count_business_days]{mysterycall_count_business_days()}}), or the call date is missing.}
}
}

\examples{
df <- data.frame(
  practice         = c("A", "B", "C", "D"),
  call_date        = as.Date(c("2026-02-02", "2026-02-02",
                                "2026-02-02", "2026-02-02")),
  appointment_date = as.Date(c("2026-02-02",  # same day  -> obtained, wait 0
                                "2026-02-06",  # 4 biz days -> obtained
                                NA,            # none       -> not obtained
                                "2026-01-30")) # before call-> undetermined
)
if (requireNamespace("bizdays", quietly = TRUE)) {
  mysterycall_appointment_obtained(df)
}
}
\seealso{
\code{\link[=mysterycall_count_business_days]{mysterycall_count_business_days()}}, \code{\link[=mysterycall_business_days]{mysterycall_business_days()}},
\code{\link[=mysterycall_hurdle_wait]{mysterycall_hurdle_wait()}}, \code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}}

Other business days:
\code{\link[=mysterycall_business_days]{mysterycall_business_days()}},
\code{\link[=mysterycall_count_business_days]{mysterycall_count_business_days()}},
\code{\link[=mysterycall_us_federal_calendar]{mysterycall_us_federal_calendar()}}
}
\concept{business days}
