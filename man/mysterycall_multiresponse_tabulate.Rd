% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_outcomes.R
\name{mysterycall_multiresponse_tabulate}
\alias{mysterycall_multiresponse_tabulate}
\title{Tabulate a Multi-Response ("Check-All-That-Apply") Call Outcome}
\usage{
mysterycall_multiresponse_tabulate(
  data,
  var,
  group_var = NULL,
  sep = ";",
  conf_level = 0.95,
  sort = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{var}{Character scalar. The multi-response column: either a list-column
of character vectors, or a delimited string (see \code{sep}). \code{NA} marks a
non-responding call (excluded from denominators); an empty string is a
responding call that named no options.}

\item{group_var}{Character scalar or \code{NULL}. Optional grouping column; when
supplied, per-option prevalence is also computed within each group.}

\item{sep}{Character scalar. Delimiter for string-valued option columns.
Default \code{";"}.}

\item{conf_level}{Numeric. Confidence level for Wilson intervals. Default
\code{0.95}.}

\item{sort}{Logical. Sort options by descending prevalence. Default \code{TRUE}.}
}
\value{
A \code{mysterycall_multiresponse} object: a list with \code{prevalence} (a
\code{\link[tibble:tibble]{tibble::tibble()}}), \code{cooccurrence} (an integer matrix; diagonal = option
counts, off-diagonal = joint counts), \code{summary} (n_calls, n_responding,
mean_options_per_call, pct_naming_any), \code{by_group} (tibble or \code{NULL}), and
\code{settings}.
}
\description{
Summarises an outcome where each call may name several options at once -- for
example the set of pain-management options a clinic offers (Sharma 2025) or
the alternatives a pharmacy suggests when a product is unavailable (Wilkinson
2018). Reports per-option prevalence with Wilson intervals, an option
co-occurrence matrix, and options-per-call summaries. Denominators use only
\emph{responding} calls; a call that responded but named nothing counts toward the
denominator with zero options.
}
\examples{
calls <- data.frame(
  options = c("ibuprofen;lidocaine", "ibuprofen", "", "misoprostol;ibuprofen"),
  region  = c("W", "W", "E", "E"),
  stringsAsFactors = FALSE
)
res <- mysterycall_multiresponse_tabulate(calls, "options", group_var = "region")
res$prevalence
}
\seealso{
Other call-outcomes:
\code{\link[=mysterycall_classify_call_outcome]{mysterycall_classify_call_outcome()}},
\code{\link[=mysterycall_ordinal_model]{mysterycall_ordinal_model()}},
\code{\link[=mysterycall_outcome_gradient]{mysterycall_outcome_gradient()}}
}
\concept{call-outcomes}
