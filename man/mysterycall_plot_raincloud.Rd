% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_raincloud.R
\name{mysterycall_plot_raincloud}
\alias{mysterycall_plot_raincloud}
\title{Raincloud plot of a numeric outcome by group}
\usage{
mysterycall_plot_raincloud(
  data,
  value,
  group,
  add_median_labels = TRUE,
  title = NULL,
  x_lab = NULL,
  y_lab = value,
  point_alpha = 0.6
)
}
\arguments{
\item{data}{A data frame.}

\item{value}{Character scalar naming the numeric outcome column (e.g. wait
days).}

\item{group}{Character scalar naming the grouping / scenario column.}

\item{add_median_labels}{Logical; annotate each group with its median. Default
\code{TRUE}.}

\item{title, x_lab, y_lab}{Character scalars for the plot title and axis labels.
\code{title} defaults to \code{NULL} (none); \code{x_lab} defaults to \code{NULL}; \code{y_lab}
defaults to \code{value}.}

\item{point_alpha}{Numeric opacity for the jittered points. Default \code{0.6}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
A raincloud layers a violin (the distribution), a boxplot (IQR and median),
and the jittered raw points (every observation) in one figure -- the
canonical way to show a right-skewed audit outcome such as wait time across
caller scenarios without hiding the sample size or the spread. Returns a
\code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} you can theme, facet, or save.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  d <- data.frame(
    wait     = c(rpois(40, 10), rpois(40, 25)),
    scenario = rep(c("Commercial", "Medicaid"), each = 40)
  )
  mysterycall_plot_raincloud(d, "wait", "scenario")
}
}
\seealso{
\code{\link[=mysterycall_plot_paired_slope]{mysterycall_plot_paired_slope()}}, \code{\link[=mysterycall_plot_distribution]{mysterycall_plot_distribution()}}

Other visualization: 
\code{\link{mysterycall_acceptance_waffle}()},
\code{\link{mysterycall_bw_theme}()},
\code{\link{mysterycall_flowchart}()},
\code{\link{mysterycall_plot_density}()},
\code{\link{mysterycall_plot_line}()},
\code{\link{mysterycall_plot_paired_slope}()},
\code{\link{mysterycall_plot_scatter}()}
}
\concept{visualization}
