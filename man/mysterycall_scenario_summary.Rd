% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenario_summary.R
\name{mysterycall_scenario_summary}
\alias{mysterycall_scenario_summary}
\title{Scenario call count summary and descriptive sentence}
\usage{
mysterycall_scenario_summary(
  data,
  scenario_col = "scenario",
  scenario_levels = NULL,
  contact_col = NULL,
  contact_value = "Able to contact",
  output_dir = NULL,
  filename = "scenario_summary.csv"
)
}
\arguments{
\item{data}{A data frame of mystery-caller records.}

\item{scenario_col}{Character scalar. Name of the column containing scenario
labels. Default \code{"scenario"}.}

\item{scenario_levels}{Named character vector. Maps display labels (names) to
column values (values), e.g.
\code{c(hip = "HIP scenario", shoulder = "SHOULDER scenario", knee = "KNEE scenario")}.
When \code{NULL} (default), all unique values found in \code{scenario_col} are used
and a generic sentence is generated.}

\item{contact_col}{Character scalar or \code{NULL}. If provided, also compute
scenario counts restricted to rows where \code{contact_col == contact_value}.
Default \code{NULL}.}

\item{contact_value}{Character scalar. The value in \code{contact_col} that
indicates a successful contact. Default \code{"Able to contact"}.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for CSV output.
\code{NULL} (default) writes to a session temp directory via
\code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing entirely.}

\item{filename}{Character scalar. Output CSV file name.
Default \code{"scenario_summary.csv"}.}
}
\value{
A named list with four elements:
\describe{
\item{\code{counts}}{\code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{scenario}, \code{count},
\code{percent}, arranged descending by \code{count}.}
\item{\code{total}}{Integer. Total number of rows in \code{data}.}
\item{\code{sentence}}{Character scalar. Manuscript-ready descriptive sentence.
When \code{scenario_levels} is \code{NULL} a generic format is used; when provided
the sentence follows the sports-medicine orthopedics template from the
source Rmd.}
\item{\code{contact_counts}}{\code{\link[tibble:tibble]{tibble::tibble()}} with the same columns as
\code{counts} but restricted to rows where \code{contact_col == contact_value}.
\code{NULL} when \code{contact_col} is not supplied.}
}
}
\description{
Counts the number of calls per scenario level, computes row percentages, and
builds a ready-to-paste sentence describing the distribution. Optionally
filters to successfully-contacted calls before generating the contact-level
scenario counts.
}
\examples{
df <- data.frame(
  scenario = c(
    "HIP scenario", "HIP scenario", "SHOULDER scenario",
    "KNEE scenario", "HIP scenario"
  ),
  status = rep("Able to contact", 5L),
  stringsAsFactors = FALSE
)
res <- mysterycall_scenario_summary(
  df,
  scenario_levels = c(
    hip      = "HIP scenario",
    shoulder = "SHOULDER scenario",
    knee     = "KNEE scenario"
  ),
  output_dir = NA
)
cat(res$sentence)
}
\seealso{
\code{\link[=mysterycall_sensitivity_both_insurance]{mysterycall_sensitivity_both_insurance()}}

Other descriptive helpers:
\code{\link[=mysterycall_demographics_sentence]{mysterycall_demographics_sentence()}},
\code{\link[=mysterycall_descriptive_stats]{mysterycall_descriptive_stats()}},
\code{\link[=mysterycall_distribution_summary]{mysterycall_distribution_summary()}},
\code{\link[=mysterycall_facet_histogram]{mysterycall_facet_histogram()}},
\code{\link[=mysterycall_log_histogram]{mysterycall_log_histogram()}},
\code{\link[=mysterycall_physicians_with_detail]{mysterycall_physicians_with_detail()}},
\code{\link[=mysterycall_sensitivity_both_insurance]{mysterycall_sensitivity_both_insurance()}}
}
\concept{descriptive helpers}
