% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supplemental_tables.R
\name{mysterycall_supplemental_tables}
\alias{mysterycall_supplemental_tables}
\title{Export a publication-ready supplemental Excel workbook}
\usage{
mysterycall_supplemental_tables(
  logistic_fit = NULL,
  poisson_fit = NULL,
  lmm_fit = NULL,
  extra_tables = list(),
  file = "supplemental_tables.xlsx",
  overwrite = FALSE,
  author = NULL
)
}
\arguments{
\item{logistic_fit}{A \code{mysterycall_logistic_model} object returned by
\code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}}, or \code{NULL} (default). When non-\code{NULL},
the full odds-ratio table (all terms, including the intercept) is written
to \strong{Sheet 1 "OR Table"}.}

\item{poisson_fit}{A \code{mysterycall_poisson_model} or \code{mysterycall_nb_model}
object, or \code{NULL} (default). When non-\code{NULL}, the full IRR table is
written to \strong{Sheet 2 "IRR Table"}.}

\item{lmm_fit}{A \code{mysterycall_lmm} object returned by
\code{\link[=mysterycall_lmm]{mysterycall_lmm()}}, or \code{NULL} (default). When non-\code{NULL}, the
coefficient table is written as an additional sheet ("LMM Coefs"); if a
geometric-mean-ratio table is present it is appended as "LMM GMR".}

\item{extra_tables}{A named \code{list} of \code{data.frame}s to include as
additional sheets. Each element name becomes the sheet name (truncated
to 31 characters as required by Excel). Default \code{list()}.}

\item{file}{Character scalar. Output file path. The \code{.xlsx} extension is
appended automatically when absent. Default \code{"supplemental_tables.xlsx"}.}

\item{overwrite}{Logical. When \code{FALSE} (default) the function stops if
\code{file} already exists. Set \code{TRUE} to silently overwrite.}

\item{author}{Character scalar or \code{NULL}. Author name written to the
workbook's Creator property. Default \code{NULL} (uses the current system
user via \code{\link[=Sys.info]{Sys.info()}}).}
}
\value{
\code{invisible(file)} — the normalised absolute path to the written
workbook, so the path can be captured and passed to downstream functions.
}
\description{
Collects results from \code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}},
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}, \code{\link[=mysterycall_nb_model]{mysterycall_nb_model()}}, and/or
\code{\link[=mysterycall_lmm]{mysterycall_lmm()}} into a single, formatted Excel workbook ready for
journal submission as an online supplement.
}
\details{
Each sheet receives zebra-stripe row shading, a bold dark-blue header row,
and auto-fitted column widths. The workbook metadata records the author name
(when supplied) and the creation date.
\subsection{Sheet contents}{\tabular{ll}{
   Sheet \tab Populated when \cr
   \strong{OR Table} \tab \code{logistic_fit} is non-\code{NULL} \cr
   \strong{IRR Table} \tab \code{poisson_fit} is non-\code{NULL} \cr
   \strong{Model Comparison} \tab at least two of the three model fits are non-\code{NULL} \cr
   \strong{Complete Cases} \tab any model fit is non-\code{NULL} \cr
   \strong{Session Info} \tab always \cr
   \strong{LMM Coefs} \tab \code{lmm_fit} is non-\code{NULL} \cr
   \strong{LMM GMR} \tab \code{lmm_fit} non-\code{NULL} and \code{lmm_fit$gmr_table} non-\code{NULL} \cr
   \emph{(extra names)} \tab corresponding element of \code{extra_tables} \cr
}

}

\subsection{Requires}{

The \code{openxlsx} package must be installed (it is listed in \code{Suggests}).
Install with \code{install.packages("openxlsx")}.
}
}
\examples{
\dontrun{
## Logistic + Poisson fits from a mystery-caller dataset
set.seed(42)
df <- data.frame(
  offered   = rbinom(80, 1L, 0.7),
  wait_days = rpois(80, lambda = 12),
  insurance = rep(c("Medicaid", "BCBS"), 40),
  physician = rep(paste0("Dr", 1:20), each = 4L),
  stringsAsFactors = FALSE
)

log_fit <- mysterycall_logistic_model(df, "offered",   "insurance", "physician")
poi_fit <- mysterycall_poisson_model(df, "wait_days",  "insurance", "physician")

out <- mysterycall_supplemental_tables(
  logistic_fit = log_fit,
  poisson_fit  = poi_fit,
  file         = "supplement.xlsx",
  overwrite    = TRUE,
  author       = "Tyler Muffly"
)
message("Workbook written to: ", out)
}
}
\seealso{
\code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}}, \code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}},
\code{\link[=mysterycall_nb_model]{mysterycall_nb_model()}}, \code{\link[=mysterycall_lmm]{mysterycall_lmm()}}, \code{\link[=mysterycall_model_table]{mysterycall_model_table()}}

Other reporting: 
\code{\link{mysterycall_abstract_numbers}()},
\code{\link{mysterycall_exclusion_summary}()},
\code{mysterycall_geographic_map()} (in the mysterymaps package),
\code{\link{mysterycall_irr_to_days}()},
\code{\link{mysterycall_results_paragraph}()},
\code{\link{mysterycall_session_snapshot}()},
\code{\link{mysterycall_write_results_paragraph}()},
\code{\link{print.mysterycall_abstract_numbers}()},
\code{\link{print.mysterycall_irr_days}()},
\code{\link{print.mysterycall_snapshot}()}
}
\concept{reporting}
