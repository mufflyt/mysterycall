% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_repeat_physicians.R
\name{mysterycall_flag_repeat_physicians}
\alias{mysterycall_flag_repeat_physicians}
\title{Flag Physicians Included More Than a Threshold Number of Times}
\usage{
mysterycall_flag_repeat_physicians(
  data,
  id_col = "id_number",
  name_col = "physician_information",
  threshold = 2L,
  output_dir = NULL,
  filename = "quality_check_repeat_physicians.csv"
)
}
\arguments{
\item{data}{A data frame of mystery-caller records.}

\item{id_col}{Character scalar. Name of the physician identifier column
(e.g. \code{"id_number"}, \code{"npi"}).}

\item{name_col}{Character scalar. Name of the physician name/info column
(e.g. \code{"physician_information"}). Set to \code{NULL} to group by \code{id_col} only.}

\item{threshold}{Integer. Flag physicians with a call count \strong{greater than}
this value. Default \code{2}.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for the CSV output.
\code{NULL} (default) writes to a session temp directory via
\code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing entirely.}

\item{filename}{Character scalar. Name of the output CSV file.
Default \code{"quality_check_repeat_physicians.csv"}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per flagged physician group,
columns \verb{<id_col>}, \verb{<name_col>} (if supplied), and \code{n_calls}
(descending). Returns a zero-row tibble (invisibly) when no physicians
exceed the threshold.
}
\description{
A quality-control check that identifies physician-call combinations
appearing more than \code{threshold} times in the dataset. Returns a summary
table and optionally writes it to CSV for audit review.
}
\section{Quality control interpretation}{

In mystery-caller studies each target physician is typically called once
or twice per wave. A count exceeding the threshold may indicate duplicate
data entry, a scheduling error, or a data-linkage problem that should be
resolved before analysis.
}

\examples{
df <- data.frame(
  id_number            = c("A", "A", "A", "B", "B", "C"),
  physician_information = c("Dr Smith", "Dr Smith", "Dr Smith",
                            "Dr Jones", "Dr Jones", "Dr Lee"),
  stringsAsFactors = FALSE
)
# Dr Smith appears 3 times (> 2) and should be flagged
result <- mysterycall_flag_repeat_physicians(df, output_dir = NA)
result
}
\seealso{
\code{\link[=mysterycall_sanity_checks]{mysterycall_sanity_checks()}} for broader pre-analysis checks.

Other quality control:
\code{\link[=mysterycall_clean_medicaid_col]{mysterycall_clean_medicaid_col()}},
\code{\link[=mysterycall_dedup_by_insurance]{mysterycall_dedup_by_insurance()}},
\code{\link[=mysterycall_flag_excluded_with_appointments]{mysterycall_flag_excluded_with_appointments()}},
\code{\link[=mysterycall_flag_exclusion_discrepancy]{mysterycall_flag_exclusion_discrepancy()}},
\code{\link[=mysterycall_flag_included_na_appointments]{mysterycall_flag_included_na_appointments()}}
}
\concept{quality control}
