% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/demographics_sentence.R
\name{mysterycall_demographics_sentence}
\alias{mysterycall_demographics_sentence}
\title{Build a Manuscript-Ready Demographics Sentence}
\usage{
mysterycall_demographics_sentence(
  data,
  gender_col = "gender",
  subspecialty_col = "Subspecialty",
  credential_col = "Provider.Credential.Text",
  digits = 1L,
  label_gender = "physician gender",
  label_subspecialty = "subspecialty",
  label_credential = "professional qualification"
)
}
\arguments{
\item{data}{A data frame.}

\item{gender_col}{Character scalar. Column name for physician gender.
Default \code{"gender"}.}

\item{subspecialty_col}{Character scalar or \code{NULL}. Column name for
subspecialty. Pass \code{NULL} to omit that clause. Default \code{"Subspecialty"}.}

\item{credential_col}{Character scalar or \code{NULL}. Column name for
professional credential/qualification. Pass \code{NULL} to omit. Default
\code{"Provider.Credential.Text"}.}

\item{digits}{Integer. Decimal places for percentages. Default \code{1}.}

\item{label_gender}{Character. Human-readable label for the gender clause.
Default \code{"physician gender"}.}

\item{label_subspecialty}{Character. Human-readable label for the
subspecialty clause. Default \code{"subspecialty"}.}

\item{label_credential}{Character. Human-readable label for the credential
clause. Default \code{"professional qualification"}.}
}
\value{
A character scalar (the sentence, visibly). The per-column summary
statistics are attached as attribute \code{"stats"} (a named list with elements
\verb{$gender}, \verb{$subspecialty}, \verb{$credential}, each a list with fields
\code{level}, \code{count}, \code{total}, \code{pct}). Invisibly, the full named list is also
available via \code{attr(result, "stats")}.
}
\description{
For each supplied column, finds the modal (most common) non-NA level, its
count, the total non-NA count, and the percentage. The results are assembled
into a single manuscript-ready sentence. Clauses for subspecialty and
credential can be suppressed by passing \code{NULL} to the corresponding column
argument.
}
\examples{
df <- data.frame(
  gender                   = c("Male", "Male", "Female", "Male"),
  Subspecialty             = c("REI", "MFM", "REI", "REI"),
  Provider.Credential.Text = c("MD", "DO", "MD", "MD"),
  stringsAsFactors = FALSE
)
sent <- mysterycall_demographics_sentence(df)
cat(sent)
}
\seealso{
Other descriptive helpers:
\code{\link[=mysterycall_descriptive_stats]{mysterycall_descriptive_stats()}},
\code{\link[=mysterycall_distribution_summary]{mysterycall_distribution_summary()}},
\code{\link[=mysterycall_facet_histogram]{mysterycall_facet_histogram()}},
\code{\link[=mysterycall_log_histogram]{mysterycall_log_histogram()}},
\code{\link[=mysterycall_physicians_with_detail]{mysterycall_physicians_with_detail()}},
\code{\link[=mysterycall_scenario_summary]{mysterycall_scenario_summary()}},
\code{\link[=mysterycall_sensitivity_both_insurance]{mysterycall_sensitivity_both_insurance()}}
}
\concept{descriptive helpers}
