% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_inclusion.R
\name{mysterycall_reconcile_inclusion}
\alias{mysterycall_reconcile_inclusion}
\title{Crosswalk between REDCap integer exclusion codes and label strings}
\usage{
mysterycall_reconcile_inclusion(
  data,
  code_col = "exclusions",
  label_col = "reason_for_exclusions",
  id_col = NULL,
  set = c("logistic", "waittime"),
  inclusion_value = "Able to contact",
  logistic_include_codes = c(0L, 7L, 9L, 10L),
  crosswalk = NULL
)
}
\arguments{
\item{data}{A data frame containing both \code{code_col} and \code{label_col}.}

\item{code_col}{Character scalar. Integer exclusion-code column.
Default \code{"exclusions"}.}

\item{label_col}{Character scalar. Label-string column.
Default \code{"reason_for_exclusions"}.}

\item{id_col}{Character scalar or \code{NULL}. Record identifier carried into the
discrepancy table for follow-up. Default \code{NULL}.}

\item{set}{Which inclusion set to reconcile. \code{"logistic"} (default) compares
the integer logistic set (\code{logistic_include_codes}) against the label
included set - this is where the paths diverge. \code{"waittime"} compares the
integer wait-time set (code 0) against the label included set, which are
expected to agree.}

\item{inclusion_value}{Character scalar. Label value treated as included.
Default \code{"Able to contact"}.}

\item{logistic_include_codes}{Integer vector forwarded to
\code{\link[=mysterycall_exclusion_crosswalk]{mysterycall_exclusion_crosswalk()}}. Default \code{c(0L, 7L, 9L, 10L)}.}

\item{crosswalk}{A crosswalk tibble from
\code{\link[=mysterycall_exclusion_crosswalk]{mysterycall_exclusion_crosswalk()}}. Built from \code{inclusion_value} and
\code{logistic_include_codes} when not supplied.}
}
\value{
A list of class \code{mysterycall_reconciliation} with elements:
\describe{
\item{\code{set}}{The set reconciled (\code{"logistic"} or \code{"waittime"}).}
\item{\code{n}}{Total rows compared.}
\item{\code{n_agree}}{Rows where both paths agree (both include or both
exclude).}
\item{\code{n_disagree}}{Rows where the paths disagree.}
\item{\code{agree_rate}}{\code{n_agree / n}.}
\item{\code{discrepancies}}{Data frame of the disagreeing rows, with
\code{disagreement_type} (\code{"code_only"} = integer path includes, label path
excludes; \code{"label_only"} = the reverse), both keys, the code label, and
\code{id_col} when supplied.}
\item{\code{by_code}}{Data frame: for each integer code, how many rows the
label path counted as included vs excluded, and whether that matches
the crosswalk expectation.}
\item{\code{label_mismatches}}{Data frame of rows whose \code{label_col} string is
inconsistent with the crosswalk's expected string for their integer
code (a data-entry integrity check independent of inclusion). \code{NULL}
when none.}
\item{\code{crosswalk}}{The crosswalk used.}
}
}
\description{
Compares, record by record, whether the integer-code path and the
label-string path agree on inclusion, and surfaces exactly where they
disagree. This is a \strong{diagnostic only} - it changes no data and picks no
winner. Use it to confirm (against real data) whether swapping the current
label-based scripts to \code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}} would change the
analytic sample, before committing to that consolidation.
}
\details{
The data frame must carry \strong{both} keys on the same rows: the integer
\code{exclusions} code (from the raw export) and the \code{reason_for_exclusions}
string (from the cleaned export). Reconciliation is the one scenario where
you have both.
}
\examples{
df <- data.frame(
  id                    = 1:6,
  exclusions            = c(0L, 0L, 9L, 7L, 5L, 10L),
  reason_for_exclusions = c(
    "Able to contact", "Able to contact",
    "Not accepting new patients",
    "Physician referral required before scheduling appointment",
    "Phone not answered or busy signal on repeat calls",
    "Must see midlevel first"
  ),
  stringsAsFactors = FALSE
)
rec <- mysterycall_reconcile_inclusion(df, id_col = "id")
rec
# Codes 9, 7, 10 are the divergence: integer-logistic includes them,
# the label path does not.
rec$discrepancies
}
\seealso{
\code{\link[=mysterycall_exclusion_crosswalk]{mysterycall_exclusion_crosswalk()}},
\code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}}, \code{\link[=mysterycall_acceptance_rate_calc]{mysterycall_acceptance_rate_calc()}}

Other data-preparation:
\code{\link[=mysterycall_exclusion_crosswalk]{mysterycall_exclusion_crosswalk()}},
\code{\link[=mysterycall_prepare_calls]{mysterycall_prepare_calls()}},
\code{\link[=print.mysterycall_prepared]{print.mysterycall_prepared()}},
\code{\link[=print.mysterycall_reconciliation]{print.mysterycall_reconciliation()}}
}
\concept{data-preparation}
