% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/twopart_power.R
\name{mysterycall_type_i_check}
\alias{mysterycall_type_i_check}
\title{Type I error calibration check for a simulation-based test}
\usage{
mysterycall_type_i_check(rejections, n_sim, alpha = 0.05)
}
\arguments{
\item{rejections}{Either the count of null replicates that rejected, or the
observed rejection \emph{rate} in \verb{[0, 1]} (auto-detected: a value \verb{<= 1} with a
non-integer or \code{n_sim > 1} is treated as a rate).}

\item{n_sim}{Number of null replicates.}

\item{alpha}{Nominal significance level. Default \code{0.05}.}
}
\value{
A one-row tibble: \code{rejection_rate}, \code{ci_low}, \code{ci_high}, \code{n_sim},
\code{alpha_in_ci}, \code{verdict}.
}
\description{
A companion to the simulation power functions: given the number of null
rejections observed when a test is run with the effect set to zero, report
the observed type I error rate, an exact binomial confidence interval, and a
verdict on whether the nominal \code{alpha} is plausibly attained. Use it to
confirm a Monte Carlo power simulator is well-calibrated before trusting its
power numbers.
}
\examples{
# 7 of 100 null replicates rejected at alpha = 0.05
mysterycall_type_i_check(7, n_sim = 100)
}
