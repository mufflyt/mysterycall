% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconcile_offer_outcome.R
\name{mysterycall_reconcile_offer_outcome}
\alias{mysterycall_reconcile_offer_outcome}
\title{Reconcile a binary "offered" flag against a granular outcome field}
\usage{
mysterycall_reconcile_offer_outcome(
  data,
  flag_col,
  outcome_col,
  offered_values,
  not_offered_values,
  dependent_cols = character(0),
  action = c("flag", "fix"),
  true_value = c(TRUE, "TRUE"),
  false_value = c(FALSE, "FALSE"),
  clear_value = NA,
  id_col = NULL
)
}
\arguments{
\item{data}{A data frame with one row per call.}

\item{flag_col}{Name of the binary offered flag column.}

\item{outcome_col}{Name of the granular disposition column.}

\item{offered_values}{Values of \code{outcome_col} that imply an appointment
\strong{was} offered.}

\item{not_offered_values}{Values of \code{outcome_col} that imply \strong{no}
appointment was offered.}

\item{dependent_cols}{Character vector of columns to blank out when a row is
flipped to "not offered" (e.g. \code{c("wait_days_business", "appointment_date")}).
Default none.}

\item{action}{\code{"flag"} (default) to detect and report discordance without
changing \code{data}, or \code{"fix"} to apply the corrections.}

\item{true_value, false_value}{The value(s) that represent offered / not
offered in \code{flag_col}. Matching uses \code{\%in\%}, so pass every spelling present
(defaults cover logical and character \code{TRUE}/\code{FALSE}). When \code{action = "fix"}
the first element of each is written back, coerced to the column's type.}

\item{clear_value}{Value written into \code{dependent_cols} on a not-offered flip.
Defaults to \code{NA}; use \code{""} for character columns you export with \code{na = ""}.}

\item{id_col}{Optional identifier column included in the discordance report.}
}
\value{
An object of class \code{"mysterycall_offer_reconciliation"}: a list with
\code{data} (unchanged when \code{action = "flag"}, corrected when \code{"fix"}),
\code{discordant} (a tibble: \code{row}, optional id, \code{flag}, \code{outcome}, \code{direction},
\code{suggested}), and \code{n_understated} / \code{n_overstated} counts.
\code{\link[=as.data.frame]{as.data.frame()}} returns the discordance report.
}
\description{
Call logs often record the same appointment fact twice: a coarse binary
"appointment offered" flag and a richer categorical disposition (e.g.
\code{"With sampled physician"}, \code{"No appointment offered"}). These drift apart
during data entry. This function finds — and optionally fixes — the two kinds
of contradiction:
}
\details{
\itemize{
\item \strong{flag understates} the outcome: the disposition names an appointment that
was offered, but the flag says it was not. The flag is set to "offered".
\item \strong{flag overstates} the outcome: the flag says an appointment was offered,
but the disposition says none was. The flag is set to "not offered" and any
\code{dependent_cols} (wait time, appointment date) are cleared so the
"a wait exists only when an appointment was offered" invariant holds.
}

The granular \code{outcome_col} is treated as authoritative. Rows whose outcome is
in neither \code{offered_values} nor \code{not_offered_values} are left untouched.
}
\examples{
d <- data.frame(
  record_id           = 1:4,
  appointment_offered = c(FALSE, TRUE, TRUE, FALSE),
  appointment_outcome = c("With sampled physician", "No appointment offered",
                          "With sampled physician", "No appointment offered"),
  wait_days_business  = c(10, 5, 8, NA)
)
res <- mysterycall_reconcile_offer_outcome(
  d, "appointment_offered", "appointment_outcome",
  offered_values     = "With sampled physician",
  not_offered_values = "No appointment offered",
  dependent_cols     = "wait_days_business",
  action = "fix", id_col = "record_id"
)
as.data.frame(res)
}
