% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/payer_mix.R
\name{mysterycall_get_payer_mix}
\alias{mysterycall_get_payer_mix}
\title{Build a county payer mix from ACS health-insurance coverage tables}
\usage{
mysterycall_get_payer_mix(
  year = NULL,
  states = NULL,
  geography = "county",
  verbose = TRUE
)
}
\arguments{
\item{year}{Integer. ACS 5-year survey end-year (2012-2023). Required — no
default, to keep results reproducible.}

\item{states}{Character vector of two-letter state abbreviations, or \code{NULL}
for all states (slow). Default \code{NULL}.}

\item{geography}{Character scalar passed to \code{\link[tidycensus:get_acs]{tidycensus::get_acs()}}.
Default \code{"county"}. Other useful values: \code{"tract"}, \code{"state"}, \code{"zcta"}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
A tibble with one row per geography and columns:
\describe{
\item{\code{GEOID}, \code{NAME}}{Geography identifier and label.}
\item{\code{total_pop}, \code{total_pop_moe}}{Civilian noninstitutionalized
population (S2701 denominator) and its MOE.}
\item{\code{n_private}, \code{n_public}, \code{n_medicare}, \code{n_medicaid},
\code{n_uninsured}}{Population counts with each coverage type.}
\item{\verb{*_moe}}{Propagated 90\% MOE for each count.}
\item{\code{pct_private}, \code{pct_public}, \code{pct_medicare}, \code{pct_medicaid},
\code{pct_uninsured}}{Share of \code{total_pop} (0-100) with each coverage type.}
\item{\code{pct_insured_s2701}, \code{pct_uninsured_s2701}}{Headline insured /
uninsured percentages taken directly from S2701.}
\item{\code{year}}{The ACS end-year supplied.}
}
}
\description{
Constructs a fuller insurance "payer mix" than the insured-vs-uninsured split
used in most access studies. Combines the ACS Subject table \strong{S2701}
("Selected Characteristics of Health Insurance Coverage", used here for the
headline insured / uninsured rates and the population denominator) with the
detailed coverage-type tables to add \strong{Private}, \strong{Public}, \strong{Medicaid},
and \strong{Medicare} shares:
}
\details{
\describe{
\item{Private}{Table \strong{B27002} — "With private health insurance".}
\item{Public}{Table \strong{B27003} — "With public coverage".}
\item{Medicare}{Table \strong{C27006} — "With Medicare coverage".}
\item{Medicaid}{Table \strong{C27007} — "With Medicaid/means-tested public
coverage".}
\item{Uninsured}{Table \strong{B27001} — "No health insurance coverage", plus the
S2701 percent-uninsured estimate.}
}

Because a person may hold more than one coverage type (e.g. dual
Medicare + Medicaid), the type shares are computed as a percentage of the
total civilian noninstitutionalized population and do \strong{not} sum to 100.
Coverage-type counts are obtained by summing every sex-by-age leaf whose
label carries the relevant "With ..." phrase, so the function self-corrects
across ACS vintages rather than depending on hard-coded cell numbers.

Margins of error are propagated with the Census Bureau sum-of-squares rule,
\eqn{MOE_{sum} = \sqrt{\sum MOE_i^2}} (ACS Handbook Appendix 3); all MOEs are
at the 90\% confidence level.
}
\note{
Requires a Census API key in the \code{CENSUS_API_KEY} environment variable
(see \code{\link[tidycensus:census_api_key]{tidycensus::census_api_key()}}). Shares are \code{NA} where \code{total_pop}
is zero.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
co <- mysterycall_get_payer_mix(year = 2022, states = "CO")
head(co[, c("NAME", "pct_private", "pct_medicaid", "pct_medicare",
            "pct_uninsured")])
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_get_acs_female_insurance]{mysterycall_get_acs_female_insurance()}} for the female-only,
single-county tract version built from the same sex-by-coverage-type
tables; this builder generalises it (all persons, multi-state, any
geography, with MOEs);
\code{\link[=mysterycall_get_acs_adults_18_90]{mysterycall_get_acs_adults_18_90()}} and \code{\link[=mysterycall_summarize_census]{mysterycall_summarize_census()}}
for other ACS covariate builders;
\code{\link[=mysterycall_get_county_provider_counts]{mysterycall_get_county_provider_counts()}} and
\code{\link[=mysterycall_summarize_county_enrollment]{mysterycall_summarize_county_enrollment()}} for the provider-supply and
Medicare/Medicaid-enrollment covariates that pair with payer mix.

Other census:
\code{\link[=mysterycall_add_hhi]{mysterycall_add_hhi()}},
\code{\link[=mysterycall_add_medicaid_expansion]{mysterycall_add_medicaid_expansion()}},
\code{\link[=mysterycall_census_female_population]{mysterycall_census_female_population()}},
\code{\link[=mysterycall_get_acs_adults_18_90]{mysterycall_get_acs_adults_18_90()}},
\code{\link[=mysterycall_get_acs_female_insurance]{mysterycall_get_acs_female_insurance()}},
\code{\link[=mysterycall_get_acs_women_18_90]{mysterycall_get_acs_women_18_90()}},
\code{\link[=mysterycall_get_census_data]{mysterycall_get_census_data()}},
\code{\link[=mysterycall_get_county_provider_counts]{mysterycall_get_county_provider_counts()}},
\code{\link[=mysterycall_plot_census_age]{mysterycall_plot_census_age()}},
\code{\link[=mysterycall_read_kff_hhi]{mysterycall_read_kff_hhi()}},
\code{\link[=mysterycall_summarize_census]{mysterycall_summarize_census()}},
\code{\link[=mysterycall_summarize_county_enrollment]{mysterycall_summarize_county_enrollment()}},
\code{\link[=print.mysterycall_provider_counts]{print.mysterycall_provider_counts()}}
}
\concept{census}
