% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_equation.R
\name{mysterycall_model_equation}
\alias{mysterycall_model_equation}
\title{Render a fitted model as a LaTeX equation}
\usage{
mysterycall_model_equation(
  model,
  coefficients = FALSE,
  digits = 3,
  wrap = TRUE
)
}
\arguments{
\item{model}{A fitted model (\code{glm}, \code{lme4::glmer}/\code{lmer}, \code{glmmTMB}) or a
mysterycall model wrapper (\code{mysterycall_logistic_model},
\code{mysterycall_poisson_model}, \code{mysterycall_nb_model}, \code{mysterycall_lmm}); the
underlying fit is extracted automatically.}

\item{coefficients}{If \code{TRUE}, substitute the fitted coefficient values for
the symbolic \eqn{\beta}s (a fitted equation); if \code{FALSE} (default) keep the
symbolic \eqn{\beta_k} with a term legend.}

\item{digits}{Rounding for fitted coefficients when \code{coefficients = TRUE}.
Default \code{3}.}

\item{wrap}{If \code{TRUE} (default) wrap the returned string in \verb{$$ ... $$} display
delimiters so it renders directly in R Markdown; if \code{FALSE} return the bare
math.}
}
\value{
A length-1 character string of LaTeX, with a \code{"legend"} attribute
mapping each \eqn{\beta_k} to its term label (when \code{coefficients = FALSE}).
Printed via \code{cat()} inside a \code{results='asis'} chunk it renders as an
equation.
}
\description{
Supplementary methods sections often print the statistical model as an
equation. This builds the LaTeX for a fitted generalized linear (mixed) model
-- the link-transformed outcome as a linear predictor over the fixed-effect
terms, plus any random intercept -- without the \pkg{equatiomatic}
dependency. It reads the family/link, the fixed-effect term labels, and the
random-effect grouping factor(s) straight off the fit.
}
\examples{
d <- data.frame(y = rbinom(100, 1, 0.5), x = rnorm(100),
                grp = factor(sample(letters[1:5], 100, TRUE)))
fit <- glm(y ~ x, family = binomial, data = d)
cat(mysterycall_model_equation(fit))
}
