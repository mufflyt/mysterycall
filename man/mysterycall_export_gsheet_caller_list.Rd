% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_gsheet_caller_list.R
\name{mysterycall_export_gsheet_caller_list}
\alias{mysterycall_export_gsheet_caller_list}
\title{Export a caller list in Google Sheets import format}
\usage{
mysterycall_export_gsheet_caller_list(
  src,
  out,
  study_title = "Mystery Caller Study",
  stage_col = "Stage 1 Calling",
  name_col = "Provider Name",
  phone_col = "Phone",
  npi_col = "NPI",
  state_col = "State",
  pair_col = "Matched Pair ID",
  group_col = "PE_or_Not",
  group_last = "PE",
  backup = NULL,
  verbose = TRUE
)
}
\arguments{
\item{src}{Path to the calling sheet CSV (one row per clinician). Must contain
\code{name_col}, \code{phone_col}, \code{npi_col}, \code{state_col}, \code{pair_col}, and \code{group_col}.}

\item{out}{Path to write the Google Sheets-formatted CSV.}

\item{study_title}{Character string placed in the first row (cell A1).}

\item{stage_col}{Name of the blank tracking column callers fill, e.g.
\code{"Stage 1 Calling"}.}

\item{name_col, phone_col, npi_col}{Input columns copied to the output, in this
order. Their names are reused verbatim as the output header.}

\item{state_col, pair_col}{Columns used for ordering. \code{pair_col} is coerced to
numeric so pairs sort naturally; each matched pair stays adjacent.}

\item{group_col, group_last}{Within a pair, the row whose \code{group_col} equals
\code{group_last} is placed second (e.g. put \code{"PE"} after \code{"Non-PE"}).}

\item{backup}{Optional path for a one-time backup of an existing \code{out} file.
The backup is written only if it does not already exist, preserving the
original. \code{NULL} (default) skips backup.}

\item{verbose}{Emit progress messages.}
}
\value{
(Invisibly) the ordered data frame that was written.
}
\description{
Writes a mystery-caller list to a CSV shaped for import into Google Sheets: a
study-title row (cell A1), a \verb{name / phone / NPI / <stage_col>} header, then one
row per clinician with the stage-tracking column left blank for callers. Rows
are ordered by state with each matched pair kept adjacent (the \code{group_last}
arm placed second within a pair).
}
\details{
The file is written with CRLF line endings and minimal quoting so it round-trips
cleanly through spreadsheet tools.
}
\examples{
\dontrun{
mysterycall_export_gsheet_caller_list(
  src = "calling_sheet.csv",
  out = "caller_list_for_google_sheets.csv",
  study_title = "Mystery Caller Study")
}
}
