% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_org_name.R
\name{mysterycall_normalize_org_name}
\alias{mysterycall_normalize_org_name}
\title{Normalize an organization / practice name for matching}
\usage{
mysterycall_normalize_org_name(
  x,
  drop_suffixes = c("THE", "LLC", "PLLC", "PC", "PA", "LP", "LLP", "INC", "CORP",
    "CORPORATION", "CO", "LTD", "MD", "DO"),
  expand_ampersand = TRUE
)
}
\arguments{
\item{x}{A character vector (or coerced to one) of organization names. \code{NA}
becomes \code{""}.}

\item{drop_suffixes}{Character vector of whole-word tokens to remove (legal
entity types and credential suffixes). Matched case-insensitively as whole
words after upper-casing. Defaults to the common US set. Pass \code{character(0)}
to keep them.}

\item{expand_ampersand}{If \code{TRUE} (default) replace \code{&} with \code{AND}.}
}
\value{
A character vector the length of \code{x} of normalized names.
}
\description{
Practice and organization names arrive spelled a dozen ways -- mixed case,
curly apostrophes, \code{&} vs \code{and}, trailing \code{LLC}/\code{PC}/\code{PA}, stray punctuation
-- which defeats exact joins when you try to match a caller cohort against an
external roster (CMS, NPPES, an ownership reference, a prior wave). This
collapses those variants to a single canonical form so equal organizations
compare equal: it upper-cases, replaces \code{&} with \code{AND}, strips punctuation to
single spaces, drops common legal-entity suffixes, and squishes whitespace.
}
\details{
It is deliberately conservative -- it does not stem words or fuzzy-match, so
two genuinely different practices stay different. Use it to build a join key,
then match on that key (optionally with your own alias/regex table on top).
}
\examples{
mysterycall_normalize_org_name(c(
  "Women's Health Specialists, LLC",
  "Womens Health Specialists PC",
  "Rocky Mountain OB & GYN, P.A."
))
# -> "WOMENS HEALTH SPECIALISTS", "WOMENS HEALTH SPECIALISTS",
#    "ROCKY MOUNTAIN OB AND GYN"
}
\seealso{
\code{\link[=mysterycall_normalize_address_df]{mysterycall_normalize_address_df()}}, \code{\link[=mysterycall_parse_physician_name]{mysterycall_parse_physician_name()}}
}
