% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical.R
\name{mysterycall_cmh_test}
\alias{mysterycall_cmh_test}
\title{Cochran-Mantel-Haenszel Test for a Matched / Stratified Design}
\usage{
mysterycall_cmh_test(
  data,
  outcome_var,
  group_var,
  strata_var,
  conf_level = 0.95,
  correct = TRUE
)
}
\arguments{
\item{data}{A data frame in long form (one row per call).}

\item{outcome_var, group_var, strata_var}{Character scalars. The outcome, the
grouping (exposure) variable, and the matching stratum (e.g. the clinic id).}

\item{conf_level}{Numeric. Confidence level for the common odds ratio.
Default \code{0.95}.}

\item{correct}{Logical. Continuity correction for the 2x2xK case. Default
\code{TRUE}.}
}
\value{
A \code{mysterycall_cmh_test} object: a list with \code{method}, \code{statistic},
\code{df}, \code{p_value}, \code{estimate} (common OR or \code{NA}), \code{conf_int}, \code{n},
\code{n_strata}, and \code{table}.
}
\description{
Tests the association between a categorical outcome and a group while
conditioning on a matching stratum -- the correct engine for within-unit
audit designs where the same clinic/pharmacy is called under several
personas (Wilkinson 2018). Wraps \code{\link[stats:mantelhaen.test]{stats::mantelhaen.test()}}; for a binary
outcome and binary group it also returns the common odds ratio and its
confidence interval.
}
\examples{
set.seed(1)
clinic <- rep(paste0("c", 1:20), each = 2)
persona <- rep(c("adult", "teen"), times = 20)
offered <- rbinom(40, 1, ifelse(persona == "teen", 0.55, 0.8))
df <- data.frame(clinic, persona, offered = ifelse(offered == 1, "yes", "no"))
mysterycall_cmh_test(df, "offered", "persona", "clinic")
}
\seealso{
Other categorical:
\code{\link[=mysterycall_compare_ranks]{mysterycall_compare_ranks()}},
\code{\link[=mysterycall_prevalence_ci]{mysterycall_prevalence_ci()}},
\code{\link[=mysterycall_test_categorical]{mysterycall_test_categorical()}}
}
\concept{categorical}
