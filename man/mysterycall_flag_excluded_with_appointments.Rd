% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_excluded_with_appointments.R
\name{mysterycall_flag_excluded_with_appointments}
\alias{mysterycall_flag_excluded_with_appointments}
\title{Flag Excluded Records That Still Have a Recorded Wait Time}
\usage{
mysterycall_flag_excluded_with_appointments(
  data,
  days_col = "business_days_until_appointment",
  exclusion_col = "reason_for_exclusions",
  contact_value = "Able to contact",
  select_cols = c("physician_information", "id_number", "notes", "reason_for_exclusions",
    "business_days_until_appointment"),
  output_dir = NULL,
  filename = "excluded_with_appointments.csv"
)
}
\arguments{
\item{data}{A data frame of mystery-caller records.}

\item{days_col}{Character scalar. Name of the wait-time column.
Default \code{"business_days_until_appointment"}.}

\item{exclusion_col}{Character scalar. Name of the exclusion-reason column.
Default \code{"reason_for_exclusions"}.}

\item{contact_value}{Character scalar. The value in \code{exclusion_col} that
means the call succeeded. Default \code{"Able to contact"}.}

\item{select_cols}{Character vector of columns to return. Columns not
present in \code{data} are silently dropped.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for CSV output.
\code{NULL} writes to a session temp directory via \code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}.
Pass \code{NA} to skip writing.}

\item{filename}{Character scalar. Output CSV file name.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} of flagged rows, sorted descending by
\code{id_number} (when present). Returns a zero-row tibble (invisibly) when
no records are flagged.
}
\description{
Finds records where \code{business_days_until_appointment >= 0} (a recorded wait,
including a same-day appointment at 0) but \code{reason_for_exclusions} is not
\code{contact_value}. These are logical contradictions: a call that failed contact
should not have a recorded appointment wait. A not-obtained appointment is
\code{NA}, so \code{0} is a real same-day appointment and is flagged.
}
\examples{
df <- data.frame(
  physician_information           = c("Dr A", "Dr B", "Dr C"),
  id_number                       = c("001", "002", "003"),
  notes                           = c(NA, NA, NA),
  reason_for_exclusions           = c("Not available", "Able to contact", "Not available"),
  business_days_until_appointment = c(5L, 3L, 0L),
  stringsAsFactors = FALSE
)
# Dr A (days=5) and Dr C (days=0, same-day) are flagged: excluded AND days >= 0
mysterycall_flag_excluded_with_appointments(df, output_dir = NA)
}
\seealso{
\code{\link[=mysterycall_flag_exclusion_discrepancy]{mysterycall_flag_exclusion_discrepancy()}}, which shares this
\verb{>= 0} boundary; \code{\link[=mysterycall_flag_included_na_appointments]{mysterycall_flag_included_na_appointments()}} for the
complementary check.

Other quality control:
\code{\link[=mysterycall_clean_medicaid_col]{mysterycall_clean_medicaid_col()}},
\code{\link[=mysterycall_dedup_by_insurance]{mysterycall_dedup_by_insurance()}},
\code{\link[=mysterycall_flag_exclusion_discrepancy]{mysterycall_flag_exclusion_discrepancy()}},
\code{\link[=mysterycall_flag_included_na_appointments]{mysterycall_flag_included_na_appointments()}},
\code{\link[=mysterycall_flag_repeat_physicians]{mysterycall_flag_repeat_physicians()}},
\code{\link[=mysterycall_guard_contaminated_wait]{mysterycall_guard_contaminated_wait()}}
}
\concept{quality control}
