% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/concordance.R
\name{mysterycall_concordance_kappa}
\alias{mysterycall_concordance_kappa}
\title{Per-Item Inter-Rater Agreement for a Concordance Rubric}
\usage{
mysterycall_concordance_kappa(rater1, rater2, rubric, call_id_col = NULL)
}
\arguments{
\item{rater1, rater2}{Data frames of identical shape, one row per call in the
same order (or aligned by \code{call_id_col}), each holding the rubric item
columns as coded by one rater.}

\item{rubric}{A \code{mysterycall_rubric}. Only its \code{item} ids are used.}

\item{call_id_col}{Character scalar or \code{NULL}. If supplied, the two frames
are aligned on this key before comparison; otherwise row order is assumed.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{item}, \code{kappa}, \code{pct_agree}, \code{n}.
}
\description{
Computes Cohen's kappa and percent agreement for each rubric item across two
independent coders/callers of the \emph{same} calls -- the reliability metric that
dual-coder counseling audits report (and that several published studies
collected the raw material for but never computed).
}
\examples{
r1 <- data.frame(asked_age = c(TRUE, TRUE, FALSE, TRUE))
r2 <- data.frame(asked_age = c(TRUE, FALSE, FALSE, TRUE))
rub <- mysterycall_concordance_rubric("asked_age", type = "expected_present")
mysterycall_concordance_kappa(r1, r2, rub)
}
\seealso{
Other concordance:
\code{\link[=mysterycall_concordance_rubric]{mysterycall_concordance_rubric()}},
\code{\link[=mysterycall_concordance_sentence]{mysterycall_concordance_sentence()}},
\code{\link[=mysterycall_score_concordance]{mysterycall_score_concordance()}}
}
\concept{concordance}
