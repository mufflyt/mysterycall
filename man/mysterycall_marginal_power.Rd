% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal_power.R
\name{mysterycall_marginal_power}
\alias{mysterycall_marginal_power}
\title{Monte Carlo power for a population-marginal, post-stratification-weighted
effect in a paired-call design}
\usage{
mysterycall_marginal_power(
  n_subject,
  cell_means,
  sigma_subject,
  phi,
  stratum_sampling,
  pop_stratum,
  condition_levels = c("A", "B"),
  family = c("negbin", "poisson", "auto"),
  disp_threshold = mysterycall_overdispersion_threshold(),
  alpha = 0.05,
  n_sim = 200,
  seed = NULL
)
}
\arguments{
\item{n_subject}{Number of subjects (each contributes two calls). May be a
vector to trace a power curve.}

\item{cell_means}{Numeric length-4 vector of the outcome mean in each
condition x stratum cell, in the order
\code{c(cond1_stratum0, cond2_stratum0, cond1_stratum1, cond2_stratum1)}
(all \verb{> 0}; counts).}

\item{sigma_subject}{Standard deviation of the subject random intercept on the
log scale.}

\item{phi}{Negative-binomial dispersion (\code{size}).}

\item{stratum_sampling}{Probability a subject is in stratum 1 \emph{in the sample}.}

\item{pop_stratum}{Target population probability of stratum 1 (drives the
post-stratification weights).}

\item{condition_levels}{Length-2 labels for the within-subject condition.
Default \code{c("A", "B")}.}

\item{family}{Working family for the fit: \code{"negbin"} (default), \code{"poisson"},
or \code{"auto"} (Poisson unless the Pearson dispersion exceeds
\code{disp_threshold}).}

\item{disp_threshold}{Dispersion ratio above which \code{"auto"} switches to NB.
Default \code{1.5}.}

\item{alpha}{Two-sided significance level. Default \code{0.05}.}

\item{n_sim}{Monte Carlo replicates per sample size. Default \code{200}.}

\item{seed}{Optional integer seed.}
}
\value{
An object of class \code{"mysterycall_marginal_power"}: a list with \code{table}
(a tibble: \code{n_subject}, \code{n_calls}, \code{pow_cond_interaction},
\code{pow_marg_unweighted}, \code{pow_marg_popweighted}, mean marginal estimates,
\code{convergence_rate}, \code{family_summary}, \code{n_sim}) and \code{truth}
(the true population-marginal condition effect in outcome units).
\code{\link[=as.data.frame]{as.data.frame()}} returns the table. Requires the \pkg{glmmTMB} and
\pkg{marginaleffects} packages.
}
\description{
Audit studies frequently oversample a rare stratum (rural practices,
minority-serving providers, a specific payer market) to get enough calls
there, yet want to report an effect that generalizes to the \emph{population} mix.
The conditional model coefficient does not answer that question; the
population-marginal effect, obtained by post-stratification weighting, does.
This simulates a paired design -- each subject (e.g. a provider) is called
under two \code{conditions} (e.g. two insurance types), subjects belong to one of
two strata sampled at \code{stratum_sampling} but reweighted to \code{pop_stratum} --
fits a negative-binomial (or Poisson) GLMM with a subject random intercept,
and reports power for three estimands: the conditional condition x stratum
interaction, the unweighted marginal condition effect, and the
population-weighted marginal condition effect.
}
\examples{
\donttest{
mysterycall_marginal_power(
  n_subject = c(150, 300),
  cell_means = c(14, 18, 17, 24),   # BCBS/Medicaid x urban/rural wait days
  sigma_subject = 0.4, phi = 1.7,
  stratum_sampling = 0.5, pop_stratum = 0.15,
  n_sim = 40, seed = 1
)
}
}
