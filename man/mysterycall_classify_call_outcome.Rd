% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/call_outcomes.R
\name{mysterycall_classify_call_outcome}
\alias{mysterycall_classify_call_outcome}
\title{Map Raw Call Dispositions to a Standard Outcome Taxonomy}
\usage{
mysterycall_classify_call_outcome(x, mapping = NULL, other = "other")
}
\arguments{
\item{x}{Character vector of raw disposition text (one per call).}

\item{mapping}{Named list or \code{NULL}. Names are canonical levels; values are
case-insensitive regular expressions matched against \code{x}. The first
matching pattern (in list order) wins. \code{NULL} uses a built-in default map
covering the common access-audit states.}

\item{other}{Character scalar. Level assigned when nothing matches. Default
\code{"other"}.}
}
\value{
A factor with levels in \code{mapping} order followed by \code{other}.
}
\description{
Collapses free-text or heterogeneous call-disposition labels into a small,
analysable set of canonical states that recur across access audits -- so an
appointment blocked pending insurance verification, a referral, or records is
distinguished from a plain refusal, and phone gatekeeping and unreachable
numbers become their own funnel stages (Pollack 2016, Sharma 2025, Hodson
2025).
}
\examples{
raw <- c("Appointment offered in 12 days", "Needs a referral first",
         "Won't give info over the phone", "No answer after 3 tries",
         "Must verify insurance ID number")
mysterycall_classify_call_outcome(raw)
}
\seealso{
Other call-outcomes:
\code{\link[=mysterycall_multiresponse_tabulate]{mysterycall_multiresponse_tabulate()}},
\code{\link[=mysterycall_ordinal_model]{mysterycall_ordinal_model()}},
\code{\link[=mysterycall_outcome_gradient]{mysterycall_outcome_gradient()}}
}
\concept{call-outcomes}
