% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_duration.R
\name{mysterycall_parse_duration}
\alias{mysterycall_parse_duration}
\title{Parse messy free-text call durations to a numeric unit}
\usage{
mysterycall_parse_duration(
  x,
  output_unit = c("seconds", "minutes"),
  default_unit = c("minutes", "seconds")
)
}
\arguments{
\item{x}{Character (or numeric) vector of durations.}

\item{output_unit}{One of \code{"seconds"} (default) or \code{"minutes"}; the unit of
the returned numbers.}

\item{default_unit}{One of \code{"minutes"} (default) or \code{"seconds"}; how to
interpret an entry that is a bare number with no \code{min}/\code{sec} token (e.g.
\code{"3"}). Set this to match the column's convention.}
}
\value{
A numeric vector the length of \code{x}, in \code{output_unit}. Entries that are
missing, empty, or contain no recoverable number return \code{NA}.
}
\description{
Mystery-caller logs capture hold time and call length as free text a caller
typed in a hurry -- \code{"1.5min"}, \code{"1min 45 sec"}, \code{"30sec"},
\code{"2min 30 sec"}, a bare \code{"90"}, an \code{"O"} meant as zero. Studies routinely
hand-build a lookup table of every variant they happen to see, which is
brittle and does not transfer to the next audit. This parses the common forms
directly: it sums any minute tokens (\verb{N min}, \verb{N m}) and second tokens
(\verb{N sec}, \verb{N s}) it finds, and falls back to treating a bare number as
\code{default_unit}.
}
\details{
Matching is case-insensitive. \code{"o"}/\code{"O"} alone (a common stand-in
for zero) parses to \code{0}. Decimals are supported in either token
(\code{"2.75min"} -> 165 s). When both minute and second tokens are present they
are added (\code{"1min 45 sec"} -> 105 s).
}
\examples{
mysterycall_parse_duration(
  c("1.5min", "1min 45 sec", "30sec", "O", "2.75min, 30sec", "3"),
  output_unit = "seconds"
)
# bare "3" as minutes -> 180 s; "O" -> 0
}
\seealso{
\code{\link[=mysterycall_categorize_wait]{mysterycall_categorize_wait()}}

Other data integrity: 
\code{\link{.mc_age_key}()},
\code{\link{.mc_data}()},
\code{\link{.mc_geocode_point}()},
\code{\link{.mc_healthgrades_ages}()},
\code{\link{.mc_state_to_abbr}()},
\code{\link{mysterycall_assign_area_covariates}()},
\code{\link{mysterycall_categorize_wait}()},
\code{\link{mysterycall_clean_zip}()},
\code{\link{mysterycall_flag_near_duplicate_keys}()},
\code{\link{mysterycall_link_physicians}()},
\code{\link{mysterycall_lookup_age}()}
}
\concept{data integrity}
