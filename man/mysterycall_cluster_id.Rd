% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_id.R
\name{mysterycall_cluster_id}
\alias{mysterycall_cluster_id}
\title{Build a random-intercept clustering key by coalescing columns}
\usage{
mysterycall_cluster_id(
  data,
  cols,
  solo_prefix = "solo_",
  empty_as_missing = TRUE
)
}
\arguments{
\item{data}{A data frame.}

\item{cols}{Character vector of candidate key columns, most specific first
(e.g. \code{c("cbsa_code", "county_fips")}). Each must exist in \code{data}.}

\item{solo_prefix}{Prefix for the singleton keys given to rows that no column
resolves. Default \code{"solo_"}. Set to \code{NA} to instead leave those rows \code{NA}
(and emit a warning naming how many) if you would rather drop or inspect
them.}

\item{empty_as_missing}{Logical; treat empty strings (and, after trimming,
whitespace-only strings) as missing. Default \code{TRUE}.}
}
\value{
A character vector of length \code{nrow(data)}: the clustering key.
}
\description{
The mixed-model fitters (\code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}},
\code{\link[=mysterycall_nb_model]{mysterycall_nb_model()}}, \code{\link[=mysterycall_lmm]{mysterycall_lmm()}}, ...) all require a
\code{random_intercept} column, but a geographic mystery-caller study rarely has a
single clean grouping key: a metro (CBSA) code exists for urban practices,
falls back to a county FIPS for the rest, and some rows have neither. Leaving
those rows blank or \code{NA} is a quiet footgun -- they collapse into one giant
pseudo-cluster and corrupt the estimated random-effect variance.
}
\details{
This helper coalesces an ordered list of candidate columns into one grouping
vector: for each row it takes the first non-missing, non-empty value across
\code{cols} (in order), and gives every row that is \emph{still} unresolved its own
singleton cluster (\verb{solo_<rownum>}) so it contributes as an independent group
rather than being merged.
}
\examples{
d <- data.frame(
  cbsa_code   = c("16980", "", NA, "31080"),
  county_fips = c("17031", "17097", "", "06037")
)
mysterycall_cluster_id(d, c("cbsa_code", "county_fips"))
# -> c("16980", "17097", "solo_3", "31080")
}
