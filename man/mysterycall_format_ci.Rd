% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_reporting.R
\name{mysterycall_format_ci}
\alias{mysterycall_format_ci}
\title{Format a confidence interval for reporting}
\usage{
mysterycall_format_ci(
  lower,
  upper,
  digits = 2L,
  sep = getOption("mysterycall.ci_sep", " to ")
)
}
\arguments{
\item{lower, upper}{Numeric vectors of interval endpoints, recycled against
each other. \code{NA} in either endpoint yields \code{NA} for that element.}

\item{digits}{Integer. Decimal places for both endpoints. Default \code{2}.}

\item{sep}{Character scalar placed between the endpoints. Defaults to
\code{getOption("mysterycall.ci_sep", " to ")}, so a whole document can be
switched with one option.}
}
\value{
A character vector the length of the recycled inputs.
}
\description{
Joins the endpoints of an interval into the string a manuscript prints.
SAMPL asks that the endpoints be separated with \code{"to"} rather than a hyphen
or a dash, so that a negative lower bound cannot be misread as a minus sign:
\code{-0.45--0.12} is ambiguous, \verb{-0.45 to -0.12} is not.
}
\details{
The separator is an argument rather than a constant because house styles
differ. \code{gtsummary}'s JAMA and Lancet journal themes make the same choice
this default does, exposing it as a \code{ci.sep} theme setting; a journal that
insists on a dash can be served by passing \code{sep} or by setting
\code{options(mysterycall.ci_sep = )} once, rather than by editing every
formatter in the package.
}
\examples{
mysterycall_format_ci(1.05, 1.57)
mysterycall_format_ci(-0.45, -0.12)
mysterycall_format_ci(c(1.05, NA), c(1.57, 2.0))
mysterycall_format_ci(0.4, 0.9, digits = 3, sep = " - ")
}
\references{
Lang TA, Altman DG. Basic statistical reporting for articles published in
biomedical journals: the "Statistical Analyses and Methods in the Published
Literature" or the SAMPL Guidelines. \emph{International Journal of Nursing
Studies}. 2015;52(1):5-9. \doi{10.1016/j.ijnurstu.2014.09.006}

The \code{sep} argument follows the pattern set by \code{gtsummary}'s journal themes
(\code{gtsummary::theme_gtsummary_journal("jama")}, MIT licensed), which likewise
separate interval endpoints with "to" and expose the separator as a setting.
}
\seealso{
\code{\link[=mysterycall_format_p]{mysterycall_format_p()}} for the matching p-value formatter, and
\code{\link[=mysterycall_sampl_checklist]{mysterycall_sampl_checklist()}} for the reporting items these serve.

Other table helpers:
\code{\link[=mysterycall_disparities_table]{mysterycall_disparities_table()}},
\code{\link[=mysterycall_format_p]{mysterycall_format_p()}},
\code{\link[=mysterycall_format_pct]{mysterycall_format_pct()}},
\code{\link[=mysterycall_max_table]{mysterycall_max_table()}},
\code{\link[=mysterycall_min_table]{mysterycall_min_table()}},
\code{\link[=mysterycall_model_table]{mysterycall_model_table()}},
\code{\link[=mysterycall_table_percentages]{mysterycall_table_percentages()}},
\code{\link[=mysterycall_table_proportion]{mysterycall_table_proportion()}},
\code{\link[=print.mysterycall_disparities_table]{print.mysterycall_disparities_table()}}
}
\concept{table helpers}
