% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_consistency.R
\name{mysterycall_consistency_rule}
\alias{mysterycall_consistency_rule}
\title{Define one cross-field consistency rule}
\usage{
mysterycall_consistency_rule(
  flag,
  description,
  action,
  predicate,
  priority = "MEDIUM"
)
}
\arguments{
\item{flag}{Short machine tag for the rule (e.g. \code{"ANSWER_CONFLICT"}).}

\item{description}{Human-readable statement of the contradiction.}

\item{action}{What the study team should do about it.}

\item{predicate}{A \verb{function(data)} returning a logical vector with one entry
per row of \code{data}; \code{TRUE} marks a row that \emph{violates} the rule. \code{NA} is
treated as "not flagged". A predicate that references a column absent from
the data should return \code{FALSE} for every row (see
\code{\link[=mysterycall_default_consistency_rules]{mysterycall_default_consistency_rules()}} for the pattern) so a rule set can
be applied to logs that lack some fields.}

\item{priority}{One of \code{"CRITICAL"}, \code{"HIGH"}, \code{"MEDIUM"}, \code{"LOW"}
(default \code{"MEDIUM"}); controls sort order in the report.}
}
\value{
A list with class \code{"mysterycall_consistency_rule"}.
}
\description{
A rule pairs a predicate with the metadata needed to triage the rows it
flags. It is the unit consumed by \code{\link[=mysterycall_check_consistency]{mysterycall_check_consistency()}}.
}
\examples{
mysterycall_consistency_rule(
  flag = "WAIT_NO_OFFER",
  description = "A wait time is recorded but no appointment was offered",
  action = "Clear wait_days or correct appointment_offered",
  predicate = function(d) !is.na(d$wait_days) & d$offered == FALSE,
  priority = "HIGH"
)
}
\seealso{
\code{\link[=mysterycall_check_consistency]{mysterycall_check_consistency()}},
\code{\link[=mysterycall_default_consistency_rules]{mysterycall_default_consistency_rules()}}
}
