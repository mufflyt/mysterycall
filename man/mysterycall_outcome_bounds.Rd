% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outcome_bounds.R
\name{mysterycall_outcome_bounds}
\alias{mysterycall_outcome_bounds}
\title{Worst-case / best-case bounds on a proportion under non-response}
\usage{
mysterycall_outcome_bounds(
  data,
  outcome_col,
  positive_values = TRUE,
  observed = NULL,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame spanning the full sampling universe (observed \emph{and}
unobserved calls).}

\item{outcome_col}{Name of the binary outcome column.}

\item{positive_values}{Value(s) of \code{outcome_col} counted as a success.
Default \code{TRUE} (also matched against \code{"TRUE"}/\code{1} via \code{\%in\%}).}

\item{observed}{Optional: which rows have a trustworthy observed outcome.
Either a column name (interpreted so that non-missing / truthy = observed)
or a logical vector the length of \code{nrow(data)}. If \code{NULL} (default), a row
is observed when \code{outcome_col} is not \code{NA}.}

\item{conf_level}{Confidence level for the Wilson interval on the
complete-case rate. Default \code{0.95}.}
}
\value{
An object of class \code{"mysterycall_outcome_bounds"}: a list with
\code{n_universe}, \code{n_observed}, \code{n_missing}, \code{n_positive}, \code{complete_case_rate}
(+ Wilson \code{cc_ci_lower}/\code{cc_ci_upper}), \code{lower_bound} (all missing are
failures), \code{upper_bound} (all missing are successes), and \code{bound_width}
(= \code{n_missing / n_universe}, the ignorance interval). All rates are
proportions in \verb{[0, 1]}. \code{\link[=as.data.frame]{as.data.frame()}} returns a one-row tibble.
}
\description{
When some calls are incomplete, the true outcome rate over the full sampling
frame is not point-identified: the missing calls could all have been
successes or all failures. This computes the Manski-style bounds -- the
complete-case rate, plus the assumption-free interval you get by assigning
every unobserved call first to failure (worst case) then to success (best
case) -- over the full universe. Reviewers of audit studies routinely ask for
these bounds so a headline offer/acceptance rate is not quietly conditioned
on the calls that happened to complete.
}
\examples{
d <- data.frame(
  offered  = c(TRUE, FALSE, TRUE, NA, NA, TRUE, FALSE, NA),
  complete = c("Y", "Y", "Y", "N", "N", "Y", "Y", "N")
)
mysterycall_outcome_bounds(d, "offered")
}
