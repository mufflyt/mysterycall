% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subspecialist_infographic.R
\name{mysterycall_subspecialist_infographic}
\alias{mysterycall_subspecialist_infographic}
\title{Workforce-density infographic for OB-GYN subspecialists}
\usage{
mysterycall_subspecialist_infographic(
  subspecialty = c("Gynecologic Oncology", "Maternal-Fetal Medicine",
    "Reproductive Endocrinology & Infertility", "Urogynecology"),
  start,
  end,
  abbrev = NULL,
  data = NULL,
  subspecialty_col = "subspecialty",
  start_col = "start",
  end_col = "end",
  abbrev_col = "abbrev",
  year_start = 2018L,
  year_end = 2023L,
  title = "OB-GYN Subspecialists per 100,000 Women",
  palette = c("#1F4E66", "#2F6F3E"),
  header_fill = "#2E6E8E",
  increase_color = "#2F6F3E",
  decrease_color = "#B22222",
  color_pct_by = c("direction", "panel"),
  digits = 2L,
  output_path = NULL,
  width = 10,
  height = 4.8,
  dpi = 300
)
}
\arguments{
\item{subspecialty}{Character vector of panel labels. Defaults to the four
ABOG OB-GYN subspecialties (Gynecologic Oncology, Maternal-Fetal Medicine,
Reproductive Endocrinology & Infertility, Urogynecology).}

\item{start, end}{Numeric vectors of density values (subspecialists per
100,000 women) at \code{year_start} and \code{year_end}. Same length as
\code{subspecialty}. Required unless supplied via \code{data}.}

\item{abbrev}{Character vector of short badge labels (one per panel). When
\code{NULL} (default) a sensible abbreviation is derived from \code{subspecialty}
(e.g. "MFM", "REI"); a newline in a badge splits it across two lines.}

\item{data}{Optional data frame carrying the values instead of the vectors.
Columns are named by \code{subspecialty_col}, \code{start_col}, \code{end_col}, and
optionally \code{abbrev_col}. When supplied it overrides the vector arguments.}

\item{subspecialty_col, start_col, end_col, abbrev_col}{Column names read from
\code{data}. Defaults \code{"subspecialty"}, \code{"start"}, \code{"end"}, \code{"abbrev"}.}

\item{year_start, year_end}{Integer years shown under each value and appended
to the header title. Defaults \code{2018} and \code{2023}.}

\item{title}{Character. Header title; the year range is appended
automatically (e.g. \verb{...: 2018-2023}).}

\item{palette}{Character vector of panel accent colours, recycled across
panels (defaults alternate a blue and a green, matching the source figure).}

\item{header_fill}{Character. Fill colour of the header bar.}

\item{increase_color, decrease_color}{Character. Colours for a rising vs.
falling density (used for the percent change when
\code{color_pct_by = "direction"}, and always for the value arrow).}

\item{color_pct_by}{One of \code{"direction"} (default; green up / red down) or
\code{"panel"} (colour the percent change with the panel accent, like the
source figure).}

\item{digits}{Integer. Decimal places for the density values. Default \code{2}.}

\item{output_path}{Character or \code{NULL}. File path to save via
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}} (\code{.png}, \code{.pdf}, \code{.tiff}, \code{.svg}). \code{NULL} (default)
writes nothing.}

\item{width, height}{Numeric. Saved size in inches. Defaults \code{10} x \code{4.8}.}

\item{dpi}{Integer. Resolution for raster output. Default \code{300}.}
}
\value{
A ggplot2 object (invisibly). When \code{output_path} is set the file is
written and the path messaged.
}
\description{
Reproduces the "clinicians per unit" infographic style (a titled header bar
over one accent-coloured panel per subspecialty) as a single ggplot2 object,
repurposed to show \strong{subspecialists per 100,000 women} at two time points.
Each panel shows the subspecialty, the percent change, and the
\code{year_start} -> \code{year_end} values with a direction arrow. Percent change is
computed from the supplied values, never typed in, so the figure cannot
disagree with its own numbers.
}
\details{
You supply the density values; nothing is imputed. Provide them either as the
\code{start} / \code{end} vectors (paste-friendly) or through a \code{data} frame.
}
\examples{
# Paste your own per-100,000-women densities for the four ABOG subspecialties:
mysterycall_subspecialist_infographic(
  start = c(1.20, 0.95, 0.80, 0.40),   # 2018
  end   = c(1.35, 1.02, 0.88, 0.61)    # 2023
)

# Or from a data frame, with custom panels and a saved file:
df <- data.frame(
  subspecialty = c("Gynecologic Oncology", "Maternal-Fetal Medicine"),
  start        = c(1.20, 0.95),
  end          = c(1.35, 1.02)
)
\dontrun{
mysterycall_subspecialist_infographic(data = df,
                                      output_path = "subspecialists_per_100k.png")
}
}
\seealso{
\code{\link[=mysterycall_flow_diagram]{mysterycall_flow_diagram()}}, \code{\link[=mysterycall_strobe_flow]{mysterycall_strobe_flow()}}

Other manuscript:
\code{\link{mysterycall_flow_diagram}()},
\code{\link{mysterycall_strobe_flow}()}
}
\concept{manuscript}
