% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subspecialist_trend.R
\name{mysterycall_subspecialist_trend}
\alias{mysterycall_subspecialist_trend}
\title{Trend of OB-GYN subspecialist density per 100,000 women over time}
\usage{
mysterycall_subspecialist_trend(
  counts,
  population,
  subspecialty_col = "subspecialty",
  year_col = "year",
  count_col = "count",
  pop_year_col = "year",
  pop_col = "population",
  per = 1e5,
  title = "OB-GYN Subspecialists per 100,000 Women",
  show_year_range = TRUE,
  y_lab = "Subspecialists per 100,000 women",
  x_lab = NULL,
  palette = NULL,
  label_ends = TRUE,
  point_size = 1.9,
  line_width = 1,
  numerator_source = NULL,
  denominator_source = paste0("U.S. Census Bureau, American Community Survey ",
    "1-year estimates, table B01001 (B01001_026E, ", "total female population)"),
  denominator_vintage = NULL,
  accessed = NULL,
  notes = NULL,
  caption = NULL,
  write_provenance = TRUE,
  output_path = NULL,
  width = 9,
  height = 5.5,
  dpi = 300
)
}
\arguments{
\item{counts}{Subspecialist counts (numerator). One of:
\itemize{
\item a \strong{long} data frame with the columns named by \code{subspecialty_col},
\code{year_col}, and \code{count_col};
\item a \strong{wide} data frame with a \code{subspecialty_col} column plus one
numeric column per year (headers like \code{2013} or \code{X2013});
\item a \strong{matrix} with subspecialty row names and year column names.
}}

\item{population}{Total female population (denominator). One of: a year-named
numeric vector (names are years, e.g. \code{setNames(pop, 2013:2023)}), an
unnamed numeric vector ordered by year, or a data frame with the columns
\code{pop_year_col} and \code{pop_col}. Must cover every year present in \code{counts}.}

\item{subspecialty_col, year_col, count_col}{Column names for the long/wide
\code{counts} form. Defaults \code{"subspecialty"}, \code{"year"}, \code{"count"}.}

\item{pop_year_col, pop_col}{Column names when \code{population} is a data frame.
Defaults \code{"year"}, \code{"population"}.}

\item{per}{Numeric denominator scale. Default \code{1e5} (per 100,000 women).}

\item{title}{Character. Plot title. The observed year range is appended when
\code{show_year_range = TRUE}.}

\item{show_year_range}{Logical. Append \verb{, <min>-<max>} to \code{title}.
Default \code{TRUE}.}

\item{y_lab, x_lab}{Axis labels. \code{x_lab} defaults to \code{NULL} (no label, since
the axis shows years).}

\item{palette}{Optional character vector of colours, one per subspecialty
(passed to \code{\link[ggplot2:scale_manual]{ggplot2::scale_colour_manual()}}). \code{NULL} uses the ggplot2
default.}

\item{label_ends}{Logical. Label each line at its final year and hide the
legend (\code{TRUE}, default) instead of showing a legend.}

\item{point_size, line_width}{Numeric point size and line width.}

\item{numerator_source}{Character or \code{NULL}. Citation for the subspecialist
counts (e.g. \code{"ABOG certified-diplomate counts, 2013-2023"}). Recorded in
the provenance and, if set, shown in the figure caption.}

\item{denominator_source}{Character. Citation for the female-population
denominator. Defaults to the U.S. Census ACS 1-year table B01001
(\code{B01001_026E}). Set to \code{NULL} to omit.}

\item{denominator_vintage}{Character or \code{NULL}. Extra denominator detail
recorded in the provenance (e.g. \code{"ACS 1-year 2013-2023; 2020 from ACS 5-year"}).}

\item{accessed}{Date or character or \code{NULL}. When the source data were
pulled (recorded in the provenance and caption).}

\item{notes}{Character or \code{NULL}. Free-text note recorded in the provenance.}

\item{caption}{Character, \code{NULL}, or \code{NA}. Figure caption. \code{NULL} (default)
auto-builds a source line from the numerator/denominator provenance; \code{NA}
(or \code{""}) draws no caption; a string is used verbatim.}

\item{write_provenance}{Logical. When \code{output_path} is set, also write a
\verb{<output>.provenance.txt} sidecar with the full provenance record and the
per-point density table. Default \code{TRUE}.}

\item{output_path}{Character or \code{NULL}. File path to save via
\code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}. \code{NULL} (default) writes nothing.}

\item{width, height}{Numeric. Saved size in inches. Defaults \code{9} x \code{5.5}.}

\item{dpi}{Integer. Resolution for raster output. Default \code{300}.}
}
\value{
A ggplot2 object (invisibly). Its \code{$data} holds the computed density
table (\code{subspecialty}, \code{year}, \code{count}, \code{population}, \code{density}), and
\code{attr(p, "provenance")} holds a \code{mysterycall_provenance} record (metric,
computation, numerator/denominator sources, package version, access date,
and creation timestamp). When saved, a \verb{<output>.provenance.txt} sidecar is
written alongside the image (unless \code{write_provenance = FALSE}).
}
\description{
Computes subspecialists per 100,000 women for each subspecialty and year from
\strong{raw counts} (numerator) divided by the \strong{total female population}
(denominator), then draws a multi-year line/point trend -- one line per
subspecialty -- with optional direct end-of-line labels. The density is
derived from the inputs (\code{count / population * per}), never typed, so the
figure cannot disagree with its own numbers.
}
\details{
\strong{You supply both the counts and the denominators.} No population figures are
bundled with the package (they go stale and would need to be a cited vintage).
To fetch the total-female-population denominators from the U.S. Census ACS
1-year tables (variable \code{B01001_026E}, "Estimate!!Total!!Female"), one call
per year:

\preformatted{
# requires an internet connection and (optionally) a Census API key
fem_pop <- vapply(2013:2023, function(y) {
  u <- sprintf(
    "https://api.census.gov/data/\%d/acs/acs1?get=B01001_026E&for=us:1", y)
  as.numeric(jsonlite::fromJSON(u)[2, 1])
}, numeric(1))
names(fem_pop) <- 2013:2023
# NB: the ACS 1-year 2020 table was not released; use the 5-year 2020
#     table or a Population Estimates (PEP) value for that year.
}
}
\examples{
# Long-format counts + a year-named female-population denominator.
counts <- data.frame(
  subspecialty = rep(c("Gynecologic Oncology", "Urogynecology"), each = 3),
  year         = rep(c(2013, 2018, 2023), times = 2),
  count        = c(1900, 2200, 2600, 900, 1400, 2100)
)
fem_pop <- c(`2013` = 160477237, `2018` = 165000000, `2023` = 168000000)
mysterycall_subspecialist_trend(counts, population = fem_pop)
}
\seealso{
\code{\link[=mysterycall_subspecialist_infographic]{mysterycall_subspecialist_infographic()}} for the two-point
(start -> end) infographic version.

Other manuscript:
\code{\link{mysterycall_flow_diagram}()},
\code{\link{mysterycall_strobe_flow}()},
\code{\link{mysterycall_subspecialist_infographic}()}
}
\concept{manuscript}
