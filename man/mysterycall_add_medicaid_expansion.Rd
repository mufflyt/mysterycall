% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_medicaid_expansion.R
\name{mysterycall_add_medicaid_expansion}
\alias{mysterycall_add_medicaid_expansion}
\title{Join ACA Medicaid-expansion status onto study data (optionally as of the call date)}
\usage{
mysterycall_add_medicaid_expansion(
  data,
  state_col = "state",
  date_col = NULL,
  prefix = ""
)
}
\arguments{
\item{data}{A data frame with one row per call/office.}

\item{state_col}{Character scalar. Name of the column in \code{data} holding the
state (full name or two-letter abbreviation). Default \code{"state"}.}

\item{date_col}{Character scalar or \code{NULL}. Name of a \code{Date} (or
date-coercible) column holding the call date. When supplied, the
as-of-date \code{expanded_at_date} flag is added. Default \code{NULL}.}

\item{prefix}{Character scalar prepended to the added column names to avoid
collisions. Default \code{""} (no prefix).}
}
\value{
\code{data} with these columns added (each optionally prefixed):
\describe{
\item{\code{expanded}}{Logical. Current ACA full-expansion status of the state.}
\item{\code{expansion_date}}{Date the expansion took effect (\code{NA} for
non-expansion states).}
\item{\code{expansion_status}}{Character \code{"Expanded"} / \code{"Not Expanded"}.}
\item{\code{expanded_at_date}}{Logical, \strong{only when \code{date_col} is supplied}.
\code{TRUE} iff the state had expanded \emph{and} \code{expansion_date <= date_col} for
that row. \code{NA} when the date is missing or the state did not match.}
}
Rows whose state does not match the crosswalk (e.g. territories, typos)
receive \code{NA} in every added column and trigger a warning.
}
\description{
Left-joins the packaged \link{medicaid_expansion} crosswalk onto a caller/office
data frame by state, adding each state's ACA Medicaid-expansion status. The
state column may hold either full state names (e.g. \code{"Texas"}) or two-letter
USPS abbreviations (e.g. \code{"TX"}); the format is auto-detected.
}
\details{
When a date column is supplied via \code{date_col}, the function additionally
computes a \strong{per-row, as-of-the-call-date} expansion flag
(\code{expanded_at_date}). This matters for states that adopted expansion \emph{during}
a study window: North Carolina implemented on 2023-12-01 and South Dakota on
2023-07-01, so calls placed to those states earlier in 2023 were made while
the state had \strong{not yet} expanded and are correctly classified as
non-expansion at the call date. The static \code{expanded} column (current status)
would misclassify them. This logic is ported from the \code{consolidation}
study's \verb{R/18_medicaid_expansion.R}.

US territories (Puerto Rico, Guam, U.S. Virgin Islands, American Samoa,
Northern Mariana Islands) sit outside the ACA state-expansion decision — they
receive capped Medicaid block grants rather than the state expansion choice —
so they never match the crosswalk and their added columns are \code{NA} (a warning
lists them).
}
\examples{
calls <- data.frame(
  office    = c("A", "B", "C", "D"),
  state     = c("TX", "North Carolina", "CA", "SD"),
  call_date = as.Date(c("2023-05-01", "2023-05-01",
                        "2023-05-01", "2023-09-01")),
  stringsAsFactors = FALSE
)

# Current (static) expansion status only
mysterycall_add_medicaid_expansion(calls)

# As-of-call-date status: NC on 2023-05-01 had NOT yet expanded (impl.
# 2023-12-01); SD on 2023-09-01 HAD (impl. 2023-07-01).
mysterycall_add_medicaid_expansion(calls, date_col = "call_date")
}
\seealso{
\link{medicaid_expansion} for the underlying dataset;
\code{\link[=mysterycall_assign_region]{mysterycall_assign_region()}} for ACOG-district groupings that pair well
with expansion status in subgroup analyses.

Other census: 
\code{\link{mysterycall_add_hhi}()},
\code{\link{mysterycall_get_acs_adults_18_90}()},
\code{\link{mysterycall_get_acs_female_insurance}()},
\code{\link{mysterycall_get_acs_women_18_90}()},
\code{\link{mysterycall_get_census_data}()},
\code{\link{mysterycall_get_county_provider_counts}()},
\code{\link{mysterycall_get_payer_mix}()},
\code{\link{mysterycall_plot_census_age}()},
\code{\link{mysterycall_read_kff_hhi}()},
\code{\link{mysterycall_summarize_census}()},
\code{\link{mysterycall_summarize_county_enrollment}()},
\code{\link{print.mysterycall_provider_counts}()}
}
\concept{census}
