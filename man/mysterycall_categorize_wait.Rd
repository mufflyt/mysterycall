% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorize_wait.R
\name{mysterycall_categorize_wait}
\alias{mysterycall_categorize_wait}
\title{Bin a wait-time-to-appointment into weekly categories and a threshold flag}
\usage{
mysterycall_categorize_wait(
  x,
  threshold_days = 14,
  bin_width = 7,
  max_bin = 28
)
}
\arguments{
\item{x}{Numeric vector of business days (or calendar days) until the
appointment.}

\item{threshold_days}{Numeric. The binary cutoff; \code{over_threshold} is \code{TRUE}
when \code{x > threshold_days}. Default \code{14} (a two-week wait).}

\item{bin_width}{Numeric. Width of each ordered bin in days. Default \code{7}
(weekly).}

\item{max_bin}{Numeric. The last finite bin edge; values above it fall in a
single top \code{"> max_bin"} bin. Default \code{28}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} the length of \code{x} with columns \code{days} (the
input), \code{bin} (an ordered factor of weekly categories), and
\code{over_threshold} (logical). \code{NA} days give \code{NA} in both derived columns.
}
\description{
Audit results are usually reported two ways: an ordered set of weekly bins
(how the wait distribution is shaped) and a single clinically-meaningful
binary threshold -- classically "more than a two-week wait", the headline
access outcome. This standardizes both from a numeric days-to-appointment
vector so the ">N-week" convention is defined once instead of re-derived with
a fresh \code{\link[=cut]{cut()}} in every analysis.
}
\examples{
mysterycall_categorize_wait(c(3, 10, 15, 30, NA))
}
\seealso{
\code{\link[=mysterycall_wait_time_summary]{mysterycall_wait_time_summary()}}, \code{\link[=mysterycall_business_days]{mysterycall_business_days()}}

Other data integrity: 
\code{\link{mysterycall_clean_zip}()},
\code{\link{mysterycall_flag_near_duplicate_keys}()},
\code{\link{mysterycall_link_physicians}()},
\code{\link{mysterycall_parse_duration}()}
}
\concept{data integrity}
