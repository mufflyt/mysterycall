% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hurdle_wait.R
\name{mysterycall_hurdle_wait}
\alias{mysterycall_hurdle_wait}
\title{Two-part hurdle model for a mystery-caller wait time}
\usage{
mysterycall_hurdle_wait(
  data,
  obtained_col,
  wait_col,
  predictors,
  random_intercept = NULL,
  count_family = c("nbinom2", "poisson"),
  truncate_zero = TRUE,
  conf_level = 0.95
)
}
\arguments{
\item{data}{A data frame, one row per call.}

\item{obtained_col}{Name of the binary "appointment obtained" column (the
hurdle outcome). Coerced with \verb{\%in\% TRUE}/\code{1}/\code{"TRUE"}/\code{"Yes"}.}

\item{wait_col}{Name of the wait-time count column (defined among obtained
appointments; \code{NA}/missing otherwise).}

\item{predictors}{Character vector of predictor columns, used in both parts.}

\item{random_intercept}{Optional name of a clustering column (e.g. practice)
for a random intercept in both parts. \code{NULL} fits fixed-effects-only.}

\item{count_family}{Working family for the count part: \code{"nbinom2"} (default)
or \code{"poisson"}.}

\item{truncate_zero}{If \code{TRUE} (default) model the wait with a \strong{zero-
truncated} count distribution fit on waits \verb{> 0} (the hurdle already
carries the not-obtained cases); if \code{FALSE} keep same-day (0-day) waits and
fit an untruncated count model.}

\item{conf_level}{Confidence level for the reported intervals. Default \code{0.95}.}
}
\value{
An object of class \code{"mysterycall_hurdle_wait"}: a list with \code{hurdle}
(a tibble of odds ratios for the obtainment part) and \code{count} (a tibble of
incidence-rate ratios for the wait part), each with \code{term}, \code{estimate},
\code{conf_low}, \code{conf_high}, \code{p_value}, plus \code{n_hurdle}, \code{n_count}, and the
fitted \code{hurdle_model} / \code{count_model}. \code{\link[=as.data.frame]{as.data.frame()}} returns the two
tables stacked with a \code{part} column. Requires the \pkg{glmmTMB} package.
}
\description{
Mystery-caller wait times have a two-part structure: an appointment is either
obtained or not (the "hurdle"), and \emph{conditional on} obtaining one, the wait is
a right-skewed count of days. This fits both parts and returns them together:
a binary hurdle model for whether an appointment was obtained, and a
(zero-truncated) negative-binomial model for the wait among obtained
appointments. Both parts accept a practice random intercept, so the paired /
clustered structure of an audit is respected -- an advantage over
\code{pscl::hurdle()}, which cannot cluster.
}
\details{
A hurdle model is the natural default when the wait is right-skewed: the
negative-binomial count part absorbs the skew and overdispersion that a
normal-outcome selection (Heckman) model cannot. It assumes the two parts are
separable given the covariates (selection on observables); address any
residual selection-on-unobservables concern with a sensitivity analysis --
\code{\link[=mysterycall_outcome_bounds]{mysterycall_outcome_bounds()}} (Manski bounds on the obtainment rate) or
\code{\link[=mysterycall_leave_one_out]{mysterycall_leave_one_out()}} -- rather than forcing a Heckman correction.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(
  practice  = rep(sprintf("p\%03d", 1:150), each = 2),
  insurance = rep(c("Commercial", "Medicaid"), 150),
  obtained  = rbinom(300, 1, 0.65))
d$wait_days <- ifelse(d$obtained == 1, rnbinom(300, mu = 16, size = 2), NA)
mysterycall_hurdle_wait(d, "obtained", "wait_days", "insurance",
                        random_intercept = "practice")
}
}
