% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/access_cascade.R
\name{mysterycall_cascade_stage}
\alias{mysterycall_cascade_stage}
\title{Define one stage of an access cascade}
\usage{
mysterycall_cascade_stage(
  label,
  column,
  success,
  denominator = "total",
  group = NULL
)
}
\arguments{
\item{label}{Human-readable measure name shown in the table and figure
(e.g. \code{"Practice accepting new patients"}).}

\item{column}{Name of the column in \code{data} that carries this measure.}

\item{success}{The value(s) in \code{column} that count as a "hit". Matched with
\code{\%in\%} (so \code{NA} never counts). Alternatively a predicate
\verb{function(x)} returning a logical vector the length of \code{x}.}

\item{denominator}{What the count is a percentage \emph{of}. One of:
\code{"total"} (the number of rows in \code{data}, i.e. the analytic sample; the
default), \code{"previous"} (the hit count of the stage defined immediately
before this one, giving a strict funnel), a single positive number, or a
character string naming the \code{label} of an earlier stage (whose hit count
becomes the denominator — e.g. reporting "share of offers" against an
earlier "offered" stage).}

\item{group}{Optional block label used to group related stages in the output
table (e.g. \code{"Access cascade"}, \code{"Reasons not accepting"}). Defaults to
\code{"Access cascade"}.}
}
\value{
A list with class \code{"mysterycall_cascade_stage"}.
}
\description{
Helper that builds a single stage specification for
\code{\link[=mysterycall_access_cascade]{mysterycall_access_cascade()}}. A stage is one row of the access cascade:
a labelled measure counted over a column, with a denominator that is either
the full analytic sample, the previous stage, another stage, or a fixed
number.
}
\examples{
mysterycall_cascade_stage(
  "New-patient appointment offered", "appointment_offered", success = TRUE
)
}
\seealso{
\code{\link[=mysterycall_access_cascade]{mysterycall_access_cascade()}}
}
