% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/facet_histogram.R
\name{mysterycall_facet_histogram}
\alias{mysterycall_facet_histogram}
\title{Faceted histogram of a numeric variable by group}
\usage{
mysterycall_facet_histogram(
  data,
  x_col,
  facet_col,
  binwidth = 1,
  fill = "skyblue",
  color = "black",
  alpha = 0.7,
  show_mean_line = TRUE,
  show_stats_text = TRUE,
  x_label = NULL,
  y_label = "Count",
  title = NULL,
  monochrome = FALSE,
  base_size = 12,
  reorder_facets = FALSE,
  dpi = 150,
  output_dir = NULL,
  filename = "facet_histogram.png"
)
}
\arguments{
\item{data}{A data frame.}

\item{x_col}{Character scalar.  Name of the numeric column to plot on the
x-axis.}

\item{facet_col}{Character scalar.  Name of the grouping column used for
the facet panels.}

\item{binwidth}{Numeric.  Histogram bin width.  Default \code{1}.}

\item{fill}{Character.  Bar fill colour.  Default \code{"skyblue"}.}

\item{color}{Character.  Bar border colour.  Default \code{"black"}.}

\item{alpha}{Numeric.  Bar transparency [0, 1].  Default \code{0.7}.}

\item{show_mean_line}{Logical.  Add a vertical dashed red line at the
group mean?  Default \code{TRUE}.}

\item{show_stats_text}{Logical.  Annotate each facet with Mean, SD,
Median, 25th percentile, and 75th percentile?  Default \code{TRUE}.}

\item{x_label}{Character or \code{NULL}.  X-axis label.  Defaults to \code{x_col}.}

\item{y_label}{Character.  Y-axis label.  Default \code{"Count"}.}

\item{title}{Character or \code{NULL}.  Plot title.  Defaults to
\code{"Distribution of [x_col] by [facet_col] (N = [n])"}.}

\item{monochrome}{Logical. If \code{TRUE}, apply a greyscale fill and
\code{\link[=mysterycall_bw_theme]{mysterycall_bw_theme()}} suitable for Greene-journal submissions.
Overrides \code{fill} and the mean-line colour.  Default \code{FALSE}.
(Inspired by Nicola Rennie, 2025.)}

\item{base_size}{Numeric. Base font size in points passed to
\code{theme_light()}.  Default \code{12}.  (Rennie 2026.)}

\item{reorder_facets}{Logical. If \code{TRUE}, facet panels are reordered from
highest to lowest median of \code{x_col} rather than alphabetically.
(Rennie 2024 — order groups by a summary statistic.)  Default \code{FALSE}.}

\item{dpi}{Integer.  Resolution for the saved PNG.  Default \code{150}.}

\item{output_dir}{Character, \code{NULL}, or \code{NA}.  Directory for PNG output.
\code{NULL} (default) writes to a session temp directory via
\code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}.  \code{NA} skips writing entirely.}

\item{filename}{Character.  Output file name.  Default
\code{"facet_histogram.png"}.}
}
\value{
A \code{ggplot} object, returned invisibly.  As a side effect, writes
a PNG to \code{output_dir} unless \code{output_dir} is \code{NA}.
}
\description{
Produces a faceted histogram (one panel per level of \code{facet_col}) with an
optional density-curve overlay, per-group mean line, and per-facet
annotation of descriptive statistics.  Mirrors the source pattern from the
analysis Rmd (lines 1094-1150).
}
\note{
Several axis and theme improvements are inspired by Nicola Rennie:
\code{guide_axis(check.overlap = TRUE)} (Rennie 2026),
\code{size.unit = "pt"} for consistent text sizing (Rennie 2026),
\code{plot.title.position = "plot"} (Rennie 2026),
\code{bg = "white"} in \code{ggsave} (Rennie 2026),
monochrome palette and \code{\link[=mysterycall_bw_theme]{mysterycall_bw_theme()}} (Rennie 2025),
facet reordering by median (Rennie 2024).
}
\examples{
set.seed(1)
df <- data.frame(
  days      = c(rpois(60, 5), rpois(60, 10)),
  insurance = rep(c("Medicaid", "BCBS"), each = 60)
)
mysterycall_facet_histogram(df, x_col = "days", facet_col = "insurance",
                             output_dir = NA)
}
\seealso{
Other descriptive helpers:
\code{\link[=mysterycall_demographics_sentence]{mysterycall_demographics_sentence()}},
\code{\link[=mysterycall_descriptive_stats]{mysterycall_descriptive_stats()}},
\code{\link[=mysterycall_distribution_summary]{mysterycall_distribution_summary()}},
\code{\link[=mysterycall_log_histogram]{mysterycall_log_histogram()}},
\code{\link[=mysterycall_physicians_with_detail]{mysterycall_physicians_with_detail()}},
\code{\link[=mysterycall_scenario_coverage]{mysterycall_scenario_coverage()}},
\code{\link[=mysterycall_scenario_summary]{mysterycall_scenario_summary()}},
\code{\link[=mysterycall_sensitivity_both_insurance]{mysterycall_sensitivity_both_insurance()}}
}
\concept{descriptive helpers}
