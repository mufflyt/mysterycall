% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_calls.R
\name{mysterycall_prepare_calls}
\alias{mysterycall_prepare_calls}
\title{Prepare raw REDCap mystery-caller data for statistical analysis}
\usage{
mysterycall_prepare_calls(
  data,
  col_calldate = "calldate1",
  col_contacted1 = "contacted1",
  col_contacted2 = "contacted2",
  col_appdate = "appdate",
  col_exclusions = "exclusions",
  col_initials = "initials",
  logistic_include_codes = c(0L, 7L, 9L, 10L),
  na_exclusions = c("warn", "drop", "include"),
  caller_col_out = "caller",
  appt_offered_col = "appt_offered",
  calendar_days_col = "calendar_days"
)
}
\arguments{
\item{data}{A data frame. The raw REDCap export.}

\item{col_calldate}{Character scalar. Column containing the date of the
first call. Default \code{"calldate1"}.}

\item{col_contacted1}{Character scalar. Binary column: office reached on
first call (1 = yes, 0 = no). Default \code{"contacted1"}.}

\item{col_contacted2}{Character scalar. Binary column: office reached on
second call (1 = yes, 0 = no, 99 = N/A). Default \code{"contacted2"}.}

\item{col_appdate}{Character scalar. Appointment date column.
Default \code{"appdate"}.}

\item{col_exclusions}{Character scalar. Integer exclusion-code column.
Default \code{"exclusions"}.}

\item{col_initials}{Character scalar. Caller-identity column.
Default \code{"initials"}.}

\item{logistic_include_codes}{Integer vector. Exclusion codes to include
in the \strong{logistic} dataset (appointment-offered model). Code 0 is
always included. Codes 7, 9, and 10 represent "office reached but
appointment refused/blocked" and default to inclusion (outcome = 0).
Default \code{c(0L, 7L, 9L, 10L)}.}

\item{na_exclusions}{Character. How to handle \code{NA} exclusion codes.
\code{"warn"} (default) keeps them but issues a warning and adds a flag
column. \code{"drop"} removes them. \code{"include"} silently keeps them.}

\item{caller_col_out}{Character scalar. Name of the standardized caller
column in the output. Default \code{"caller"}.}

\item{appt_offered_col}{Character scalar. Name of the binary outcome
column added to \verb{$logistic_data}. Default \code{"appt_offered"}.}

\item{calendar_days_col}{Character scalar. Name of the calendar-days
column added to \verb{$waittime_data}. Default \code{"calendar_days"}.}
}
\value{
A list of class \code{mysterycall_prepared} with elements:
\describe{
\item{\code{logistic_data}}{Data frame ready for \code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}}.
Adds columns: \code{caller} (title-cased), \code{appt_offered} (0/1),
\code{reached} (logical), \code{exclusion_na} (logical flag).}
\item{\code{waittime_data}}{Subset of \code{logistic_data} with \code{appdate}
present. Adds \code{calendar_days} (numeric, days from call to appt).}
\item{\code{waterfall}}{Data frame: one row per filter step showing
\code{step}, \code{n_remaining}, \code{n_dropped}, and \code{reason}.}
\item{\code{exclusion_summary}}{Data frame: counts by exclusion code with
labels.}
\item{\code{caller_summary}}{Data frame: standardized caller names and
call counts.}
\item{\code{na_exclusion_records}}{Data frame: rows where \code{exclusions} is
\code{NA}, for manual review. \code{NULL} when none.}
}
}
\description{
Applies the standard filtering pipeline for mystery-caller studies:
removes records without a call date, applies exclusion-code filtering,
resolves second-call contacts, standardizes caller names to title case,
and creates ready-to-model outcome columns for both the logistic model
(appointment offered: yes/no) and the wait-time model (calendar days).
}
\details{
\strong{Two inclusion sets are created (see Value):}
\describe{
\item{\verb{$logistic_data}}{All calls where the office was \emph{reached} —
whether or not an appointment was offered. Use with
\code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}}. Exclusion codes 7, 9, and 10 are
included as "appointment not offered" (outcome = 0).}
\item{\verb{$waittime_data}}{Subset of \verb{$logistic_data} where an appointment
date was recorded. Use with \code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}},
\code{\link[=mysterycall_nb_model]{mysterycall_nb_model()}}, or \code{\link[=mysterycall_auto_model]{mysterycall_auto_model()}}.}
}

\strong{Standard REDCap column names assumed} (override via \verb{col_*} arguments):
\itemize{
\item \code{calldate1} — date of first call attempt
\item \code{contacted1} — 1 = office reached, 0 = not reached (first call)
\item \code{contacted2} — 1 = reached on second attempt, 0 = not, 99 = N/A
\item \code{appdate} — date of appointment offered
\item \code{exclusions} — integer exclusion code (0 = included)
\item \code{initials} — caller identity (standardized to title case)
\item \code{medicaid_status} — 1=accepts, 2=refuses, 3=BCBS call, 4=unknown
}
}
\examples{
df <- data.frame(
  calldate1   = c("2024-01-10", "2024-01-11", "2024-01-12", NA, "2024-01-14"),
  contacted1  = c(1, 1, 0, 1, 1),
  contacted2  = c(99, 99, 1, 99, 99),
  appdate     = c("2024-02-01", "", "2024-02-05", "2024-02-03", "2024-02-10"),
  exclusions  = c(0, 9, 0, 0, 7),
  initials    = c("lizeth", "MERILYN", "Lizeth", "jessica", "merilyn"),
  stringsAsFactors = FALSE
)
result <- mysterycall_prepare_calls(df)
print(result)
}
\seealso{
\code{\link[=mysterycall_logistic_model]{mysterycall_logistic_model()}}, \code{\link[=mysterycall_nb_model]{mysterycall_nb_model()}},
\code{\link[=mysterycall_auto_model]{mysterycall_auto_model()}}

Other data-preparation: 
\code{\link{print.mysterycall_prepared}()}
}
\concept{data-preparation}
