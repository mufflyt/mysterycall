% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region_labels.R
\name{mysterycall_region_labels}
\alias{mysterycall_region_labels}
\title{Region Labels for a US State Choropleth}
\usage{
mysterycall_region_labels(
  system = c("aao_hns", "acog", "census"),
  label = c("region", "abbr")
)
}
\arguments{
\item{system}{Region system passed to \code{\link[=mysterycall_assign_region]{mysterycall_assign_region()}}. One of
\code{"aao_hns"} (default), \code{"acog"}, or \code{"census"}.}

\item{label}{Character. Which column is exposed as \code{label} for convenience:
\code{"region"} (default) or \code{"abbr"} (the two-letter state code).}
}
\value{
A data frame with one row per of the 50 US states and columns
\code{state}, \code{abbr}, \code{region}, \code{lon}, \code{lat}, and \code{label}. Because
\code{datasets::state.center} covers only the 50 states, the District of
Columbia and territories are not included; add their label positions
manually if your map shows them.
}
\description{
Returns one row per US state with its region label and an approximate
centroid, ready to overlay on a state choropleth as a
\code{\link[ggplot2:geom_text]{ggplot2::geom_text()}} layer -- e.g. to label each state with its AAO-HNS
district. Region assignments come from \code{\link[=mysterycall_assign_region]{mysterycall_assign_region()}};
centroids use base R \code{datasets::state.center}, so no spatial packages are
needed.
}
\examples{
labs <- mysterycall_region_labels(system = "aao_hns")
head(labs)

# Overlay on a state choropleth (ggplot2):
\dontrun{
ggplot2::ggplot() +
  ggplot2::geom_sf(data = states_sf, ggplot2::aes(fill = region)) +
  ggplot2::geom_text(
    data = mysterycall_region_labels(),
    ggplot2::aes(x = lon, y = lat, label = label), size = 3
  )
}
}
\seealso{
\code{\link[=mysterycall_assign_region]{mysterycall_assign_region()}}
}
\concept{mapping}
