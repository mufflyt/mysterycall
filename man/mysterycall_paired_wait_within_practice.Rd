% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paired_analyses.R
\name{mysterycall_paired_wait_within_practice}
\alias{mysterycall_paired_wait_within_practice}
\title{Within-practice paired wait-time comparison}
\usage{
mysterycall_paired_wait_within_practice(
  data,
  id_col,
  scenario_col,
  wait_col,
  contrasts = NULL,
  power = 0.8,
  conf_level = 0.95,
  output_dir = NA,
  filename = NA
)
}
\arguments{
\item{data}{A long data frame: one row per practice x scenario.}

\item{id_col}{Practice / cluster identifier column.}

\item{scenario_col}{Scenario column (2+ levels).}

\item{wait_col}{Numeric wait-time column.}

\item{contrasts}{Optional list of length-2 character vectors of scenario
pairs. Default \code{NULL} tests all pairwise combinations.}

\item{power, conf_level}{Power for the minimum detectable difference and
confidence level for the mean-difference interval. Defaults \code{0.80}, \code{0.95}.}

\item{output_dir, filename}{Optional CSV export of the result table.}
}
\value{
An object of class \code{"mysterycall_paired_wait"}: a list with \code{table}
(one row per contrast: \code{contrast}, \code{n_paired}, \code{mean_diff_days},
\code{ci_lower}, \code{ci_upper}, \code{sd_diff}, \code{paired_t_p}, \code{wilcoxon_p},
\code{mde_days_power}). \code{\link[=as.data.frame]{as.data.frame()}} returns the table.
}
\description{
The matched analogue of \code{\link[=mysterycall_paired_acceptance_mcnemar]{mysterycall_paired_acceptance_mcnemar()}} for a
continuous wait-time outcome: for each pairwise scenario contrast it pairs the
practices called under both scenarios, forms the within-practice wait
differences, and reports the mean difference (with a paired-t confidence
interval), the paired t-test and Wilcoxon signed-rank p-values, and the
minimum detectable difference in days at the requested power. Pairing removes
each practice's baseline speed, which the unmatched comparison confounds.
}
\examples{
set.seed(2)
d <- data.frame(
  practice = rep(1:30, each = 2),
  scenario = rep(c("Commercial", "Medicaid"), 30),
  wait_days = round(c(rbind(rpois(30, 10), rpois(30, 18))))
)
mysterycall_paired_wait_within_practice(d, "practice", "scenario", "wait_days")
}
