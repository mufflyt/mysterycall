% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_paired_slope.R
\name{mysterycall_plot_paired_slope}
\alias{mysterycall_plot_paired_slope}
\title{Within-cluster paired slope plot across scenarios}
\usage{
mysterycall_plot_paired_slope(
  data,
  value,
  group,
  id,
  title = NULL,
  x_lab = NULL,
  y_lab = value,
  line_alpha = 0.5
)
}
\arguments{
\item{data}{A long data frame: one row per cluster x scenario call.}

\item{value}{Character scalar naming the numeric outcome column.}

\item{group}{Character scalar naming the grouping / scenario column
(the x axis).}

\item{id}{Character scalar naming the cluster / practice id column (lines are
grouped by this).}

\item{title, x_lab, y_lab}{Character scalars for the title and axis labels.
\code{title} defaults to \code{NULL}; \code{x_lab} to \code{NULL}; \code{y_lab} to \code{value}.}

\item{line_alpha}{Numeric opacity for the connecting lines. Default \code{0.5}.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
The visual companion to \code{\link[=mysterycall_paired_wait_within_practice]{mysterycall_paired_wait_within_practice()}} and
\code{\link[=mysterycall_paired_acceptance_mcnemar]{mysterycall_paired_acceptance_mcnemar()}}: one line per cluster (practice)
connecting its outcome across the scenarios it was called under, over the raw
points. It shows the within-cluster differencing that motivates the matched
design -- whether the \emph{same} practice tends to schedule one persona sooner
than another -- rather than the between-practice comparison a boxplot implies.
Only clusters observed under at least two scenarios are drawn (a single point
has no slope to show). Returns a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}}.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  d <- data.frame(
    practice = rep(1:15, each = 2),
    scenario = rep(c("Commercial", "Medicaid"), 15),
    wait     = c(rbind(rpois(15, 10), rpois(15, 22)))
  )
  mysterycall_plot_paired_slope(d, "wait", "scenario", "practice")
}
}
\seealso{
\code{\link[=mysterycall_paired_wait_within_practice]{mysterycall_paired_wait_within_practice()}},
\code{\link[=mysterycall_plot_raincloud]{mysterycall_plot_raincloud()}}

Other visualization:
\code{\link[=mysterycall_acceptance_waffle]{mysterycall_acceptance_waffle()}},
\code{\link[=mysterycall_bw_theme]{mysterycall_bw_theme()}},
\code{\link[=mysterycall_flowchart]{mysterycall_flowchart()}},
\code{\link[=mysterycall_plot_density]{mysterycall_plot_density()}},
\code{\link[=mysterycall_plot_line]{mysterycall_plot_line()}},
\code{\link[=mysterycall_plot_raincloud]{mysterycall_plot_raincloud()}},
\code{\link[=mysterycall_plot_scatter]{mysterycall_plot_scatter()}}
}
\concept{visualization}
