% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/univariate_lmm_screen.R
\name{mysterycall_univariate_lmm_screen}
\alias{mysterycall_univariate_lmm_screen}
\title{Screen predictors one-at-a-time using a linear mixed model}
\usage{
mysterycall_univariate_lmm_screen(
  data,
  outcome_col = "business_days_until_appointment",
  random_effect = "last",
  exclude_cols = c(outcome_col, random_effect, "record_id", "middle", "first", "phone",
    "zip", "notes", "address"),
  alpha = 0.2,
  p_adjust_method = "none",
  output_dir = NULL,
  filename = "univariate_lmm_screen.csv"
)
}
\arguments{
\item{data}{A data frame. Rows where \code{outcome_col} is \code{NA} are dropped.}

\item{outcome_col}{Character scalar. The numeric outcome column.
Default \code{"business_days_until_appointment"}.}

\item{random_effect}{Character scalar. Column used as the random intercept
\code{(1 | random_effect)}. Default \code{"last"}.}

\item{exclude_cols}{Character vector. Columns to exclude from the predictor
loop. Default includes \code{outcome_col}, \code{random_effect}, and common
identifier / free-text columns.}

\item{alpha}{Numeric. Significance threshold for the \verb{$significant} table.
Default \code{0.2}.}

\item{p_adjust_method}{Character scalar passed to \code{\link[stats:p.adjust]{stats::p.adjust()}} after
all raw p-values are collected. \code{"none"} (default) skips adjustment and
preserves existing behaviour. Common choices: \code{"BH"}, \code{"bonferroni"},
\code{"holm"}. When not \code{"none"}, a \code{P_Value_Adjusted} column is added to
\verb{$results} and significance is evaluated against the adjusted values.}

\item{output_dir}{Character scalar or \code{NULL}. Directory for CSV output.
\code{NULL} uses \code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing.}

\item{filename}{Character scalar. CSV file name.
Default \code{"univariate_lmm_screen.csv"}.}
}
\value{
A named list with four elements:
\describe{
\item{\code{results}}{\code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{Predictor}, \code{P_Value},
\code{P_Formatted}, \code{Estimate} (additive mean difference in outcome units),
\code{CI_Lower}, \code{CI_Upper} for every predictor attempted.}
\item{\code{significant}}{Subset of \code{results} where \code{P_Value < alpha}, sorted
ascending by \code{P_Value}.}
\item{\code{sentence}}{Character scalar summarising significant predictors.}
\item{\code{alpha}}{The threshold used.}
}
}
\description{
For each predictor (excluding \code{outcome_col}, \code{random_effect}, and any
\code{exclude_cols}), fits a linear mixed model using \code{\link[lmerTest:lmer]{lmerTest::lmer()}} with
\code{REML = FALSE}, then extracts the fixed-effect estimate, standard error, and
p-value from the second row of the coefficient table (the predictor row).
Predictors with only one unique non-NA value are skipped with a message.
}
\details{
Because the underlying model is linear (not Poisson), each predictor's
fixed-effect coefficient is an additive mean difference in the outcome's own
units, returned as \code{Estimate} with a 95 \% Wald confidence interval on the
same additive scale. (Earlier versions exponentiated this coefficient and
mislabelled it "IRR", which is meaningless for a linear-scale day
difference.) Fit \code{\link[=mysterycall_univariate_poisson_screen]{mysterycall_univariate_poisson_screen()}} instead if a true
rate ratio is wanted.
}
\examples{
\dontshow{if (requireNamespace("lmerTest", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(1)
df <- data.frame(
  business_days_until_appointment = rpois(60, 10),
  insurance = rep(c("BCBS", "Medicaid"), 30),
  gender    = rep(c("M", "F"), 30),
  last      = rep(paste0("Dr", 1:10), each = 6),
  stringsAsFactors = FALSE
)
res <- mysterycall_univariate_lmm_screen(df, output_dir = NA)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=mysterycall_univariate_poisson_screen]{mysterycall_univariate_poisson_screen()}},
\code{\link[=mysterycall_interaction_screen]{mysterycall_interaction_screen()}}

Other modeling helpers:
\code{\link[=mysterycall_check_normality]{mysterycall_check_normality()}},
\code{\link[=mysterycall_create_formula]{mysterycall_create_formula()}},
\code{\link[=mysterycall_interaction_screen]{mysterycall_interaction_screen()}},
\code{\link[=mysterycall_overdispersion_sentence]{mysterycall_overdispersion_sentence()}},
\code{\link[=mysterycall_plot_interaction]{mysterycall_plot_interaction()}},
\code{\link[=mysterycall_r2_sentence]{mysterycall_r2_sentence()}},
\code{\link[=mysterycall_random_effect_variance]{mysterycall_random_effect_variance()}},
\code{\link[=mysterycall_univariate_poisson_screen]{mysterycall_univariate_poisson_screen()}}
}
\concept{modeling helpers}
