% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/access_cascade.R
\name{mysterycall_access_cascade}
\alias{mysterycall_access_cascade}
\title{Summarize an access cascade across the call pathway}
\usage{
mysterycall_access_cascade(
  data,
  stages,
  conf_level = 0.95,
  plot = FALSE,
  title = NULL,
  subtitle = NULL,
  output_dir = NA,
  filename = NA
)
}
\arguments{
\item{data}{A data frame (one row per call / encounter). Filter it to your
analytic sample \emph{before} calling; \code{"total"} denominators use \code{nrow(data)}.}

\item{stages}{A list of stage specifications. Each element is either the
output of \code{\link[=mysterycall_cascade_stage]{mysterycall_cascade_stage()}} or a plain list with the same
fields (\code{label}, \code{column}, \code{success}, and optionally \code{denominator},
\code{group}). Stages are evaluated in order, which is what makes \code{"previous"}
and by-name denominators resolvable.}

\item{conf_level}{Confidence level for the Wilson interval on each
proportion. Default \code{0.95}.}

\item{plot}{Logical; if \code{TRUE} (requires \pkg{ggplot2}) attach a horizontal
funnel figure of every stage to the returned object as \verb{$plot}.}

\item{title, subtitle}{Optional figure title / subtitle. When \code{plot = TRUE}
and left \code{NULL}, sensible defaults describing the analytic sample are used.}

\item{output_dir, filename}{Optional. If both are non-\code{NA}, the table is
written to \code{file.path(output_dir, filename)} as CSV.}
}
\value{
An object of class \code{"mysterycall_access_cascade"}: a list with
\code{table} (a tibble of \code{group}, \code{measure}, \code{n}, \code{denominator}, \code{pct},
\code{ci_lower}, \code{ci_upper}), \code{n_total} (rows in \code{data}), and \code{plot} (a ggplot
or \code{NULL}). \code{\link[=as.data.frame]{as.data.frame()}} returns the table.
}
\description{
Mystery-caller and simulated-patient studies capture a sequence of
intermediate access constructs on the way to (or instead of) an appointment:
reaching a live office, whether the practice accepts new patients, whether it
treats the presented complaint, whether an appointment is offered, and with
whom. This function turns an ordered set of stage definitions into a tidy
count / denominator / percent table with Wilson confidence intervals, and an
optional funnel-style figure. Denominators may be the full analytic sample, a
strictly nested previous stage, another named stage, or a fixed number, so a
single call can mix a nested funnel with "share of offers" sub-breakdowns.
}
\examples{
d <- data.frame(
  office_answered      = c(TRUE, TRUE, TRUE, FALSE, TRUE),
  new_patient_status   = c("Accepting", "Accepting", "Not accepting", NA, "Accepting"),
  appointment_offered  = c(TRUE, FALSE, FALSE, FALSE, TRUE)
)
mysterycall_access_cascade(d, list(
  mysterycall_cascade_stage("Reached a live office", "office_answered", TRUE),
  mysterycall_cascade_stage("Accepting new patients", "new_patient_status", "Accepting"),
  mysterycall_cascade_stage("Appointment offered", "appointment_offered", TRUE)
))
}
