% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irr_to_days.R
\name{mysterycall_irr_to_days}
\alias{mysterycall_irr_to_days}
\title{Convert IRRs to absolute wait-time differences in days}
\usage{
mysterycall_irr_to_days(
  model_result,
  baseline_mean,
  exposure_col = NULL,
  ref_group = NULL,
  digits = 1L,
  irr_digits = 2L
)
}
\arguments{
\item{model_result}{A \code{mysterycall_poisson_model} or \code{mysterycall_nb_model}
object, or a data frame with columns \code{term}, \code{irr}, \code{ci_lower},
\code{ci_upper}, and (optionally) \code{p_value_fmt}.}

\item{baseline_mean}{Numeric scalar. The observed mean wait days in the
reference group (e.g. the mean for BCBS callers). Obtain this from
\code{\link[=mysterycall_wait_time_summary]{mysterycall_wait_time_summary()}} or \code{mean(data$wait_days[data$insurance == "BCBS"])}.}

\item{exposure_col}{Character scalar. Name of the exposure variable whose
terms to report (e.g. \code{"insurance"}). Only rows whose \code{term} starts with
this string are included. Pass \code{NULL} to include all non-intercept terms.}

\item{ref_group}{Character scalar. Label for the reference group used in the
narrative sentences (e.g. \code{"BCBS"}). When \code{NULL}, sentences omit the
"compared with X" clause.}

\item{digits}{Integer. Decimal places for days in the output table and
sentences. Default \code{1L}.}

\item{irr_digits}{Integer. Decimal places for IRR in the sentences. Default \code{2L}.}
}
\value{
A list of class \code{mysterycall_irr_days} with elements:
\describe{
\item{\code{table}}{Data frame with columns: \code{term}, \code{level} (group label
stripped of the exposure prefix), \code{irr}, \code{days_mean} (reference-group
mean x IRR), \code{days_diff} (days_mean - baseline_mean), \code{days_ci_lower},
\code{days_ci_upper}, \code{direction} (\code{"more"} or \code{"fewer"}), \code{p_value_fmt}.}
\item{\code{sentences}}{Character vector. One manuscript-ready sentence per row.}
\item{\code{paragraph}}{Character scalar. All sentences joined into a paragraph.}
\item{\code{baseline_mean}}{Numeric. The baseline_mean supplied by the caller.}
}
}
\description{
Translates incidence rate ratios (IRRs) and their Wald confidence intervals
from a fitted mystery-caller model into clinically interpretable absolute
differences in wait days. Given a reference-group mean, the conversion is:
}
\details{
\deqn{\text{days difference} = \mu_{\text{ref}} \times (\text{IRR} - 1)}
\deqn{\text{CI in days} = \mu_{\text{ref}} \times (\text{CI}_{\text{lower/upper}} - 1)}

Positive values mean the comparison group waits \emph{longer}; negative values
mean the comparison group waits \emph{fewer} days than the reference group.
}
\section{Example sentence}{

\emph{"Medicaid-insured callers waited a mean of 4.3 additional days compared
with BCBS (95\% CI 1.2 to 7.4 days; IRR 1.24, p = 0.003)."}
}

\examples{
irr_tbl <- data.frame(
  term        = c("(Intercept)", "insuranceMedicaid", "insuranceUninsured"),
  irr         = c(1.00, 1.28, 0.81),
  ci_lower    = c(NA,   1.05, 0.61),
  ci_upper    = c(NA,   1.56, 1.07),
  p_value_fmt = c(NA,   "0.014", "0.134"),
  stringsAsFactors = FALSE
)
result <- mysterycall_irr_to_days(
  irr_tbl,
  baseline_mean = 21,
  exposure_col  = "insurance",
  ref_group     = "BCBS"
)
result$table
cat(result$paragraph)
}
\seealso{
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}}, \code{\link[=mysterycall_nb_model]{mysterycall_nb_model()}},
\code{\link[=mysterycall_write_results_paragraph]{mysterycall_write_results_paragraph()}} for IRR-scale reporting;
\code{\link[=mysterycall_irr_plot]{mysterycall_irr_plot()}} to visualise the same estimates.

Other reporting: 
\code{\link{mysterycall_write_results_paragraph}()},
\code{\link{print.mysterycall_irr_days}()}
}
\concept{reporting}
