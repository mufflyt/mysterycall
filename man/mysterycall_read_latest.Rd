% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_latest.R
\name{mysterycall_read_latest}
\alias{mysterycall_read_latest}
\title{Read (or locate) the most recent file matching a pattern - loudly}
\usage{
mysterycall_read_latest(
  dir,
  pattern = NULL,
  format = NULL,
  read = TRUE,
  max_age_days = NULL,
  recursive = FALSE,
  ignore_case = FALSE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{dir}{Character scalar. Directory to search. Must exist.}

\item{pattern}{Character scalar or \code{NULL}. Regular expression matched against
file names (as in \code{\link[=list.files]{list.files()}}). \code{NULL} (default) matches every file.
To match a literal string containing regex metacharacters, wrap it in
\code{\link[stringr:modifiers]{stringr::fixed()}}-style escaping yourself or pass an anchored pattern such as
\code{"^redcap_export_.*\\\\.csv$"}.}

\item{format}{Optional character scalar passed to \code{\link[=mysterycall_read_table]{mysterycall_read_table()}}:
\code{"csv"} or \code{"parquet"}. When \code{NULL} (default) the format is inferred from
the chosen file's extension. Ignored when \code{read = FALSE}.}

\item{read}{Logical. When \code{TRUE} (default) the chosen file is read and its
contents returned. When \code{FALSE} only the resolved file path (character
scalar) is returned.}

\item{max_age_days}{Positive number or \code{NULL}. When supplied, a warning is
emitted if the newest matching file is more than \code{max_age_days} days old.
Default \code{NULL} (no staleness check).}

\item{recursive}{Logical. When \code{TRUE}, search \code{dir} recursively. Default
\code{FALSE}.}

\item{ignore_case}{Logical. When \code{TRUE}, \code{pattern} matching is
case-insensitive. Default \code{FALSE}.}

\item{quiet}{Logical. When \code{TRUE}, suppress the "chose \if{html}{\out{<file>}}" announcement
message. Warnings and errors are still raised. Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to \code{\link[=mysterycall_read_table]{mysterycall_read_table()}} when
\code{read = TRUE}.}
}
\value{
When \code{read = TRUE}, a data frame (tibble) with the resolved absolute
path attached as the \code{"path"} attribute. When \code{read = FALSE}, the resolved
file path as a character scalar.
}
\description{
Mystery-caller pipelines routinely re-export the same REDCap instrument or
roster many times, leaving a directory full of dated files. The common
\code{latest()} idiom - "glob a folder, pick the newest, read it" - is a quiet
footgun: it returns \code{NA} (or nothing) when no file matches, never announces
\emph{which} file it chose, and silently reads a stale export when a fresh one
failed to land. A wrong file then flows all the way to the results with no
warning.
}
\details{
\code{mysterycall_read_latest()} is the loud replacement. It:
\itemize{
\item \strong{errors} (never returns \code{NA}) when zero files match, printing the
directory and pattern that came up empty;
\item \strong{announces} the chosen file and its modification time, plus how
many candidates it beat, so the selection is visible in the log;
\item \strong{warns on staleness} when the newest match is older than
\code{max_age_days}, catching the "today's export never wrote" case;
\item \strong{warns on ties} when two files share the newest modification time,
because "the newest" is then ambiguous.
}

By default it reads the file via \code{\link[=mysterycall_read_table]{mysterycall_read_table()}} (CSV or Parquet)
and returns the data frame with the resolved path stored in the \code{"path"}
attribute. Set \code{read = FALSE} to return just the path - a drop-in for the
bare \code{latest()} calls that only needed a filename.
}
\section{Migrating from \code{latest()}}{


\if{html}{\out{<div class="sourceCode r">}}\preformatted{# Before - silent, returns NA when nothing matches:
f  <- latest("data/redcap", "\\\\.csv$")
df <- read.csv(f)

# After - announces its choice, errors instead of returning NA,
# warns if the newest export is more than a day old:
df <- mysterycall_read_latest("data/redcap", "\\\\.csv$", max_age_days = 1)
path <- attr(df, "path")   # which file it actually read
}\if{html}{\out{</div>}}
}

\examples{
# Set up a directory with two dated exports
dir <- tempfile("redcap_")
dir.create(dir)
old_file <- file.path(dir, "export_2026-07-01.csv")
new_file <- file.path(dir, "export_2026-07-20.csv")
utils::write.csv(data.frame(x = 1), old_file, row.names = FALSE)
utils::write.csv(data.frame(x = 2), new_file, row.names = FALSE)
# Make the second file unambiguously newer
Sys.setFileTime(old_file, Sys.time() - 3600)

# Return just the path to the newest match
mysterycall_read_latest(dir, "\\\\.csv$", read = FALSE)
}
\seealso{
\code{\link[=mysterycall_read_table]{mysterycall_read_table()}} for the underlying reader;
\code{\link[=mysterycall_resolve_path]{mysterycall_resolve_path()}} for constructing standard paths.

Other utilities: 
\code{\link{.title_case}()},
\code{\link{\%>\%}()},
\code{\link{format_phone_number}()},
\code{\link{mysterycall_assess_data_quality}()},
\code{\link{mysterycall_check_api_response}()},
\code{\link{mysterycall_check_data_completeness}()},
\code{\link{mysterycall_check_dependencies}()},
\code{\link{mysterycall_check_no_data_loss}()},
\code{\link{mysterycall_check_no_limits}()},
\code{\link{mysterycall_download_file}()},
\code{\link{mysterycall_estimate_resources}()},
\code{\link{mysterycall_export_with_backup}()},
\code{\link{mysterycall_normalize_file_format}()},
\code{\link{mysterycall_parse_redcap_labels}()},
\code{\link{mysterycall_preflight_check}()},
\code{\link{mysterycall_quality_tier}()},
\code{\link{mysterycall_read_table}()},
\code{\link{mysterycall_require_arrow}()},
\code{\link{mysterycall_resolve_path}()},
\code{\link{mysterycall_save_quality_table}()},
\code{\link{mysterycall_scan_for_limits}()},
\code{\link{mysterycall_standard_labels}()},
\code{\link{mysterycall_standard_palette}()},
\code{\link{mysterycall_write_table}()}
}
\concept{utilities}
