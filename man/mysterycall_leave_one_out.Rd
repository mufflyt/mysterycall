% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leave_one_out.R
\name{mysterycall_leave_one_out}
\alias{mysterycall_leave_one_out}
\title{Leave-one-group-out refit sensitivity}
\usage{
mysterycall_leave_one_out(
  model,
  data,
  group,
  term,
  joint_predictor = NULL,
  exponentiate = TRUE,
  min_rows = 10L
)
}
\arguments{
\item{model}{A fitted model (\code{glm}, \code{glmer}, \code{glmmTMB}, ...) or a mysterycall
model wrapper; the underlying fit is extracted automatically.}

\item{data}{The data frame the model was fit on (needed to subset and refit).}

\item{group}{Column in \code{data} whose levels are left out one at a time.}

\item{term}{Name of the coefficient to track across refits (e.g.
\code{"ent_typePediatrics"}).}

\item{joint_predictor}{Optional predictor to also joint-test on each refit
(via \code{\link[=mysterycall_joint_test]{mysterycall_joint_test()}}), adding a \code{joint_p} column.}

\item{exponentiate}{If \code{TRUE} (default) report \code{exp(estimate)} (odds/rate
ratio) alongside the estimate.}

\item{min_rows}{Skip a leave-out fit if fewer than this many rows remain.
Default \code{10}.}
}
\value{
An object of class \code{"mysterycall_leave_one_out"}: a list with \code{table}
(a tibble: \code{group_excluded}, \code{n}, \code{estimate}, optional \code{ratio}, \code{std_error},
\code{p_value}, optional \code{joint_p}, \code{converged}) and \code{full} (the target
estimate/ratio/p on the complete data). \code{\link[=as.data.frame]{as.data.frame()}} returns the table.
}
\description{
Refits a model repeatedly, each time dropping all rows belonging to one level
of a grouping variable (e.g. leave-one-caller-out, leave-one-site-out), and
tabulates how a target coefficient -- and, optionally, the joint significance
of a factor -- moves across refits. A headline estimate that is stable when
any single group is removed is more credible than one that hinges on one
caller or site.
}
\examples{
set.seed(1)
d <- data.frame(
  y = rbinom(300, 1, 0.4),
  grp = factor(sample(c("a", "b", "c"), 300, TRUE)),
  caller = factor(sample(paste0("c", 1:5), 300, TRUE))
)
fit <- glm(y ~ grp, family = binomial, data = d)
mysterycall_leave_one_out(fit, d, group = "caller", term = "grpb",
                          joint_predictor = "grp")
}
