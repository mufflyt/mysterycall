% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dedup_by_insurance.R
\name{mysterycall_dedup_by_insurance}
\alias{mysterycall_dedup_by_insurance}
\title{Deduplicate data by insurance and physician phone}
\usage{
mysterycall_dedup_by_insurance(
  data,
  phone_col = "phone",
  insurance_col = "insurance",
  name_col = "physician_information",
  keep_all = TRUE,
  output_dir = NULL,
  filename = "deduped_by_insurance.csv"
)
}
\arguments{
\item{data}{A data frame of mystery-caller records.}

\item{phone_col}{Character scalar. Column used as the unique physician
identifier (typically a phone number). Default \code{"phone"}.}

\item{insurance_col}{Character scalar. Column identifying the insurance type.
Default \code{"insurance"}.}

\item{name_col}{Character scalar or \code{NULL}. An additional column (e.g.
\code{"physician_information"}) included in the distinctness check. Pass \code{NULL}
to deduplicate on \code{phone_col} \eqn{\times} \code{insurance_col} only.
Default \code{"physician_information"}.}

\item{keep_all}{Logical. When \code{TRUE} (default), all columns are retained in
the output (equivalent to \code{.keep_all = TRUE} in \code{\link[dplyr:distinct]{dplyr::distinct()}}).}

\item{output_dir}{Character scalar or \code{NULL}. Directory for the CSV output.
\code{NULL} (default) writes to a session temp directory via
\code{\link[=mysterycall_tempdir]{mysterycall_tempdir()}}. Pass \code{NA} to skip writing entirely.}

\item{filename}{Character scalar. Name of the output CSV file.
Default \code{"deduped_by_insurance.csv"}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with one row per unique \code{phone_col} \eqn{\times}
\code{insurance_col} (and \code{name_col}, if supplied) combination.
}
\description{
Removes duplicate rows so that each unique combination of \code{phone_col} and
\code{insurance_col} (and optionally \code{name_col}) appears only once, mirroring the
source-Rmd pattern:
\code{df \%>\% dplyr::distinct(insurance, phone, physician_information, .keep_all = TRUE)}.
}
\examples{
df <- data.frame(
  phone                = c("555-1234", "555-1234", "555-9999"),
  insurance            = c("Medicaid", "Medicaid", "BCBS"),
  physician_information = c("Dr A", "Dr A", "Dr B"),
  wait_days            = c(3, 3, 7),
  stringsAsFactors     = FALSE
)
mysterycall_dedup_by_insurance(df, output_dir = NA)
}
\seealso{
\code{\link[=mysterycall_flag_repeat_physicians]{mysterycall_flag_repeat_physicians()}} for duplicate-call detection;
`mysterycall_sanity_checks()` for broader pre-analysis validation.

Other quality control: 
\code{\link{mysterycall_clean_medicaid_col}()},
\code{\link{mysterycall_flag_excluded_with_appointments}()},
\code{\link{mysterycall_flag_exclusion_discrepancy}()},
\code{\link{mysterycall_flag_included_na_appointments}()},
\code{\link{mysterycall_flag_repeat_physicians}()}
}
\concept{quality control}
