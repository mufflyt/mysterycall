% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lmm.R
\name{mysterycall_lmm}
\alias{mysterycall_lmm}
\title{Fit a Linear Mixed Model (LMM) for wait-time analysis}
\usage{
mysterycall_lmm(
  data,
  outcome,
  predictors,
  random_intercept,
  conf_level = 0.95,
  REML = TRUE,
  auto_log = TRUE,
  sensitivity_poisson = "auto",
  ...
)
}
\arguments{
\item{data}{A data frame. Rows with \code{NA} in any model column are dropped;
the count is reported.}

\item{outcome}{Character scalar. Name of the continuous wait-day column
(e.g. \code{"calendar_days"}). Must be numeric.}

\item{predictors}{Character vector of fixed-effect column names.
Character and factor columns are dummy-coded; the reference level is the
first level alphabetically (or \code{levels()[[1]]} for ordered factors).}

\item{random_intercept}{Character scalar. Grouping column for the physician
random intercept (e.g. \code{"physician"}). A \code{(1 | column)} term is added
automatically.}

\item{conf_level}{Numeric. Confidence level for Wald CIs. Default \code{0.95}.}

\item{REML}{Logical. Use Restricted Maximum Likelihood? Default \code{TRUE}
(recommended for variance estimation). Set \code{FALSE} for AIC-based model
comparison.}

\item{auto_log}{Logical. When \code{TRUE} (default), the function checks whether
the outcome is right-skewed (sample skewness > 1 on non-negative values)
and automatically applies a \code{log1p()} transform before fitting. Coefficients
are returned on both the log scale (\code{coef_table}) and back-transformed as
geometric mean ratios (\code{gmr_table}). Set to \code{FALSE} to suppress this
behaviour and model the raw outcome.}

\item{sensitivity_poisson}{One of \code{"auto"} (default), \code{TRUE}, or \code{FALSE}.
Controls whether a Poisson GLMM sensitivity analysis is run on the
\strong{original untransformed} outcome alongside the LMM:
\describe{
\item{\code{"auto"}}{Runs the Poisson model only when the Shapiro-Wilk test
on LMM residuals returns p < 0.05, indicating non-normal residuals.}
\item{\code{TRUE}}{Always runs the Poisson sensitivity model.}
\item{\code{FALSE}}{Never runs it.}
}
Results are stored in \verb{$sensitivity} (a \code{mysterycall_poisson_model}
object) and printed as a compact IRR table. Requires \code{lme4}.}

\item{...}{Additional arguments forwarded to \code{\link[lme4:lmer]{lme4::lmer()}}.}
}
\value{
A list of class \code{mysterycall_lmm} with elements:
\describe{
\item{\code{model}}{\code{lmerMod} (or \code{lmerModLmerTest}). The fitted model.}
\item{\code{coef_table}}{\code{tibble}. One row per fixed-effect term: \code{term},
\code{estimate}, \code{se}, \code{t_value}, \code{df}, \code{p_value}, \code{p_value_fmt},
\code{ci_lower}, \code{ci_upper}. Units are days (raw outcome) or log1p(days)
when \code{log_transformed} is \code{TRUE}.}
\item{\code{gmr_table}}{\code{tibble} or \code{NULL}. Present only when
\code{log_transformed = TRUE}. Columns: \code{term}, \code{GMR} (geometric mean ratio
of (days + 1) vs. reference level), \code{GMR_lo}, \code{GMR_hi}, \code{p_value_fmt}.
A GMR < 1 means shorter wait relative to the reference.}
\item{\code{log_transformed}}{\code{logical}. \code{TRUE} when \code{auto_log} triggered a
\code{log1p()} transform.}
\item{\code{outcome_original}}{\code{character}. The column name as supplied by
the caller.}
\item{\code{outcome_used}}{\code{character}. The column actually modelled
(\verb{log1p_<outcome>} when transformed, same as \code{outcome_original}
otherwise).}
\item{\code{random_effects}}{\code{data.frame} from \code{\link[lme4:VarCorr]{lme4::VarCorr()}}.}
\item{\code{factor_refs}}{\code{list}. Reference levels for character/factor
predictors.}
\item{\code{formula}}{\code{formula}. The formula passed to lmer.}
\item{\code{n}}{\code{integer}. Complete-case rows used.}
\item{\code{n_dropped}}{\code{integer}. Rows excluded for missing values.}
\item{\code{n_clusters}}{\code{integer}. Unique values of \code{random_intercept}.}
\item{\code{sigma}}{\code{numeric}. Residual standard deviation.}
\item{\code{r_squared}}{\code{list}. \code{marginal} and \code{conditional} R^2 (Nakagawa
& Schielzeth 2013).}
\item{\code{normality}}{\code{list}. Shapiro-Wilk test on model residuals:
\code{statistic}, \code{p_value}, \code{interpretation}, \code{method}. Skipped when
n > 5000.}
\item{\code{convergence}}{\code{list}. \code{converged} (logical), \code{singular} (logical),
\code{messages} (character vector).}
\item{\code{aic}}{\code{numeric}. AIC (valid only when \code{REML = FALSE}).}
\item{\code{bic}}{\code{numeric}. BIC (valid only when \code{REML = FALSE}).}
\item{\code{sensitivity}}{\code{mysterycall_poisson_model} or \code{NULL}. A Poisson
GLMM fit on the original (untransformed) outcome using the same
predictors and random intercept. Present when \code{sensitivity_poisson}
triggers; \code{NULL} otherwise. Use \verb{$sensitivity$irr_table} for IRR
estimates. IRR < 1 = shorter wait than reference.}
\item{\code{sensitivity_note}}{\code{character} or \code{NULL}. One-sentence explanation
of why the sensitivity analysis was (or was not) run.}
}
}
\description{
Fits a multilevel linear mixed model (\code{lme4::lmer}) treating appointment
wait days as a continuous, approximately normal outcome. The physician
identifier is modelled as a random intercept. Fixed-effect coefficients are
returned directly in \strong{days} (not as incidence rate ratios), making results
straightforward to explain to clinical audiences.
}
\details{
\strong{When to use LMM vs. Poisson/NB GLMM:}
Prefer LMM when the wait-day distribution is approximately symmetric with no
strong floor effect at zero (check with a Q-Q plot via
\code{\link[=plot.mysterycall_lmm]{plot.mysterycall_lmm()}}). A Shapiro-Wilk normality test on the residuals
is run automatically; a warning is issued when p < 0.05 (residuals depart
from normality). If the outcome is right-skewed or contains many zeroes,
use \code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}} or \code{\link[=mysterycall_nb_model]{mysterycall_nb_model()}} instead.

P-values use the Satterthwaite denominator-df approximation when the
\code{lmerTest} package is available (recommended), and fall back to a
conservative t-approximation with residual df otherwise.
}
\section{Interpreting coefficients}{

\strong{Raw scale (\code{auto_log = FALSE} or skewness <= 1):} An estimate of \code{+5.2}
for \code{insuranceMedicaid} means physicians called with Medicaid had, on
average, \strong{5.2 more wait days} than the reference group.

\strong{Log scale (\code{auto_log = TRUE}, skewness > 1):} Coefficients are on the
\code{log1p(days)} scale. Use \code{gmr_table} for interpretation: a GMR of \code{0.87}
means the group waits ~13\% fewer days (* (days + 1)) than the reference.
Report as: "GMR = 0.87 (95\% CI 0.49-1.54), p = 0.631."
}

\section{R-squared}{

Marginal R^2 reflects variance explained by fixed effects alone;
conditional R^2 includes the physician random intercept. Both are computed
from the variance-component decomposition (Nakagawa & Schielzeth 2013) and
do not require any additional packages.
}

\examples{
\dontshow{if (requireNamespace("lme4", quietly = TRUE)) withAutoprint(\{ # examplesIf}
set.seed(42)
df <- data.frame(
  wait_days = round(rnorm(80, mean = 21, sd = 8)),
  insurance = rep(c("Medicaid", "BCBS"), 40),
  physician = rep(paste0("Dr", 1:20), each = 4),
  stringsAsFactors = FALSE
)
fit <- mysterycall_lmm(df, "wait_days", "insurance", "physician")
print(fit)
\dontshow{\}) # examplesIf}
}
\references{
Nakagawa S, Schielzeth H (2013). A general and simple method for obtaining
R^2 from generalized linear mixed-effects models. \emph{Methods in Ecology and
Evolution} 4(2):133-142. \doi{10.1111/j.2041-210x.2012.00261.x}
}
\seealso{
\code{\link[=plot.mysterycall_lmm]{plot.mysterycall_lmm()}} for Q-Q and residual plots;
\code{\link[=mysterycall_poisson_model]{mysterycall_poisson_model()}} for count outcomes;
\code{\link[=mysterycall_nb_model]{mysterycall_nb_model()}} for overdispersed counts;
\code{\link[=mysterycall_model_comparison_table]{mysterycall_model_comparison_table()}} to compare models by AIC/BIC.

Other outcomes: 
\code{\link{.as_positive_logical}()},
\code{\link{.disp_rate_ci}()},
\code{\link{.fmt_model_pval}()},
\code{\link{.fmt_pvalue}()},
\code{\link{.me_find_mf_col}()},
\code{\link{.me_mf_to_orig}()},
\code{\link{.wait_stats}()},
\code{\link{.wilson_ci}()},
\code{\link{mysterycall_acceptance_rate}()},
\code{\link{mysterycall_acceptance_rate_calc}()},
\code{\link{mysterycall_auto_model}()},
\code{\link{mysterycall_bootstrap_ci}()},
\code{\link{mysterycall_calendar_sensitivity}()},
\code{\link{mysterycall_caller_drift}()},
\code{\link{mysterycall_check_zero_inflation}()},
\code{\link{mysterycall_forest_plot}()},
\code{\link{mysterycall_icc}()},
\code{\link{mysterycall_icc_report}()},
\code{\link{mysterycall_icc_sentence}()},
\code{\link{mysterycall_impute_calls}()},
\code{\link{mysterycall_interaction_sentences}()},
\code{\link{mysterycall_interaction_table}()},
\code{\link{mysterycall_irr_plot}()},
\code{\link{mysterycall_kaplan_meier}()},
\code{\link{mysterycall_logistic_model}()},
\code{\link{mysterycall_marginal_effects}()},
\code{\link{mysterycall_missing_data_analysis}()},
\code{\link{mysterycall_model_metrics}()},
\code{\link{mysterycall_nb_model}()},
\code{\link{mysterycall_nb_power}()},
\code{\link{mysterycall_overdispersion_test}()},
\code{\link{mysterycall_plot_distribution}()},
\code{\link{mysterycall_plot_effect}()},
\code{\link{mysterycall_plot_emmeans_full}()},
\code{\link{mysterycall_plot_emmeans_interaction}()},
\code{\link{mysterycall_plot_inclexcl}()},
\code{\link{mysterycall_plot_residuals}()},
\code{\link{mysterycall_plot_sjplot_interaction}()},
\code{\link{mysterycall_plot_stacked_bar}()},
\code{\link{mysterycall_poisson_model}()},
\code{\link{mysterycall_power_curve}()},
\code{\link{mysterycall_predicted_means}()},
\code{\link{mysterycall_screen_interactions}()},
\code{\link{mysterycall_screen_predictors}()},
\code{\link{mysterycall_select_best_model}()},
\code{\link{mysterycall_sensitivity}()},
\code{\link{mysterycall_wait_time_crossover}()},
\code{\link{mysterycall_wait_time_summary}()},
\code{\link{plot.mysterycall_lmm}()},
\code{\link{print.mysterycall_acceptance_rate_calc}()},
\code{\link{print.mysterycall_auto_model}()},
\code{\link{print.mysterycall_calendar_sensitivity}()},
\code{\link{print.mysterycall_caller_drift}()},
\code{\link{print.mysterycall_icc}()},
\code{\link{print.mysterycall_icc_report}()},
\code{\link{print.mysterycall_impute_calls}()},
\code{\link{print.mysterycall_interaction_table}()},
\code{\link{print.mysterycall_lmm}()},
\code{\link{print.mysterycall_logistic_model}()},
\code{\link{print.mysterycall_missing_data}()},
\code{\link{print.mysterycall_nb_model}()},
\code{\link{print.mysterycall_nb_power}()},
\code{\link{print.mysterycall_poisson_model}()},
\code{\link{print.mysterycall_power_curve}()},
\code{\link{print.mysterycall_predicted_means}()},
\code{\link{print.mysterycall_sensitivity}()},
\code{\link{print.mysterycall_wait_time_crossover}()},
\code{\link{tidy.mysterycall_lmm}()},
\code{\link{tidy.mysterycall_logistic_model}()},
\code{\link{tidy.mysterycall_poisson_model}()}
}
\concept{outcomes}
