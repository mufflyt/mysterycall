# Print a `mysterycall_simple_poisson` object

Print a `mysterycall_simple_poisson` object

## Usage

``` r
# S3 method for class 'mysterycall_simple_poisson'
print(x, digits = 3, ...)
```

## Arguments

- x:

  A `mysterycall_simple_poisson` object.

- digits:

  Number of decimal places for printed estimates. Default `3`.

- ...:

  Ignored; present for S3 method consistency.

## Value

`x`, invisibly.
