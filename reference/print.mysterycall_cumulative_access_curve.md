# Print a `mysterycall_cumulative_access_curve` object

Print a `mysterycall_cumulative_access_curve` object

## Usage

``` r
# S3 method for class 'mysterycall_cumulative_access_curve'
print(x, ...)
```

## Arguments

- x:

  A `mysterycall_cumulative_access_curve` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

`x`, invisibly.
