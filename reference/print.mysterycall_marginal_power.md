# Print a `mysterycall_marginal_power` object

Print a `mysterycall_marginal_power` object

## Usage

``` r
# S3 method for class 'mysterycall_marginal_power'
print(x, ...)
```

## Arguments

- x:

  A `mysterycall_marginal_power` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

`x`, invisibly.
