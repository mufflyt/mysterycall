# Print a `mysterycall_matched_controls` object

Print a `mysterycall_matched_controls` object

## Usage

``` r
# S3 method for class 'mysterycall_matched_controls'
print(x, ...)
```

## Arguments

- x:

  A `mysterycall_matched_controls` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

`x`, invisibly.
