# Coerce a `mysterycall_consistency_report` object to a data frame

Coerce a `mysterycall_consistency_report` object to a data frame

## Usage

``` r
# S3 method for class 'mysterycall_consistency_report'
as.data.frame(x, ...)
```

## Arguments

- x:

  A `mysterycall_consistency_report` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

A `data.frame` representation of `x`.
