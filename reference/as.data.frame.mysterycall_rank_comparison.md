# Coerce a `mysterycall_rank_comparison` object to a data frame

Coerce a `mysterycall_rank_comparison` object to a data frame

## Usage

``` r
# S3 method for class 'mysterycall_rank_comparison'
as.data.frame(x, ...)
```

## Arguments

- x:

  A `mysterycall_rank_comparison` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

A `data.frame` representation of `x`.
