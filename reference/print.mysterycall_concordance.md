# Print a `mysterycall_concordance` object

Print a `mysterycall_concordance` object

## Usage

``` r
# S3 method for class 'mysterycall_concordance'
print(x, ...)
```

## Arguments

- x:

  A `mysterycall_concordance` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

`x`, invisibly.
