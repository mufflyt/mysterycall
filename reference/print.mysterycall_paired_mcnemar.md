# Print a `mysterycall_paired_mcnemar` object

Print a `mysterycall_paired_mcnemar` object

## Usage

``` r
# S3 method for class 'mysterycall_paired_mcnemar'
print(x, ...)
```

## Arguments

- x:

  A `mysterycall_paired_mcnemar` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

`x`, invisibly.
