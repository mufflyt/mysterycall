# Coerce a `mysterycall_paired_wait` object to a data frame

Coerce a `mysterycall_paired_wait` object to a data frame

## Usage

``` r
# S3 method for class 'mysterycall_paired_wait'
as.data.frame(x, ...)
```

## Arguments

- x:

  A `mysterycall_paired_wait` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

A `data.frame` representation of `x`.
