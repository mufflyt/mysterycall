# Print a `mysterycall_hurdle_wait` object

Print a `mysterycall_hurdle_wait` object

## Usage

``` r
# S3 method for class 'mysterycall_hurdle_wait'
print(x, ...)
```

## Arguments

- x:

  A `mysterycall_hurdle_wait` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

`x`, invisibly.
