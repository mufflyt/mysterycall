# Coerce a `mysterycall_multiresponse` object to a data frame

Coerce a `mysterycall_multiresponse` object to a data frame

## Usage

``` r
# S3 method for class 'mysterycall_multiresponse'
as.data.frame(x, ...)
```

## Arguments

- x:

  A `mysterycall_multiresponse` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

A `data.frame` representation of `x`.
