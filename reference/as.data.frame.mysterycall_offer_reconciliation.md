# Coerce a `mysterycall_offer_reconciliation` object to a data frame

Coerce a `mysterycall_offer_reconciliation` object to a data frame

## Usage

``` r
# S3 method for class 'mysterycall_offer_reconciliation'
as.data.frame(x, ...)
```

## Arguments

- x:

  A `mysterycall_offer_reconciliation` object.

- ...:

  Ignored; present for S3 method consistency.

## Value

A `data.frame` representation of `x`.
