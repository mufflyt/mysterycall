# Shift Holiday to Observed Weekday

Shifts a fixed-date holiday to its observed weekday (Friday if Saturday,
Monday if Sunday).

## Usage

``` r
.mc_observe(d)
```

## Arguments

- d:

  Date object.

## Value

Date object (possibly shifted).
