on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

name: test-coverage

jobs:
  test-coverage:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      GITHUB_PAT: ${{ secrets.GITHUB_TOKEN }}
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
      # See the nightly coverage job: Config/testthat/parallel makes the suite
      # fan out across callr subprocesses, and a worker that dies under covr
      # takes the run down as an opaque .Rout.fail. Serial under coverage.
      TESTTHAT_PARALLEL: 'FALSE'
    steps:
      - uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5
      - uses: r-lib/actions/setup-r@d3c5be51b12e724e68f33216ca3c148b66d5f0b6 # v2
        with:
          use-public-rspm: true
      - uses: r-lib/actions/setup-r-dependencies@d3c5be51b12e724e68f33216ca3c148b66d5f0b6 # v2
        with:
          extra-packages: any::covr, any::xml2
          needs: coverage
          cache-version: 2
      - name: Test coverage
        run: |
          cov <- covr::package_coverage(
            quiet = FALSE,
            clean = FALSE,
            install_path = file.path(normalizePath(Sys.getenv("RUNNER_TEMP"), winslash = "/"), "package")
          )
          covr::to_cobertura(cov)
        shell: Rscript {0}
      - name: Show test failures
        if: failure()
        run: |
          fail_files <- Sys.glob(file.path(
            normalizePath(Sys.getenv("RUNNER_TEMP"), winslash = "/"),
            "package/mysterycall/mysterycall-tests/testthat.Rout.fail"
          ))
          if (length(fail_files) == 0) {
            fail_files <- Sys.glob(file.path(
              normalizePath(Sys.getenv("RUNNER_TEMP"), winslash = "/"),
              "package/*/*-tests/testthat.Rout.fail"
            ))
          }
          for (f in fail_files) {
            if (file.exists(f)) {
              cat("── Failures from", f, "──\n")
              lines <- readLines(f, warn = FALSE)
              # Print last 200 lines to capture failure block without drowning in R startup noise
              cat(tail(lines, 200), sep = "\n")
            }
          }
        shell: Rscript {0}
      - name: Print coverage summary
        run: |
          if (file.exists("cobertura.xml")) {
            xml <- xml2::read_xml("cobertura.xml")
            pct <- as.numeric(xml2::xml_attr(xml, "line-rate")) * 100
            cat(sprintf("Coverage: %.1f%%\n", pct))
          }
        shell: Rscript {0}
      - uses: codecov/codecov-action@0fb7174895f61a3b6b78fc075e0cd60383518dac # v5
        with:
          fail_ci_if_error: false
          files: ./cobertura.xml
          disable_search: true
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: mufflyt/mysterycall
