# These functions have moved to the mysterymaps package.
# The tests for them live in that package.
test_that("placeholder — tests moved to mysterymaps", {
  skip("Functions in this file have moved to the mysterymaps package")
})
